# פערים שנמדדו במנוע — ומה לא נשלח בגללם

המסמך הזה קיים כדי שאיש לא יחקור שוב את מה שכבר נמדד. כל שורה כאן היא
תוצאה של הרצה ב-Chrome אמיתי מול ה-`dist` הארוז, ולא קריאה של טיפוסים.

**הכלל שנגזר מכל אלה:** `available: true` בקטלוג היכולות ו-`success: true`
בקבלה **אינם הוכחה שהפעולה עובדת**. פעולה שכותבת קוד שדה של Word חייבת
אימות מול תיעוד Word, ורצוי גם מול ה-docx המיוצא.

המתכון למדידה נמצא בסוף `docs/spike.md` ובקוד: `scripts/cdp.mjs`.

**על הגרסאות שבקובץ הזה:** סבב המדידה של 15.9.2026 נעשה כולו על
`superdoc` **2.15.0-next.15**, וכל מקום שכתוב בו „נמדד על 2.15.0-next.15”
הוא מדידה שנעשתה שם בפועל. התוסף עצמו נעול היום על **2.15.0 היציבה**
(‏`@superdoc/docx-engine` 0.14.0), שיצאה באותו יום מאותו קו גרסאות —
ומדידות לא הורצו מחדש עליה.

## מסמך שהקפיא את המנוע לצמיתות — נסגר ב-2.13.0

### `w:defaultTabStop` של אפס — לולאה שאין ממנה יציאה

**נסגר — נמדד על superdoc 2.15.0-next.15 (15.9.2026); כבר ב-2.13.0.** אותו
מסמך בדיוק, עם ההגנה שלנו מנוטרלת בדף (הקריאה היחידה של השלב המקדים אל
ה-`data:` נדחית, ולכן הוא מוסר למנוע את הבייטים כמות שהם — מאומת בייצוא:
`w:val="0"` הגיע למנוע, מול `720` כשההגנה פועלת) נפתח ב-4.7–5.2 שניות, והדף
ענה לכל דגימה במהלך הפתיחה. הגלאי אומת מול מנועים ישנים באותו גשש עצמו:
ב-2.12.0 אפס מתוך עשר דגימות נענו בשישים שניות — כלומר קפוא — וב-2.13.0
וב-2.14.0-next.5 המסמך נפתח. כלומר התיקון נחת ב-2.13.0, ולא ב-2.15.
ה-issue למעלה **סגור** (4.9.2026).

**וההגנה נשארת, בהכרעה ולא בהרגל** (הכרעת PR #67): היא זולה, היא מגנה על מי
שמריץ מארח שננעל על מנוע ישן — 2.12.0 עדיין קופא — ואי אפשר להסיר אותה
לבדה, מפני שאותו שלב מקדים נושא גם את תיקון ה-`bCs` שבסעיף הבא. מה שהתיישן
הוא הנימוק ולא ההכרעה: מה שכתוב מכאן ולמטה מתאר לולאה **חיה**, והיא היסטוריה
מ-2.12.0 ומטה.

`<w:defaultTabStop w:val="0"/>` ב-`word/settings.xml` מקפיא את החוט הראשי
לתמיד: לא `onReady`, לא `onException`, ו-100% מעבד. נמדד מעל עשר דקות לפני
שוויתרנו. נמדד ב-superdoc 2.8.0 (engine 0.7.0) **וגם** ב-2.10.0 (engine 0.9.0),
ובאותה מידה מ-`file://` ומ-`http://127.0.0.1` עם workers אמיתיים.

זה הפער היחיד כאן שאי אפשר להתגונן מפניו אחרי שהמסמך נמסר: `OPEN_TIMEOUT_MS`
צריך בדיוק את החוט שחסום, ולכן אינו יורה. שום שעון-שמירה אינו יכול לתפוס את
זה. לכן ההגנה היא **לפני** המסירה — `src/engine/docx-preflight.ts`.

מה שנמדד בבידוד, כדי שלא ייחקר שוב:

| מה נבדק | תוצאה |
|---|---|
| הקובץ שדווח, כמות שהוא | קופא |
| אותו קובץ, `0` הוחלף ב-`720`, `document.xml` זהה בייט-בבייט | נפתח |
| מסמך Word ריק ותקין, `defaultTabStop` הוכרח ל-`0` | קופא |
| אותו קובץ עם גוף שכולו `<w:p/>` ריקה | נפתח |
| אותו קובץ עם פסקה אחת שיש בה טקסט | קופא |

השורות האחרונות הן מה שקובע את היקף ההגנה: אין צורך ב-`<w:tab/>` ואין צורך
בעצירות טאב מפורשות. די בכך שיש טקסט לפרוס.

דווח למעלה: https://github.com/superdoc/docx-editor/issues/3944

**וההגנה עצמה הייתה חלקית, ואינה עוד.** הרגקס שכתב את הערך
(`/<w:defaultTabStop\b[^>]*\/>/`) לא הותאם בשלוש צורות חוקיות לגמרי, ובכל אחת
מהן הקיפאון פשוט לא נמנע: הערה שיש בה ערך תקין **לפני** האלמנט החי (ההתאמה
הראשונה היא זו שבהערה), אלמנט שאינו סוגר את עצמו
(`<w:defaultTabStop w:val="0"></w:defaultTabStop>`), ו-`>` בתוך ערך של מאפיין
אחר. בכיוון ההפוך, הערך **בתוך** הערה תוקן. שלושתן מכוסות עכשיו, וההגנה עוברת
דרך הסורק המודע-למרכאות של הסעיף הבא. אין שער CDP על המסלול הזה — רק בדיקות
יחידה — מפני שמסמך שמפעיל אותו מקפיא את הדפדפן ולא נותן לשער דרך לדווח.

## מסמך עברי שכותרותיו מודגשות ב-Word ומגיעות אלינו דקות

### `w:bCs` אינו מרונדר — הדגשת כתב מורכב נבלעת

`<w:bCs/>` היא „מודגש לכתב מורכב”, וזה מה ש-Word העברי כותב כשמדגישים בחירה
שכולה עברית: `w:bCs` לבדה, בלי `w:b`. Word מרנדר הדגשה של ריצה עברית מ-`bCs`;
המנוע מרנדר אותה מ-`w:b` **בלבד**.

נמדד ב-superdoc 2.10.0 (engine 0.9.0) **וגם** ב-2.11.0 (engine 0.10.0), Chrome
אמיתי על ה-dist הארוז, על הקובץ שדווח (סגנונות „כותרת 2/3/4/5” נושאים
`<w:bCs/>` ואין בהם `<w:b/>`):

| מה נבדק | `font-weight` שנצבע |
|---|---|
| הקובץ שדווח, כמות שהוא | 400 בכל עשר הכותרות |
| אותו קובץ, `<w:b/>` הושלם לצד כל `<w:bCs/>` | 700 בכולן |
| `bCs` על **הריצה** (עיצוב ישיר), בלי `b` | 400 |
| `bCs` על **`w:rPrDefault`** ב-`styles.xml`, בלי `b` | 400 |

**נמדד מחדש על 2.15.0-next.15 (15.9.2026) — פתוח.** על המנוע לבדו (התיקון
המקומי נוטרל בדף, בלי מתג ובלי עריכת קוד — אין כזה — ואומת שהייצוא אכן נושא
`w:bCs` בלי `w:b` ושההודעה למשתמש לא הופיעה): `bCs` על הריצה 400, `bCs` על
`w:rPrDefault` 400, **וגם `bCs` לצד `<w:rtl/>` על הריצה 400** — כלומר אפילו
המסלול המצומצם של PR #3958 אינו בבאנדל הזה. עם התיקון פועל: שש שורות המסך
עוברות, ו-`rPrChange` נכנס ויוצא נקי.

**ובאותה מדידה התברר שהשער עצמו לא מדד דבר:** ‏`open()` ב-
`scripts/qa/bold-cs-qa.mjs` קדם לדיאלוג „פתח קובץ”, ולכן מסמך הבדיקה לא
נפתח כלל ושלוש השורות שמודדות את המנוע לבדו חזרו אדומות על **כל** מנוע.
תוקן מאז (קומיט `3660214`): הפתיחה עוברת דרך „עיון בקבצים…”, ופתיחה שלא
קרתה זורקת במקום לדווח כשל מדומה. הלקח נשאר רשום כאן — **אדום בשער שאינו
מוכיח שהמסמך נפתח אינו עדות על המנוע.**

זה **הכיוון ההפוך** לפער שמתועד למטה על `format.apply` (שם המנוע כותב `w:b`
בלי `w:bCs`, ולכן Word אינו מציג את מה שאנחנו מציגים). שני הפערים הם אותה
עובדה אחת: המנוע מכיר `b` ואינו מכיר `bCs`.

**מה נעשה כאן:** ההשלמה נעשית ב-`src/engine/docx-preflight.ts`, לפני שהמנוע רואה את
הבייטים. לא בגיליון סגנונות, ומשתי סיבות שנמדדו: ה-DOM שהמנוע מצייר אינו נושא
שום סימן ל-`bCs` (`.superdoc-text-run` מקבל `styleid` ו-`font-weight` מחושב
ב-`style`, ולא את מאפייני הריצה), והמשקל נכנס לחישוב שבירת השורות — הדגשה
שנצבעת אחרי הפריסה מזיזה טקסט מתחת לפריסה שכבר חושבה.

השער: `scripts/qa/bold-cs-qa.mjs`. הוא אינו ריק — עם התיקון מושבת בבאנדל
נופלות שתי שורות המסך שנבנות מהייצוא (`bCs` על הריצה, `bCs` על ברירות המחדל)
וגם „הודעה למשתמש”, ובקרה של „טקסט בלי `bCs` נשאר דק” עוברת בשני המצבים.
השורה של הקובץ שדווח נמדדת רק כשנמסר לו נתיב — ב-`verify:qa` היא „לא נבדק”,
מפני שהקובץ אינו במאגר.

`<w:iCs/>` (נטוי לכתב מורכב) הוא אותו מנגנון בדיוק, וכך גם `w:szCs` ו-
`w:rFonts/@cs`: המנוע מונה את ארבעתם כ„מחסנית הכתב המורכב” ואינו מיישם אף אחד
מהם. כאן תוקנה הדגשה בלבד — זה מה שדווח.

**ושתי הרגליים האחרות דווחו בנפרד (9.9.2026):**
https://github.com/superdoc/docx-editor/issues/3994 — הגודל והגופן, והן פער חמור
מההדגשה ולא קל ממנה. `resolveFontSize` ב-`run-attrs.ts:80` קורא `props.fontSize`
לבדו, ו-`resolveFontFamily` (שם, `:165`) מגיע לחריץ ה-`cs` **רק אם** כל המסלול
הלטיני — ‏`ascii`, `hAnsi`, ערכות הנושא של שניהם ו-`eastAsia` — החזיר ריק. כלומר
ריצה עברית נמדדת ונצבעת בגופן ובגודל של הכתב הלטיני, וההערה בפונקציה אומרת זאת
בעצמה: המסלול הלטיני הוא „the only thing v2 paints generically right now”.

‏`TextRunStyleAttrs` מחזיק `fontFamily` אחד ו-`fontSize` אחד, ולכן בחירת המחסנית
חייבת לקרות במנרמל עצמו — בדיוק היכן ש-PR #3958 עושה אותה להדגשה ולנטייה, דרך
`usesComplexScriptToggles`.

**וגם התוצאה נמדדה ב-Chrome, לא רק הוסקה מהמקור:** השער
`scripts/qa/font-advanced-qa.mjs` פותח את ה-`dist` ב-Chrome אמיתי, מחיל
`fontSizeCs: 28` על ריצה עברית, ומאמת גם את `<w:szCs w:val="56"/>` ב-docx
המיוצא וגם שאין שינוי במסך. זו שורת `fontSizeCs` בטבלת המדידה למטה; קריאת הקוד
כאן מסבירה *למה* המדידה הזאת נכשלת, ולא מחליפה אותה. ה-issue מוסיף מסמך שחזור
סינתטי ובקרה לטינית שתיקון אינו רשאי לגעת בה; ההסבר המנגנוני שבו נבנה מהקוד
הפתוח בלבד.

**נמדד מחדש על 2.15.0-next.15 (15.9.2026) — פתוח, ללא שינוי.** על ריצה חיה:
`fontSizeCs: 28` נכתב `<w:szCs w:val="56"/>` והריצה נשארת 16px, ו-
`rFonts: {cs: 'Courier New'}` נכתב והמשפחה נשארת Arial. מקובץ: ריצה עברית עם
`ascii`/`hAnsi` ‏Arial, ‏`cs` ‏Courier New, ‏`sz` 24, ‏`szCs` 56 ו-`<w:rtl/>`
נצבעת Arial 16px (וכך גם עם `<w:cs/>` במקום `w:rtl`), בעוד בקרה הפוכה
(`sz` 56 / `szCs` 24) נצבעת 37.33px — כלומר הגודל והגופן מגיעים מהחריצים
הלטיניים בלבד. **ומה שנוסף לידיעה:** מסמך אמיתי שמצהיר את הגופן ב-`w:cs`
בלבד (עם `w:hint="cs"` ו-`<w:rtl/>` על כל ריצה) נצבע ב-`Calibri` של ערכת
הנושא הלטינית — כלומר גופן שהמשתמש מעולם לא ביקש, ולא נפילה לברירת מחדל
עברית.

**מה השלב המקדים אינו מכסה, ובמפורש:** הוא רץ **בפתיחה** בלבד. „מודגש (מורכב)”
בדיאלוג הגופן המתקדם כותב `bCs` על ריצה חיה (ראו `engine/font-advanced.ts`,
והסעיף על `format.apply` למטה), וההדגשה הזאת לא תיראה על המסך עד שהקובץ ייפתח
מחדש. זו נגזרת של אותה עובדה שנמדדה — המנוע מרנדר מ-`w:b` בלבד — ולא מדידה
נפרדת. מה שסוגר את זה הוא התיקון במעלה הזרם, שיושב בשכבת הרינדור ולכן חל גם
על עריכה חיה.

### התיקון במעלה הזרם

**הפער הוא בקוד ה-OSS של המנוע, ולא בחלק הקנייני.** הפירוק של `w:bCs` אכן
קורה — `RunProperties.boldCs` מגיע מלא — אבל
`packages/layout-engine/style-engine/src/normalize/run-attrs.ts` (הפונקציה
`normalizeRunAttrsFromOoxml`) קורא רק את הווריאנטים הלטיניים, ומשם והלאה חוזה
הפריסה נושא `bold` בודד. אימות שהפונקציה הזאת אכן על מסלול הרינדור: הצורה
המהודרת שלה נמצאת בתוך `@superdoc/docx-engine/dist/docx-engine.es.js` שורה
אחר שורה.

המנוע עצמו מתעד את זה כעבודה שלא נעשתה —
`layout-engine/contracts/src/direction-context.ts`: „preservation-only in Wave
1a. **Wave 1b** implements the stack-selection logic (`resolveRunScriptContext`
…)”. `resolveRunScriptContext` אינו ממומש בשום קובץ — השם מופיע רק בהערה
הזאת (גם ב-`.d.ts` שבחבילה), ולא בשום `.js` — וגם לא ב-0.10.0 שיצא מאז.

דווח למעלה: https://github.com/superdoc/docx-editor/issues/3959
תיקון נשלח: https://github.com/superdoc/docx-editor/pull/3958 — בחירת מחסנית
לפי `w:rtl`/`w:cs` עם נפילה חזרה לדגל הלטיני, ובדיקות.

**ושני התיקונים אינם כפילות — נמדד.** התיקון של המנוע הוחל על הבאנדל הארוז
בצורתו המהודרת, עם התיקון המקומי **מושבת**, ואותו שער רץ עליו:

| מה נמדד | המנוע לבדו | התיקון המקומי לבדו |
|---|---|---|
| הקובץ שדווח (ריצות עם `<w:rtl/>`) | 10/10 מודגשות | 10/10 מודגשות |
| `bCs` על ריצה **בלי** `w:rtl`/`w:cs` | 400 — לא מודגש | 700 |
| `bCs` על `w:rPrDefault`, בלי `w:rtl` | 400 — לא מודגש | 700 |

וזה בדיוק הגבול התיאורטי: לפי Annex I, בהיעדר `w:rtl` ו-`w:cs` המחסנית נבחרת
לפי **הכתב של הטקסט**, וזה מידע שאינו מגיע לשכבה שבה התיקון של המנוע יושב. Word
העברי כותב `w:rtl` על ריצות עבריות, ולכן המנוע יכסה את המסמכים המציאותיים —
והתיקון כאן נשאר הרחב מהשניים.

### מה מסיר את השלב המקדים, ומתי

השלב המקדים **משנה את המסמך** (`w:b` שהוזרק נשמר לקובץ), ולכן הוא אינו אמור
להישאר מטעמי הרגל אחרי שהמנוע יתוקן. תנאי ההסרה, שניהם יחד:

1. superdoc/docx-editor#3958 (או שקול לו) נכלל בגרסת `@superdoc/docx-engine`
   ש-`package.json` מצביע עליה.
2. `scripts/qa/bold-cs-qa.mjs` רץ עם `repairComplexScriptBold` **מושבת**, ושלוש
   שורות המסך עוברות: `bCs` על הריצה, `bCs` על ברירות המחדל, והקובץ שדווח —
   שדורש להעביר לשער את הנתיב שלו, כי ב-`verify:qa` הוא מדולג.

ומה שנשאר פתוח גם אז, ומוכרע רק כשמגיעים לשם: שתי השורות של „בלי `w:rtl`”
בטבלה שלמעלה יישארו דקות במנוע גם אחרי #3958, וזו בחירה בין השארת ההזרקה
כרשת רחבה יותר (עם המחיר של שינוי המסמך) לבין הסתפקות בהתנהגות של Word — שגם
הוא, על ריצה בלי `w:rtl`/`w:cs`, מכריע לפי הכתב ולא לפי הדגל.

עד אז אין שער גרסה ואין מתג: השלב רץ על כל מסמך, ומסמכים שנשמרו בינתיים
נושאים את `w:b` לתמיד. זה מקובל בכוונה — `w:b` לצד `w:bCs` הוא מה ש-Word עצמו
כותב כשמדגישים בחירה מעורבת, ואינו מסמך „מקולקל”.

**זיכרון בפתיחה.** החלק המתוקן נכתב דחוס (`CompressionStream('deflate-raw')`,
מאומת בפריסה חוזרת והשוואת CRC, ונופל ל-`STORED` כשהדחיסה חסרה או נכשלת).
הגרסה הראשונה כתבה אותו גלוי, ועל 81 מסמכים אמיתיים זה היה פי 5.2 בסך הכול
ופי 15.3 בגרוע — 35.9MB למנוע מקובץ של 4.3MB. הפרטים בכותרת
`src/engine/docx-preflight.ts`.

## תוכן עניינים — שני פערים שהתגלו מדיווח משתמש

הדיווח היה שני משפטים: „תוכן העניינים נוצר רק בכותרת אחת גם כאשר מגדירים
התאמה לשאר הכותרות”, ו„בכותרת אחת הוא מוצג לצד שמאל ללא הפרדה בין הכותרת
למספר העמוד”. שני הסימפטומים, ואף אחד מהם אינו בתוכן העניינים עצמו. הפער
השלישי שמתועד כאן מסביר למה לא מוסיפים הזחה ישירה לשורות עד שהמנוע יתוקן.

**נמדד מחדש על 2.13.0 / docx-engine 0.12.0 (8.9.2026), בעץ שאין בו את התיקון
שלנו:** ארבעת הממצאים משחזרים זהה — הגלריה שבעה פריטים בלי „כותרת 2”, החלה
בפקודה מצליחה ואינה פותחת אותה, שורות הטבלה בלי `w:tabs` ובלי הזחה, אין סגנון
`TOC*` ב-`styles.xml`, מספר עמוד שחורג מאזור הטקסט בעצירה ב„סוף”, והזחה שגוררת
את העצירה. שני דברים נוספו למה שידענו: `toc.configure` עם `tabLeader` או
`rightAlignPageNumbers` מחזיר `success: true` בלי אזהרות, והערך אינו חוזר
ב-`displayConfig` של `toc.list` — בעוד `outlineLevels` באותו patch **כן** משנה
את ה-`instruction`; וה-`instruction` שההכנסה יוצרת הוא `TOC \h` בלבד, בלי `\o`.

**ונמדד שוב על 2.15.0-next.15 (15.9.2026), על המנוע לבדו** (גשש שקורא ישירות
`ui.styles.getSnapshot().quickGallery` ו-`doc.styles.getCatalog`, ומכניס תוכן
עניינים דרך הפקודה בלי שכבת הפריסה שלנו; שתי ריצות): **שלושת הפערים פתוחים
ללא שינוי.** הגלריה מחזירה את אותם שבעה פריטים לפני החלת „כותרת 2”/„כותרת 3”
ואחריה (ההחלה עצמה מצליחה ו-`pStyle` נכתב), ו-`semiHidden` עם `unhideWhenUsed`
נשארים ב-`styles.xml` גם אחרי השימוש. ההכנסה הגולמית כותבת ארבע שורות
(TOC1/TOC2/TOC3/TOC1 — האיסוף לפי רמות עובד), כל אחת עם `<w:tab/>` יחיד, בלי
`w:tabs` ובלי `w:ind`, אין סגנון `TOC*` כלל, וה-`instruction` הוא `TOC \h`;
על המסך אותם מספרים בדיוק כמו במדידת 2.13 — הכותרת ב-x=631 ומספר העמוד
ב-x=593, 38 פיקסלים, בלי מוביל. `toc.configure` עם `tabLeader` ו-
`rightAlignPageNumbers` עדיין מחזיר `success: true` ואינו משנה דבר.

**דווח ל-upstream (8.9.2026), שלושה אישוז:**
[#3987](https://github.com/superdoc/docx-editor/issues/3987) — `unhideWhenUsed`
אינו ממומש ולכן „כותרת 2”–„כותרת 9” אינן מגיעות לגלריה;
[#3988](https://github.com/superdoc/docx-editor/issues/3988) — הכנסת תוכן
עניינים בלי עצירת טאב, ו-`TOC1`…`TOC9` שאינם נוצרים;
[#3989](https://github.com/superdoc/docx-editor/issues/3989) — עצירות טאב
שאינן מודעות לכיוון הפסקה (מיפוי `left`/`right` ל-`start`/`end` בלי כיוון,
עצירה ב„סוף” שחורגת, והזחה שגוררת את העצירה). הטיעון בשלושתם נבנה מהקוד הפתוח
ומהתבנית `shared/common/data/blank.docx` בלבד, בלי מדידות מנוע — ראו הרישיון
ב-`DOCX-ENGINE-LICENSE.md`. שני החלקים הראשונים של #3989 יושבים ב-`layout-engine`
ו-`word-layout`, כלומר ניתנים ל-PR.

### `quickGallery` מסנן רמות כותרת שהמסמך מסמן `semiHidden` — ולא פותח אותן בשימוש

זו הסיבה ל„רק כותרת אחת”. `word/styles.xml` של המסמך הריק שהמנוע ארוז איתו
מגדיר את `Heading2`…`Heading9` עם `<w:semiHidden/>` ו-`<w:unhideWhenUsed/>` —
בדיוק כמו התבנית של Word — ו-`ui.styles.getSnapshot().quickGallery` מסנן
אותם. מה שחזר משם, על ה-dist הארוז ומסמך חדש:

```
["Normal","Heading1","Title","Subtitle","Quote","IntenseQuote","ListParagraph"]
```

`styles.getCatalog({view:'all'})` מחזיר 218 פריטים ואף אחד מהם אינו
`Heading2`; עם `includeHidden: true` הוא כן מחזיר אותם. כלומר הסינון הוא
מכוון, וההסתרה היא של המסמך.

**מה שהופך אותו לפער ולא להתנהגות:** `w:unhideWhenUsed` פירושו „מוסתר עד
שמשתמשים בו”, ו-Word מסיר את הדגל בשימוש הראשון. המנוע אינו. נמדד:
`linked-style` עם `Heading2` החזיר `success: true`, הפסקה קיבלה
`<w:pStyle w:val="Heading2"/>` — ו-`quickGallery` נשאר אותם שבעה פריטים
בדיוק. כלומר גם מסמך שהגיע מ-Word או מאוצריא **ומשתמש** ברמה 2 אינו מציע
אותה בגלריה.

ומה שכן עובד, כדי שלא ייחקר שוב: `toc.configure` כותב את המתג נכון
(ה-`instruction` נעשה `TOC \o "1-3" \h`), והאיסוף אוסף — פסקת `Heading2`
נאספה אל הטבלה כשורת `TOC2`. הטבלה בנתה רמה אחת מפני שבמסמך הייתה רמה אחת.

**מה נעשה כאן:** הגלריה משלימה את רמות 1–3 בעצמה
(`withHeadingLadder` ב-`src/engine/style-gallery.ts`), ומעליהן מה שהמסמך
עצמו מציע. התיקון בשכבת הגלריה ולא בתבנית „מסמך חדש”, מפני שהוא היחיד שרואה
גם מסמכים שנפתחו מהדיסק.

### אין בשום שכבה עצירת טאב לשורות תוכן העניינים

זו הסיבה ל„מספר העמוד בלי הפרדה”. המנוע כותב את השורה עם תו טאב יחיד בין
הכותרת ל-`PAGEREF`:

```xml
<w:p><w:pPr><w:pStyle w:val="TOC1"/></w:pPr>…<w:r><w:tab/></w:r>… PAGEREF …</w:p>
```

ותו הטאב זקוק לעצירה שתגיד לאן הוא נדחף ואם מציירים משהו בדרך. נמדד
(`scripts/qa/toc-layout-qa.mjs`) שאין אף אחת:

* ב-`pPr` של שורות ה-TOC אין `w:tabs` כלל;
* הסגנונות `TOC1`…`TOC9` — המקום שבו Word מחזיק את העצירה הזאת — **אינם
  מוגדרים ב-`word/styles.xml`** של התבנית (33 סגנונות, ואף אחד מהם אינו
  `TOC*`), ולמנוע אין API לחבר סגנון חדש: `styles.apply` נוגע ב-`docDefaults`
  בלבד;
* ולכן התו נופל על `w:defaultTabStop`.

התוצאה על המסך, A4 עם שוליים של אינץ' (אזור טקסט 602px שמתחיל ב-x=96 בעמוד):
הכותרת ב-x=631 ומספר העמוד ב-x=593 — 38 פיקסלים ממנה, בלי נקודות, ובלי קשר
לרוחב העמוד.

**מה שכן עובד:** `format.paragraph.setTabStop` על פסקת שורה מקבל
`alignment: 'right'` ו-`leader: 'dot'`, כותב `<w:tab w:val="right" w:pos="…"
w:leader="dot"/>` קנוני, והצייר **כן** מצייר את המוביל — `div.superdoc-leader`
עם `border-bottom: 1px dotted`. העצירה גם שורדת `toc.update`. העלות הראשונית
נמדדה ב-26ms לשורה בממוצע (10 שורות ב-261ms); הקוד שומר מטמון לפי שורה ורוחב
עמוד, ולכן עדכון חוזר כותב רק שורות חדשות או שורות שמיקום העצירה שלהן השתנה.

### עצירת „סוף” בפסקה עברית מוצבת בהיפוך כפול, והטקסט חורג אל השוליים

הפער שנשאר פתוח, ובגללו העצירה שאנחנו כותבים נסוגה פנימה. בפסקה שכיוונה RTL
עצירה ב„סוף” אמורה להצמיד את הטקסט שאחרי הטאב אל הקצה השמאלי **מבפנים**;
המנוע מציב את הקצה **הימני** שלו על העצירה, כלומר הוא חורג משמאל ברוחב עצמו.
נמדד על אותו עמוד (אזור טקסט 9026 twips = 602px, מתחיל ב-x=96):

| העצירה שנכתבה | מספר העמוד על המסך |
|---|---|
| `right@9026` (רוחב אזור הטקסט) | x=87 — 9 פיקסלים **מחוץ** לאזור הטקסט |
| `left@9026` | x=87 — אותו מקום בדיוק, כלומר היישור אינו מבדיל כאן |
| `right@8826` | x=100 — בתוך אזור הטקסט |

**נמדד מחדש על 2.15.0-next.15 (15.9.2026) — פתוח, ועם בקרה שמחדדת אותו.**
הפעם על פסקאות שנושאות `w:tabs` ישירות (בלי תוכן העניינים ובלי הקוד שלנו),
A4 עם שוליים של אינץ', אזור טקסט x=96..698, שתי ריצות זהות ועוד ריצה זהה על
2.14.0-next.5: ב-`right@9026` **וגם** ב-`end@9026` תיבת המספר יושבת 88..96,
כלומר הקצה הימני שלה בדיוק על העצירה והיא כולה 8 פיקסלים מחוץ לאזור הטקסט;
עם „123” התיבה 72..96, כלומר החריגה גדלה ברוחב המספר; וב-`right@8826` היא
101..109, בפנים. **הבקרה הלועזית באותו מסמך:** `right@9026` בפסקה לטינית
נותן 690..698 — בתוך אזור הטקסט, תקין. כלומר הפער תלוי-כיוון, וזה בדיוק
הטיעון של #3989.

**לא הוכרע:** שורת `left@9026` שבטבלה לא שוחזרה בפיקסטורה הזאת — שם
`left`/`start@9026` שובר את המספר לשורה שנייה בקצה ההתחלה, ולא מניח אותו
באותו מקום כמו `right`. אותה התנהגות ב-2.14.0-next.5 וב-2.15.0-next.15, ולכן
זהו הבדל פיקסטורה (הטבלה נמדדה דרך שורות TOC אמיתיות) ולא שינוי במנוע.

החריגה היא ברוחב הטקסט שאחרי הטאב, ולכן מספר תלת-ספרתי היה יוצא כפול ממנה.
`src/engine/toc.ts` מקזז אותה ב-`TOC_PAGE_NUMBER_RESERVE_TWIPS` (540 twips —
מכסה מספר בן ארבע ספרות בגופן שנמדד). אם המנוע יתקן, הקיזוז נשאר בטוח: הוא
רק מקרב את עמודת המספרים סנטימטר פנימה.

### עצירת טאב נמדדת מקצה הפסקה המוזחת, ולא משולי העמוד

הפער השלישי באותה משפחה, והוא נמצא כשנוספה ההזחה לפי רמה. ECMA-376 מגדיר את
`w:pos` של `w:tab` **ביחס לשולי העמוד**, כלומר הזחת פסקה אינה מזיזה את
עצירות הטאב שלה — ולכן ב-Word עמודת מספרי העמודים של תוכן העניינים מיושרת
למרות ש-`TOC2` ו-`TOC3` מוזחים. המנוע מודד מקצה ההתחלה של הפסקה **אחרי**
ההזחה.

נמדד על A4 עם שוליים של אינץ', אחרי `setIndentation({left})` על שורות
`TOC2`/`TOC3` (`w:ind w:left="220"` ו-`"440"`, כמו ב-Word):

| מה נכתב | קצה ימני של הכותרת | x של מספר העמוד |
|---|---|---|
| בלי הזחה | 698 / 698 / 698 | 123 / 123 / 123 |
| הזחה בלבד | 698 / 683 / 668 | 123 / **108** / **94** |
| הזחה + `position - indent` | 698 / 683 / 668 | 123 / 123 / 123 |

**נמדד מחדש על 2.15.0-next.15 (15.9.2026), ועל 2.14.0-next.5 באותם מספרים —
פתוח.** על פסקאות עם `w:tabs` ישירות, עצירת `right@8486` והזחת התחלה של
0 / 220 / 440 twips: המספר נוחת ב-x=124 / 109 / 95, כלומר העצירה זזה יחד עם
ההזחה, כ-14.7px לכל 220 twips. ‏`w:left="440"` בפסקה עברית התנהג בדיוק כמו
`w:start="440"`. **והבקרה הלועזית סוגרת את הטיעון:** אותה עצירה ואותה הזחה
בפסקה לטינית נותנות 654..662 בשני המקרים — העצירה **אינה** זזה. כלומר החלק
הזה של #3989 הוא RTL בלבד, ולא באג כללי של מיפוי ההזחה.

ההזחה עצמה **כן** נופלת בצד הנכון: `setIndentation({left})` נכתב כ-`w:start`
הלוגי, ובפסקה עברית הכותרת נדחפת מימין פנימה. אולם אין דרך לשמור את אותה
תוצאה גם ב-Word: פיצוי `position - indent` מתקן את SuperDoc אך שומר `w:pos`
שגוי לכל רמה ב-DOCX. לכן `layOutTocRows` אינה כותבת הזחה ישירה ואינה מפצה
אותה; כל השורות מקבלות אותו `w:pos`, ועמודת המספרים נשארת תקנית בשני העורכים.

## אפקטים שנכתבים לקובץ נכון, ואינם מצוירים על המסך

### „בחרתי עיצוב, לחצתי אישור, ולא קרה כלום”

ארבעת האפקטים של קבוצת „אפקטים” בדיאלוג „גופן מתקדם” — מסגרת לתו, צל, חרוט
ושקוע — נכתבים ל-docx **קנונית**, ו-Word מציג אותם. העורך אינו מצייר אף אחד
מהם. מבחינת מי שלחץ „אישור” זה בדיוק כמו פקד שאינו עובד, ואין שום סימן לכך
שמשהו קרה: הפעולה מדווחת הצלחה, ולכן גם שורת המצב נשארת ריקה.

נמדד ב-Chrome אמיתי על ה-dist הארוז, `format.apply` חי אחד לכל ריצה, פירוק
ה-zip של `export.toDocx` מול `getComputedStyle` על `.superdoc-text-run`:

המדידה חזרה בסבב שני, פקד אחד לכל ריצה, והפעם גם בהשוואת **צילום מסך
בייט-בבייט** של השורה לפני ואחרי — לא רק בסגנון המחושב:

| מה הוחל | ב-`document.xml` | מה צויר |
|---|---|---|
| `outline` | `<w:outline/>` | **כלום** (PNG זהה, 768/768 בתים) |
| `shadow` | `<w:shadow/>` | **כלום** (PNG זהה) |
| `emboss` | `<w:emboss/>` | **כלום** (PNG זהה) |
| `imprint` | `<w:imprint/>` | **כלום** (PNG זהה) |
| `kerning` | `<w:kern w:val="24"/>` | **כלום** — הצייר קובע `font-kerning: none` ואיש אינו מבטל אותו |
| `fontSizeCs` | `<w:szCs w:val="56"/>` | **כלום** — נמדד על טקסט עברי, כלומר כתב מורכב אמיתי |
| `bCs` / `iCs` / `cs` / `rFonts@cs` | קנוני | **כלום** — מחסנית הכתב המורכב, ראו למעלה |
| `dstrike` | `<w:dstrike/>` | קו חוצה **בודד**, לא כפול |
| `rtl` | `<w:rtl/>` | מגיע ל-DOM (`dir="rtl"`, `unicode-bidi: isolate`) |
| מתיחה / ריווח / הרמה / טקסט מוסתר | קנוני | **מצוירים במלואם** |

**נמדד מחדש על 2.15.0-next.15 (15.9.2026), שתי ריצות — פתוח, והטבלה עומדת
כמות שהיא.** ארבעת האפקטים נכתבים קנונית ואינם מצוירים (עובי מסגרת 0px,
מילוי אטום, `text-shadow` הוא `none`), `<w:dstrike/>` מצייר קו חוצה **בודד**
בסגנון `solid`, ‏`kern` משאיר `font-kerning: none` — גם כשהרף נמוך מגודל
הריצה, והרוחב זהה לבקרה בלי `w:kern` — ו-`szCs` אינו משנה דבר. כלומר ההודעה
בדיאלוג עדיין נכונה מילה במילה.

**והערה על השער, שאינה על המנוע:** שורת „גם על מה שאינו אפקט” ב-
`scripts/qa/font-advanced-qa.mjs` נצבעה אדומה בשתי הריצות מסיבה של השער
עצמו — הבחירה במקלדת על השורה המעורבת לא בודדה את המילה שעליה הוא מודד —
והיא הייתה אדומה באותה מידה על 2.14.0-next.5, כלומר לא רגרסיה של 2.15.
אותן כתיבות עצמן (`kerning`, `fontSizeCs`) הצליחו דרך ה-API בגשש. תוקן מאז
(קומיט `3660214`), וכל עוד השורה הזאת אדומה — **השומר של הודעת הקרנינג כבוי
בפועל**, מפני שהיא זו שמודדת אותו.

`kerning` אינו נכלל בתיקון שנשלח למעלה, ובכוונה: קרנינג משנה את **רוחב
הזחילה של הגליפים**, כלומר את המדידה ולא רק את הציור, ו-PR שמצהיר על עצמו
כ„paint-only” אינו המקום להכניס בו שינוי שהפריסה חייבת למדוד מחדש. הוא נאמר
למשתמש בדיאלוג.

דווח למעלה (9.9.2026): https://github.com/superdoc/docx-editor/issues/3995 —
והמדידה ב-Chrome בטבלה למעלה מאמתת את התוצאה: `kerning: 12` נכתב כ-
`<w:kern w:val="24"/>` ואין שינוי במסך. שלושת האתרים בקוד הפתוח מסבירים את
הסיבה: `RunProperties.kern`
מוצהר (`style-engine/src/ooxml/types.ts:334`) ואינו מנורמל; `TextRunStyleAttrs`
אינו מכיר קרנינג כלל; והשניים שקובעים בפועל מתעדים את עצמם —
`measuring/dom/src/index.ts:699` כותב „The current projection does not surface
that opt-in yet”, והצייר (`painters/dom/src/runs/text-run.ts:92`) קובע
`fontKerning = 'none'` תחת „OOXML leaves kerning disabled unless w:kern opts the
run in”.

כלומר **ברירת המחדל נכונה במכוון, ומה שחסר הוא ההצטרפות** — וזו הנקודה שחשוב לא
לאבד: `w:kern` הוא **רף** בחצאי נקודות (הריצה מקורננת מהגודל הזה ומעלה), ולא מתג.
המסמך הסינתטי כולל שורה שהרף בה גבוה מגודל הריצה, וזו בקרת הרגרסיה: היא חייבת
להישאר בלי קרנינג גם אחרי תיקון, ותיקון שקורא את `w:kern` כ„קרנינג דלוק” שובר
אותה.

### היכן זה נבלע — נמדד, לא הוסק

הצייר של המנוע (`layout-engine/painters/dom/src/runs/text-run.ts`) תוקן ידנית
בבאנדל הארוז כדי לצייר את חמשת המאפיינים, ואז נמנו המפתחות שאובייקט הריצה
מוסר לצייר בפועל:

```
outline / shadow / emboss / imprint →  kind|text|fontFamily|fontSize|color|script|pmStart|pmEnd
dstrike                             →  kind|text|fontFamily|fontSize|strike|doubleStrike|color|script|pmStart|pmEnd
```

כלומר שני פערים שונים באותה משפחה:

- **`dstrike` הוא פער של הצייר בלבד.** `doubleStrike` כן מגיע אליו — הוא פשוט
  אינו נקרא. עם שורה אחת (`textDecorationStyle = 'double'`) הקו נצבע כפול.
- **ארבעת האחרים נבלעים שכבה אחת קודם**, ב-`normalizeRunAttrsFromOoxml`
  שב-`style-engine`, שאינו קורא אותם כלל. כשארבע שורות נוספו שם — בבאנדל,
  בצורתו המהודרת — כל הארבעה הופיעו על אובייקט הריצה **ונצבעו**.

הממצא השני הוא גם התשובה לשאלה „האם בכלל אפשר לתקן את זה מבחוץ”: הגשר שבין
ה-`TextRunStyleAttrs` של ה-style-engine ובין חוזה הפריסה הוא **גנרי** — הוא
העביר את `doubleStrike`, שאינו מוצהר כלל בחוזה `TextRun` הפתוח. לכן **כל
התיקון יושב בקוד ה-OSS**, ואין בו שום תלות בחלק הקנייני.

תיקון נשלח: <https://github.com/superdoc/docx-editor/pull/3983> — מיפוי
ב-style-engine, שדות בחוזה, ציור בצייר, וחתימת המיזוג של ריצות סמוכות
(`textRunMergeSignature`) שבלעדיה שתי ריצות שנבדלות רק באפקט היו נדבקות
לאותו `span`.

### ומה נעשה כאן, עד שזה ינחת

**אין מה לעשות בצד שלנו.** `.superdoc-text-run` אינו נושא שום סימן למאפייני
הריצה, ולכן אין וו שאפשר לתלות עליו CSS מקומי — אותה עובדה בדיוק שתועדה למעלה
על `bCs`. השלב המקדים (`docx-preflight.ts`) אינו עוזר כאן: הוא היה יכול לתרגם
`w:outline` ל-`w14:textOutline` — שאותו הצייר **כן** מכיר — אבל הוא רץ בפתיחה
בלבד, ולכן לא היה נוגע בעיצוב שהמשתמש מחיל עכשיו, וזה בדיוק המקרה שדווח.

מה שכן נעשה: **הדיאלוג אומר את זה.** ברגע שנבחר פקד מהרשימה מופיעה מתחת לפס
התצוגה המקדימה שורה שאומרת שהוא ייכתב לקובץ ויוצג ב-Word ושהעורך אינו מצייר
אותו, ובחירת „קו חוצה כפול” אומרת שהקו יצויר בודד. הודעה ולא אזהרה — שום דבר
לא נכשל. ומתחת לפס דווקא, מפני שהכיתוב שלו („כך ייראה ב-Word”) הוא מה שההודעה
משלימה: הפס מתחייב על הקובץ, וההודעה אומרת מה המסך לא יראה.

**והרשימה היא כל מה שנמדד, לא רק „האפקטים”.** גרסה ראשונה מנתה את ארבעת
האפקטים בלבד — ובדיוק לידם, באותה רשת פקדים, ישבו „קרנינג” ו„גודל” של הגופן
המורכב שגם הם אינם מצוירים. הודעה שאומרת חצי אמת על אותו דיאלוג גרועה מהודעה
שאינה קיימת, מפני שהיא מלמדת לסמוך על מה ששותק.

והשער `scripts/qa/font-advanced-qa.mjs` **מודד את ההודעה עצמה** מול הפיקסלים:
אם המנוע יתחיל לצייר צל, השורה תיפול ותאמר להוריד אותו מהרשימה. הודעה שאיש
אינו מודד היא הודעה שתשקר בשקט ברגע שהעולם מתחתיה ישתנה.

### הפער השני שנמדד באותו סבב — „אין טקסט מסומן”

`applyFontAdvanced` דורש טווח מסומן (`format.apply` מקבל SelectionTarget בלבד),
ועד עכשיו התשובה הגיעה רק ברגע ה„אישור”: הדיאלוג נסגר, לקח איתו את שבעה-עשר
השדות שמולאו, והשאיר „יש לסמן טקסט תחילה” בשורת המצב. זו אינה מגבלת מנוע אלא
סדר של ממשק, והיא תוקנה בשלושה חלקים:

1. **`hasRangeSelection` נקראת בפתיחה.** ההודעה בראש הדיאלוג, „אישור” נעול
   מאחוריה, והמונה אומר למה. ארבע תשובות ולא שתיים: „יש טווח”, „אין בחירה”,
   „אין `format.apply` בגרסה הזאת” — שלוש הודעות שונות — ו„אין לדעת”, שאינו
   נועל דבר. **„אין מסמך” נכלל ב„אין לדעת” ולא ב„אין יכולת”:** בין מסמכים
   ובזמן טעינה המסמך הפעיל הוא `null`, והודעה שאומרת שם „אינו זמין בגרסה זו
   של המנוע” היא טענה שקרית על הבניין, שאין למשתמש דרך להפריך.
2. **והיא נשאלת שוב בכל `selectionchange`, וגם כשהמסמך מתחלף.** הדיאלוג הזה
   אינו חוסם את המסמך: אין מאחוריו רקע, הוא נגרר בכוונה, ואפשר ללחוץ בטקסט
   בזמן שהוא פתוח. תשובה שנקראה פעם אחת הייתה מתיישנת בשני הכיוונים — בחירה
   שהתכווצה מתחת לכפתור שנשאר פתוח, או כפתור שנשאר נעול לנצח אחרי שהמשתמש
   סימן טקסט כמו שההודעה ביקשה. שני הכיוונים מחזירים בדיוק את הבאג שדווח.
   `selectionchange` אינו נורה על מעבר בין לשוניות מסמך, ולכן יש גם `watch`
   על המסמך הפעיל. ופס התצוגה המקדימה נקרא מחדש יחד עם התשובה — אחרת הכפתור
   היה נפתח והפס היה ממשיך להראות את הטקסט הקודם, מתחת לכיתוב שמתחייב „כך
   ייראה ב-Word”.
3. **והדיאלוג נסגר על הצלחה בלבד.** קודם הוא נסגר לפני שההחלה יצאה לדרך,
   ולכן כשלון לקח איתו את מה שהוקלד. עכשיו הוא נשאר פתוח עם הערכים, המשתמש
   מסמן טקסט, „אישור” נפתח מעצמו, והוא לוחץ שוב.

## פעולות שמדווחות הצלחה וכותבות מסמך שבור

**כל הסעיפים בפרק הזה נמדדו מחדש על superdoc 2.15.0-next.15 (15.9.2026),
Chrome אמיתי על ה-`dist` הארוז ועם פירוק ה-zip של הייצוא, כרגיל.** שניים
מהם **השתנו**, וכתוב בגופם מה נמדד היום: `crossRefs.insert` (מיד למטה)
ו-`captions.insert` עם עוגן שאינו פסקה — ולצידם החצי של „אין דרך להביא
תמונה” בסעיף `images.insertCaption`. כל השאר משחזרים תו-בתו, כולל הודעות
השגיאה.

### `crossRefs.insert` — הפניה מקושרת

**השתנה — נמדד מחדש על 2.15.0-next.15 (15.9.2026), כל 54 הצירופים
(9 תצוגות × 6 סוגי יעד), ואותה מטריצה בדיוק גם על 2.14.0-next.5.** כלומר
התיאור שהיה כאן, שנכתב על גרסת 2.8, אינו „פער שנסגר ב-2.15” אלא **תיאור
מיושן**: הטענה שכל צירוף כותב `SDXREF` ושה-`resolvedText` נשאר ריק נכונה
היום לחלק בלבד. שלושים וחמישה מן הצירופים **מסורבים ביושר**
(`CAPABILITY_UNAVAILABLE`, „does not yet support target kind … with
display …”), ותשעה־עשר מתקבלים:

| היעד והתצוגה | מה שנכתב היום |
|---|---|
| סימנייה + טקסט / מספר בלבד / מספר בהקשר מלא / „לעיל-לקמן” | `REF "bm1" \h` קנוני (ולצידו `\n`, `\w`, `\p`), ו-`resolvedText` מתמלא אחרי `rebuild` — טקסט הסימנייה עצמו |
| פסקה מסוגננת | `STYLEREF` קנוני |
| סימנייה + מספר עמוד | `SDXREF` — הצורה השבורה; Word רוצה `PAGEREF` |
| כותרת ופריט ממוספר, בכל התצוגות | `SDXREF` — תשעה שדות כאלה ב-docx המיוצא |
| הערה, ו„לעיל-לקמן” על הערה | `NOTEREF "1"` — מזהה ההערה במקום שם סימנייה, ואינו נפתר גם במנוע |
| סימנייה שאינה קיימת | `success: true`, בלי בדיקה |

כלומר יש היום מסלול אחד שכותב בדיוק את מה ש-Word פותר — **יעד סימנייה**,
בשלוש תצוגות הטקסט/המספר ובתצוגת „לעיל-לקמן”.

והצורה השבורה נראית כך, למי שיפגוש אותה בקובץ — האסימון שאחרי `REF` הוא
`SDXREF` ולא שם הסימנייה, ולכן Word יציג „שגיאה! מקור ההפניה לא נמצא”:

    REF SDXREF kind=bookmark;value=%7B%22kind%22%3A%22bookmark%22...%7D;display=pageNumber

**ובכל זאת לא נשלח פקד, וזו הכרעה ולא מגבלה.** פקד „הפניה מקושרת” מצומצם
היה אפשרי היום: יעד סימנייה בלבד, התצוגות שנמדדו קנוניות, ובדיקה משלנו
שהסימנייה קיימת (המנוע אינו בודק, כאמור בשורה האחרונה בטבלה). **הוכרע לא
לבנות אותו עכשיו** — זו תוספת של פקד חדש, לא סגירת פער, והיא אינה בגל הזה.
מה שנשלח נשאר כשהיה: `crossRefs.list` תופס שדות `REF` שנוצרו ב-Word,
ו-`rebuild` עליהם מחשב באמת.

### `authorities.entries.insert` — סימון ציטוט לטבלת מקורות
ה-`instruction` שנכתב:

    TA "בראשית א, א" \s "בר׳ א א" \c 1

ל-`TA` של Word **אין ארגומנט כללי**; התחביר הוא `{ TA [switches] }`,
והציטוט הארוך מגיע רק מ-`\l`. כלומר Word יקרא שדה בלי ציטוט ארוך, והערך
יופיע ריק בטבלה. אומת בשלוש שכבות: `entries.get`, ה-docx המיוצא עצמו
(`<w:fldSimple w:instr="TA &quot;בראשית א, א&quot; ...">`), והבנאי במנוע —
תבנית קשיחה שאין בה מסלול שפולט `\l`.

בנוסף אין שום בריחה של גרשיים: `longCitation` שמכיל `"` נכתב כמות שהוא
ומייצר גרשיים מקוננים, עם `success: true`.

### `index.entries.insert` — השדה `subEntry`
`{text:'אבות', subEntry:'יצחק'}` כותב `XE "אבות" \s "יצחק"`, ו-`\s` אינו
מתג של `XE` ב-Word (המתגים: `\b \f \i \r \t \y`).

**נעקף:** הצורה הקנונית `XE "אבות:יצחק"` עובדת, והמנוע מפרק אותה נכון
בחזרה. המודול שולח תמיד אותה ולעולם לא את `subEntry`.

### `bibliography.configure` — סגנון הביבליוגרפיה
הקריאה עובדת בצד אחד ושבורה בצד שני, ושניהם נמדדו באותו קובץ מיוצא.
הסגנון **כן** מגיע למקום הנכון: `configure({style:'Chicago'})` כתב
`<b:Sources SelectedStyle="/CHICAGO.XSL" StyleName="Chicago" Version="16">`,
ואחד עשר השמות הקנוניים ממופים נכון. אבל אותה קריאה כותבת גם ל-instruction:

    BIBLIOGRAPHY \sdStyle "Chicago"

`\sdStyle` אינו מתג של Word — המתגים המתועדים לשדה `BIBLIOGRAPHY` הם `\l`
ו-`\f` — ואין דרך לבקש את הראשון בלי השני.

גם המסלול השני כותב אותו: `bibliography.insert({style:'Chicago'})` מייצר
את אותו `BIBLIOGRAPHY \sdStyle "Chicago"` (נמדד). כלומר אין קריאה שמכניסה
ביבליוגרפיה עם סגנון ובלי המתג הלא-מתועד.

**לא נשלח פקד סגנון.** בלעדיו כל קוד שדה שנכתב הוא קנוני, והסגנון נשאר
ברירת המחדל שגם Word מתחיל בה (APA).

### `citations.insert` עם יותר ממקור אחד
שני מקורות כותבים `CITATION src-a;src-b` (נמדד גם ב-docx). תחביר ריבוי
המקורות של Word הוא המתג `\m`: `{ CITATION Tag1 \m Tag2 }`. אסימון אחד
שמחבר שני תגים בנקודה ופסיק אינו tag קיים.

**נעקף:** המודול שולח תמיד מקור אחד, וגם הממשק מאפשר רק אחד.

### `citations.sources.remove` על מקור מצוטט
מחזיר `success: true`, מוחק את המקור, ומשאיר את שדה ה-`CITATION` מצביע
לתג שכבר אינו קיים — כלומר בדיוק המסמך השבור של `crossRefs`, רק שכאן
אנחנו אלה שיוצרים אותו.

**נעקף:** `removeCitationSource` סופר את הציטוטים דרך `citations.list`
ומסרב, ומדווח כמה מהם מחזיקים במקור.

### `captions.update` — עריכת טקסט של כיתוב
הפעולה **אינה מחליפה את הטקסט אלא מוסיפה עליו**. שלושה צעדים רצופים על
אותו כיתוב:

    insert 'אלף'   → get 'אלף'
    update 'בית'   → get 'אלף: בית'
    update 'גימל'  → get 'אלף: בית: גימל'

אומת ב-docx ולא רק בקבלה: הריצה שאחרי השדה מכילה
`<w:t xml:space="preserve">: שרטוט המשכן: </w:t>` — הישן, המפריד והחדש.
`patch: { text: '' }` אינו מוחק אלא מוסיף מפריד ריק, ו-`patch` בלי `text`
(או עם שדה שאינו בחוזה) מוחזר `NO_OP`.

**נעקף:** עריכה היא `remove` ואז `insert` באותו מקום, והעוגן נקרא **לפני**
ההסרה. נמדד שהתוצאה זהה לכיתוב שנוצר מאפס, שהמיקום נשמר ושהמספור מתעדכן.

בחירת העוגן נקראת מ-`blocks.list`, וסוגו נשלח כפי שהוא באמת —
`nodeType: 'paragraph'` או `nodeType: 'table'` — ולא תמיד כ-`'paragraph'`
כפי שנשלח עד לאחרונה (ראו הסעיף הבא, שבו ההנחה ההיא נמדדה כשגויה). שני
צעדים, כדי לתת לבלוק השני סיכוי כשהראשון אינו קביל:

1. הבלוק **שלפני** הכיתוב, עם `position: 'below'` ועם ה-`nodeType` האמיתי
   שלו.
2. אם סוגו אינו נקלט — לא פסקה ולא טבלה — הבלוק **שאחריו**, עם
   `position: 'above'` ועם ה-`nodeType` האמיתי שלו.

שני העוגנים מצביעים על אותו רווח בדיוק, ולכן הנפילה-לאחור אינה מזיזה את
הכיתוב. כיתוב מתחת ללוח הוא הצורה השכיחה ביותר, וכל docx מיובא מלא בהם.

**והעקיפה כבר צומצמה בעקבות זה, באותו קומיט:** ירדה ההנחה שהעוגן הראשון
הוא תמיד פסקה, ואיתה ירדה הנפילה-לאחור שההנחה הזאת חייבה כשהבלוק שלפני
היה טבלה — טבלה נשלחת עכשיו במסלול הישיר, עם הסוג האמיתי שלה. ירד גם
הסירוב שהיה כאן על „שני השכנים טבלאות”: `טבלה │ כיתוב │ טבלה` נערך היום
כמו כל זוג שכנים אחר (ראו הסעיף הבא לפרטי המדידה). מה שנשאר נכון בלי קשר
לכך הוא שהעוגן נקרא **לפני** ההסרה, ורשת השחזור שאחריה.

הרשימה שהעוגן מוגבל אליה נשארת סגורה — פסקה וטבלה, ותו לא — ולכן הסירוב
נשאר לשני מצבים, ובשניהם **לפני** שנגעו במסמך: כיתוב שהוא הבלוק היחיד,
וכיתוב ששני שכניו אינם פסקה ואינם טבלה, כגון תוכן עניינים (ראו הסעיף
הבא). ומעליהם רשת ביטחון: הוספה שנכשלה אחרי הסרה שהצליחה מנסה להחזיר את
התוכן הישן, ורק כשגם השחזור נכשל ההודעה מודה שהכיתוב הוסר ומפנה ל-Ctrl+Z.

### `captions.insert` — עוגן שאינו פסקה

**נמדד על 2.15.0-next.15 (15.9.2026), ואותה תוצאה על 2.14.0-next.5:**
‏`adjacentTo` מקבל בלוק שאינו פסקה — ובלבד שהכתובת נושאת את הסוג
**האמיתי** שלו. הכתובת `{kind:'block', nodeType:'table', nodeId:'tbl:…'}`
מתקבלת גם עם `position: 'below'` וגם עם `'above'`, וסדר הבלוקים שיוצא
הוא בדיוק
`פסקה │ כיתוב „טבלה 1: מעל ללוח” │ טבלה │ כיתוב „טבלה 2: מתחת ללוח” │ פסקה`.
מה שנדחה תמיד הוא בדוי: `nodeType: 'paragraph'` עם מזהה של טבלה מוחזר
`TARGET_NOT_FOUND`, פסקה שנשלחת עם `nodeType: 'table'` מוחזרת גם היא
`TARGET_NOT_FOUND`, וכתובת בלי `nodeType` כלל מוחזרת `INVALID_TARGET` —
כלומר המנוע בודק את הסוג ולא רק את קיום ה-`nodeId`.

**מה שאנחנו שולחים היום:** רשימת סוגים סגורה — `paragraph` ו-`table`,
ותו לא — נקראת תמיד מ-`blocks.list` (ראו הסעיף הקודם). העקיפה הישנה
שנרשמה כאן — עוגן שנשלח תמיד כ-`'paragraph'` בלי קשר לסוגו האמיתי,
ונפילה-לאחור שהניחה שהבלוק השני הוא פסקה — ירדה: הצורך בה נעלם ברגע
שאפשר לשלוח את הסוג האמיתי. מה שנשאר מהעקיפה הוא הסירוב **לפני** ההסרה
על סוג שאינו ברשימה, וזה נחוץ בלי קשר להרחבה.

**מה שעדיין נעקף — בלוק `tableOfContents`:** נמדד: כתובת עם הסוג האמיתי
שלו מוחזרת `INVALID_TARGET` בנוסח המנוע עצמו — „paragraph-like blocks or
top-level tables”. שליחת אותו בלוק כ-`'paragraph'` — השקר שהיה מציל טבלה
לפני שהיא הפכה לעוגן קביל — כאן **אינה** מצילה: היא מוחזרת `success: true`,
אבל הכיתוב נכתב **בתוך** ה-sdt של תוכן העניינים, לא לידו — מסמך שגוי
בשקט, לא כשל גלוי. זו הסיבה שהרחבת רשימת הסוגים נעצרה בטבלה: אין דרך
זולה לתמוך גם בבלוק הזה בלי לבדוק כל סוג sdt בנפרד, וכרגע הסירוב שבקוד
הוא מה שמונע את המסמך השגוי במקום „success” שקרי.

**ונמדד גם דרך הדיאלוג, לא רק ב-API:** מקטע ב-`scripts/qa/references-qa.mjs`
מכניס לוח מהרצועה, מוסיף לו כיתוב מהדיאלוג, מכניס לוח שני ועורך את הכיתוב
שביניהם — אדום על הבנייה שקדמה לצמצום, ירוק אחריה, כולל סדר הבלוקים
ב-docx וכיוון ימין-לשמאל. שער נוסף באותו מקטע יוצר בלוק `tableOfContents`,
קורא את ה-`nodeType` האמיתי שלו ושולח אותו כעוגן — ומצפה לסירוב; אם המנוע
יתחיל לקבל אותו יום אחד, השער הזה ייפול ויסמן שאפשר להרחיב את הרשימה.
בדיקות היחידה והרכיב מאמתות ש-`nodeType` הנשלח הוא האמיתי, ושתי מוטציות
מכוונות — כל אחת בנפרד — מפילות ארבע בדיקות. „נקי” בסעיף הכיתובים שלמטה
מתייחס לקוד השדה שנכתב, לא לקבילות העוגן.

### `images.insertCaption` — כיתוב לתמונה
התווית קשיחה ובאנגלית. אין ב-`InsertCaptionInput` שדה `label` בכלל
(`{ imageId, text }`), והמימוש כותב `SEQ Figure` וטקסט `Figure <n> <text>`
— בלי נקודתיים ובלי `\* ARABIC`. זו בדיוק הבעיה שהפילה את טבלת המקורות,
רק שכאן אין אפילו פרמטר לנסות דרכו.

**והחצי השני של הסעיף השתנה — נמדד על 2.15.0-next.15 (15.9.2026), ואותה
תוצאה על 2.14.0-next.5:** ‏`create.image` עם PNG כ-`data:` **עובדת** —
`success: true`, ‏`word/media/image1.png` נכתב, `w:drawing` אחד נכנס לגוף,
ו-`images.list` מחזיר את התמונה — בעוד `capabilities.get()` ממשיך לדווח
עליה `available: false` / `OPERATION_UNAVAILABLE`. כלומר „אין דרך להביא
תמונה למסמך” אינו נכון עוד; מה שכן נכון הוא ש-`doc.insert` של HTML עם
`<img src="data:…">` עדיין נדחה (`INVALID_PAYLOAD`, „HTML produced no safe
canonical content”), ושהקטלוג משקר כאן — ראו „פעולות שהמנוע מסמן
כלא-זמינות” למטה.

**לא נשלח.** `captions.insert` עושה את אותו דבר טוב יותר — הוא מקבל תווית
עברית, והעוגן שלו הוא הפסקה, כולל הפסקה שהתמונה יושבת בה.

### `captions.configure` — מספור הכיתובים
אינרטית לגמרי. `configure({label:'איור', format:'upperRoman'})` חזר
`success: true`, והכיתוב הבא נכתב `SEQ איור \* ARABIC` — ה-`format` אינו
מגיע לשום מקום, גם לא כשהוא `'zigzag'`. `includeChapter` הוא היחיד שאומר
את האמת: `CAPABILITY_UNAVAILABLE / caption-include-chapter-unsupported`.

**לא נשלח פקד מספור.** המספור נשאר ערבי, מה שגם Word מתחיל בו.

### `footnotes` — כתובת אחת לשתי הערות שונות
`FootnoteAddress` הוא `{ kind:'entity', entityType:'footnote', noteId }`,
ו-**`entityType` הוא `'footnote'` גם עבור הערת סיום**. שני הרצפים מתחילים
מ-1 בנפרד, ולכן במסמך שיש בו הערת שוליים 1 והערת סיום 1 שתיהן נושאות את
אותה כתובת בדיוק, ואת אותו `handle.ref` (`footnote:1`) ואת אותו `id`
ב-`list`. `type` הוא ההבדל היחיד, והוא **אינו** בכתובת.

מה שנמדד: `get`/`update`/`remove` על הכתובת הזאת פוגעים תמיד ב**הערת
השוליים**; אחרי שהיא הוסרה, אותה כתובת פוגעת בהערת הסיום;
ו-`entityType: 'endnote'` נזרק („target must be a FootnoteAddress …
entityType 'footnote'”). כלומר „הסר” על הערת סיום מוחק הערת שוליים אחרת,
עם `success: true`.

**נעקף:** לפני כל עריכה והסרה נקרא `footnotes.get` על הכתובת, והפעולה
מסרבת כשהסוג שחזר אינו הסוג שהמשתמש בחר — לפני שנגעו במסמך. `get` ולא
השוואה מול `list`, כי הוא מודד את אותו מסלול שהמוטציה תלך בו.

**וההגנה עצמה היא TOCTOU, ודורשת נעילה בצד הממשק.** בין ה-`get` ובין
המוטציה יש חלון: `get` נפתר מעבר לגבול macrotask (נמדד ~10ms במסמך ריק
וקר, וגדל עם גודל המסמך), ובזמן שהוא באוויר לחיצה על „הערת שוליים”
ברצועה נקלטת ומוסיפה הערה — כלומר משנה את מה שהכתובת נפתרת אליו. מה
שנמדד: „הסר” על הערת סיום 1, שאושר מפני שלא הייתה אז הערת שוליים 1, מחק
את הערת השוליים **החדשה**, הערת הסיום נשארה, והמשתמש קיבל „בוצע”
(שוחזר במנוע האמיתי, פעם אחת בשש חזרות).

`get` נוסף אינו פותר — הוא רק מקצר את החלון. מה שסוגר אותו הוא שההוספה
לא תיקלט כל עוד פעולה על הערה באוויר: `noteBusy` ב-`ReferencesTab.vue`
שנדלק כשהפעולה יוצאת ונכבה ב-`finally`, ומנטרל בזמן הזה את שני כפתורי
ההוספה שברצועה ואת כפתורי הדיאלוג. **זו מלכודת שתחזור בכל פעולה עתידית
על הערות**: כל אימות שנעשה מול המנוע לפני מוטציה על כתובת שאינה מזהה
את מושאה באופן חד-משמעי הוא בדיקה של מצב שעלול להתחלף עד המוטציה, וכל
מסלול שיכול לשנות את המצב הזה חייב להיות נעול בזמן שהיא באוויר.

### `footnotes.configure` — כותב קנונית, ובכל זאת לא נשלח
זה ה-`configure` הראשון מאז גל 3 שבאמת מגיע לקובץ. `settings.xml` קיבל
`<w:footnotePr><w:numFmt w:val="lowerLetter"/><w:numStart w:val="4"/>
<w:numRestart w:val="eachPage"/><w:pos w:val="beneathText"/></w:footnotePr>`,
ובמסמך נקי אין `w:footnotePr` כלל עד לקריאה הראשונה. ובכל זאת אין לו פקד,
משלוש סיבות שנמדדו:

1. **אין קריאה.** אין בכל ה-API דרך לקרוא את ההגדרות שבמסמך — לא
   ב-`footnotes`, לא ב-`sections` ולא ב-`info`. דיאלוג היה מציג ערכים
   שאינם של המסמך שעל המסך, וזה בדיוק מה שנאסר בדיאלוג של תוכן העניינים.
2. **כל קריאה מחליפה את האלמנט כולו, ואינה מטליאה אותו.** `configure`
   מלא ואחריו `configure({ numbering: { start: 9 } })` משאיר
   `<w:footnotePr><w:numStart w:val="9"/></w:footnotePr>` בלבד, ו-
   `numbering: {}` משאיר `<w:footnotePr></w:footnotePr>` ריק. כלומר אישור
   אחד בטופס שאינו יודע מה היה במסמך מוחק את מה שהוגדר ב-Word.
3. **שלושה ערכים שכן בחוזה נכתבים כאסימונים שאינם של Word:**
   `restartPolicy:'eachSection'` → `eachSection` (התקן: `eachSect`),
   `format:'symbol'` → `symbol` (התקן: `chicago`), ומיקום הערת סיום →
   `sectionEnd`/`documentEnd` (התקן: `sectEnd`/`docEnd`). מיקום הערת
   שוליים (`pageBottom`/`beneathText`) וחמשת פורמטי המספור הלטיניים כן
   תקניים.

**מספור עברי אפשרי, ואינו נשלח.** `numFmt` נכתב גולמית, ולכן
`format: 'hebrew1'` מייצר `<w:numFmt w:val="hebrew1"/>` — אסימון תקני של
Word ובדיוק המספור שספר תורני רוצה. `'hebrew1'` אינו ב-union של
`FootnoteNumberingConfig`, כלומר זו הישענות על ערך שאינו בטיפוסים
הציבוריים, והבריף אוסר אותה. הממצא מדווח למפקח.

`scope: { kind: 'section' }` הוא היחיד שאומר את האמת:
`CAPABILITY_UNAVAILABLE` („section-scoped note configuration is not
supported by v2 yet”).

## `doc.sections` — ה-namespace היחיד שכן מאמת קלט

נמדד בגל 10 (פריסת עמוד מתקדמת), Chrome headless על ה-dist הארוז, כולל פירוק
ה-zip של `export.toDocx`. זו ההפך מכל תשעת הגלים שקדמו: ערך שאינו ב-union
**נזרק** בזמן ריצה ואינו נבלע.

**נמדד מחדש על 2.15.0-next.15 (15.9.2026) — כל הפרק הזה עומד, חוץ משורה אחת
שהתבררה כמדויקת פחות ומתוקנת למטה („כל קריאה מחליפה את האלמנט כולו”).** כל
הזריקות חוזרות מילה במילה (הודעת הפורמט מונה היום גם `hebrew1` ו-`hebrew2`),
כתובת מומצאת עדיין `TARGET_NOT_FOUND`, מה שנכתב ל-docx קנוני ובסדר האלמנטים
של `CT_SectPr`, וההפיכות כשהייתה.

    setLineNumbering.restart: 'zigzag'  → „must be one of: continuous, newPage, newSection.”
    setLineNumbering.countBy: 0/-3/2.5  → „must be a positive integer.”
    setLineNumbering.enabled חסר        → „must be a boolean.”
    setVerticalAlign.value: 'zigzag'    → „must be one of: top, center, bottom, both.”
    setBreakType.breakType:'nextColumn' → „must be one of: continuous, nextPage, evenPage, oddPage.”
    setPageNumbering.format: 'hebrew1'  → „must be one of: decimal, lowerLetter, upperLetter,
                                            lowerRoman, upperRoman, numberInDash.”
                                            ← נכון ל-2.8.0 בלבד. ב-2.10.0 ה-union
                                              כולל hebrew1/hebrew2, והקריאה כותבת
                                              <w:pgNumType w:fmt="hebrew1"/>.
                                              ר' docs/superdoc-2.10-review.md
    setPageBorders.borders.display      → „must be one of: allPages, firstPage, notFirstPage.”
    sections.list limit: -3             → „limit must be a positive integer.”
    target מומצא                        → קבלה `TARGET_NOT_FOUND` („Section 'section-99' was not found.”)

וכל אסימון שכן ב-union **הוא אסימון Word תקני**: שלושת ערכי
`ST_LineNumberRestart`, ארבעת ערכי `ST_VerticalJc`, ארבעת ערכי `ST_SectionMark`
וששת ערכי `ST_NumberFormat` — אין כאן `eachSection` מול `eachSect`. זו
הקבוצה הראשונה שאין בה אף אסימון פנימי.

### מה שנכתב, ונמדד ב-docx

    <w:type w:val="oddPage"/>
    <w:pgBorders w:display="allPages" w:offsetFrom="text" w:zOrder="front">
      <w:top w:val="double" w:sz="12" w:space="24" w:color="FF0000" w:shadow="0" w:frame="0"/>…
    <w:lnNumType w:countBy="5" w:start="1" w:distance="360" w:restart="newPage"/>
    <w:pgNumType w:start="3" w:fmt="upperRoman"/>
    <w:vAlign w:val="center"/>
    <w:pgMar … w:header="1008" w:footer="864"/>

הכול קנוני ובסדר האלמנטים של `CT_SectPr`, ובאותה יחידה כמו השוליים:
**אינצ'ים** ב-API, twips ב-XML. גרשיים בתוך `style` מוברחים ל-`&quot;` —
אין הזרקת XML.

### מספור עמודים עברי — היה בלתי אפשרי, ונסגר ב-2.10.0

הפער נמדד על 2.8.0: ה-union **נאכף בזמן ריצה**, ולא הייתה דרך ציבורית לכתוב
`<w:pgNumType w:fmt="hebrew1"/>` — `SectionPageNumberingFormat` היה שש
אפשרויות לטיניות בלבד. זה ההבדל שהיה אז מול `footnotes.configure`, שבו
`numFmt` נכתב גולמית.

**ב-superdoc@2.10.0 ה-union כולל `hebrew1` ו-`hebrew2`**, ואותה קריאה בדיוק
נמדדה כמצליחה: `<w:pgNumType w:start="1" w:fmt="hebrew1"/>` ב-docx המיוצא.
שני הפורמטים מוצעים בדיאלוג, נשלחים, וחוזרים מ-`sections.list` — ר'
`PAGE_NUMBER_FORMATS` ב-`src/engine/page-setup.ts` ואת שתי הבדיקות
(`tests/unit/page-setup.test.ts`, `tests/component/layout-group.test.ts`).

### ההפיכות נמדדה

`setLineNumbering({enabled:false})` מוריד את `<w:lnNumType>` כולו, ו-
`clearPageBorders` מוריד את `<w:pgBorders>` (קריאה שנייה → `NO_OP`). לעומתם
**`<w:pgNumType>` אינו ניתן להסרה**: `setPageNumbering` דורש לפחות שדה אחד
ואין לו `clear`, כלומר „המשך מהמקטע הקודם” של Word אינו ניתן להשגה מהתוסף.
זה כתוב בדיאלוג לפני האישור.

### כל קריאה מחליפה את האלמנט כולו — חוץ מ-`pgNumType`

`setLineNumbering({enabled:true,countBy:1,distance:0})` אחרי קריאה מלאה
השאיר `<w:lnNumType w:countBy="1" w:distance="0"/>` — `start` ו-`restart`
ירדו. זו בדיוק הסיבה ש-`footnotes.configure` לא נשלח בגל 9, ומה שמבדיל כאן
הוא ש**יש קריאה**: `sections.list` מחזיר `lineNumbering`, `pageNumbering`,
`headerFooterMargins`, `verticalAlign` ו-`pageBorders` מלאים, ולכן הפקד משמר
את `countBy`/`start`/`distance` שנקבעו ב-Word במקום למחוק אותם. ההשלמה
נקראת מאותו `sections.list` שהכתובת נלקחה ממנו — ולכן אין חלון TOCTOU בין
הקריאה למוטציה.

**ו„כל קריאה” אינו מדויק: `setPageNumbering` מטליא ואינו מחליף.** נמדד על
2.15.0-next.15 (15.9.2026), ואותה תוצאה על 2.14.0-next.5:
`{format:'decimal'}` אחרי `{start:3, format:'upperRoman'}` השאיר
`start="3"` והחליף רק את הפורמט; `{start:7}` אחריו שמר את `decimal`; וקריאה
עם `format` בלבד שמרה את `start="7"`. `lnNumType` ו-`pgBorders` **כן**
מוחלפים, כמתואר למעלה — כלומר שתי התנהגויות שונות באותו namespace, ואין
להסיק מאחת על השנייה. הקוד אצלנו כבר תואם למיזוג הזה: `applyPageNumbering`
ב-`page-setup.ts` מתייחס ל-`start: null` כ„אל תיגע”, ולא משלים ערך מדומה.

### `setBreakType` — עובד, ולא נשלח

כותב `<w:type w:val="oddPage"/>` קנונית ומשנה מקטע קיים (אין צורך ב-
`create.sectionBreak`). מה שאין לו הוא פקד שאפשר להציג: פעולות המקטע חלות
על **כל** המקטעים, ובמסמך בעל מקטע יחיד ה-`w:type` היחיד מתאר איך המסמך
מתחיל — כלומר אינו עושה דבר; ובמסמך מרובה מקטעים הוא הופך פקד של מקטע אחד
לסריקה שמשכתבת את כל מעברי המקטע שנקבעו ב-Word.

### `chapterStyle` ו-`chapterSeparator` נבלעים לגמרי

`setPageNumbering({chapterStyle:1,chapterSeparator:'colon'})` החזיר
`success: true` וכתב `<w:pgNumType/>` **ריק**. שני השדות אינם מגיעים לשום
מקום, ו-`sections.get` אינו מדווח אותם. אין להם פקד.

## פעולות שבולעות קלט בשקט

בכל אלה המנוע מחזיר `success: true` על ערך שאינו בחוזה, אינו חוקי, או
אינו נכתב כלל. **כל ולידציה חייבת לשבת אצלנו, לפני הקריאה.**

**כל שורות הטבלה נמדדו מחדש על 2.15.0-next.15 (15.9.2026) ובלעו כמקודם**,
עם שינוי אחד קטן לטובה, שמסומן בשורה עצמה.

| פעולה | מה נבלע |
|---|---|
| `toc.configure` | `tabLeader` (גם `'zigzag'`), `rightAlignPageNumbers`, `includePageNumbers` |
| `index.configure` | `columns` של 0 / 1- / 2.5, שדה שאינו בחוזה, `letterRange:{from:'zigzag'}` → `\p "zigzag-9"` |
| `authorities.configure` | `tabLeader:'zigzag'` → `\l "zigzag"`, שדה שאינו בחוזה |
| `authorities.entries.insert` | `category` של `99`, `0`, `2.5`, `'zigzag'` ואפילו `'פסוקים'` — כולם נכתבים גולמית ל-`\c` |
| `index.insert` | `\c 99` — מעל התקרה של Word (4) |
| `toc.markEntry` | `\l 12` — מעל התקרה של Word (9) |
| `fields.insert` | `DATE \* HEBREW` — מתג לוח השנה נבלע לגמרי |
| `citations.sources.insert` | `fields: {}`, `title: ''`, `title: '   '`, `type: 'zigzag'`, ושדה שאינו בחוזה — כולם `success: true` ונכתבים לקובץ. **שינוי (15.9.2026):** ‏`type` שאינו בחוזה נכתב היום מנורמל — `<b:SourceType>Misc</b:SourceType>` — ולא גולמית; שאר השורה ללא שינוי |
| `citations.bibliography.configure` | `style: 'zigzag'` → `SelectedStyle="/zigzag.XSL"`, גיליון סגנון שאינו קיים |
| `captions.insert` | `label: '   '` → `SEQ "   " \* ARABIC`; ירידת שורה בתווית נכתבת **גולמית לתוך קוד השדה**; `text: '   '` נכתב `: ` ואז רווחים בעוד ש-`get` מחזיר `''`; ירידת שורה בטקסט נכתבת גולמית לתוך `<w:t>`; `text: 5` חוזר `success: true` והטקסט נעלם בלי זכר |
| `captions.configure` | `format: 'upperRoman'`, `format: 'zigzag'`, ושדה שאינו בחוזה — הכול `success: true` ואינו נכתב |
| `captions.list` | `limit: -3` מחזיר `total` נכון ורשימה ריקה |
| `footnotes.update` | `patch: {}` ו-`patch` עם שדה שאינו בחוזה — `success: true` בלי `NO_OP` ובלי שינוי |
| `footnotes.configure` | `format:'zigzag'` → `<w:numFmt w:val="zigzag"/>`; `start` של `0`, `-5`, `2.5` ו-`'א'` — כולם נכתבים גולמית ל-`w:numStart`; `position:'zigzag'` → `<w:pos w:val="zigzag"/>`; `restartPolicy:'eachZigzag'` נכתב אף הוא |
| `footnotes.list` | `limit: -3` מחזיר `total` נכון ורשימה ריקה |
| `sections.setPageBorders` | `style` הוא `string` חופשי: `'zigzag'` → `<w:top w:val="zigzag"/>`, ו-`style: ''` מייצר `<w:top w:sz="8"/>` — גבול **בלי `w:val`**, שהיא תכונה נדרשת ב-`CT_Border`. `size: 999` → `w:sz="999"` (התקרה 96), `size: 2.5` → `w:sz="2.5"` בתכונה שהיא מספר שלם, `space: 999` → `w:space="999"` (התקרה 31), `color: '#FF0000'` ו-`color: 'zigzag'` נכתבים כמות שהם ואינם `ST_HexColor` |
| `sections.setHeaderFooterMargins` | `header: 99` → `w:header="142560"`, כלומר כותרת במרחק 2.5 מטר מקצה הדף. אין תקרה |
| `sections.setLineNumbering` | `distance: 999` → `w:distance="1438560"`, ו-`countBy`/`start` של מיליארד נכתבים כמות שהם. `enabled: true` בלי שדה נוסף מייצר `<w:lnNumType/>` ריק |
| `sections.setPageNumbering` | `chapterStyle` ו-`chapterSeparator` — `success: true`, ואינם נכתבים כלל; `{chapterStyle:1}` לבדו מייצר `<w:pgNumType/>` ריק |

## מתג שכן עובד

`fields.insert` עם `DATE \@ "dd/MM/yyyy"` — מתג תמונת-הפורמט **מפורש**
כהלכה, ומתקן גם היסט של יום שיש ב-`DATE`  (הוא ISO ב-UTC).

**נמדד מחדש על 2.15.0-next.15 (15.9.2026):** אותה קריאה בדיוק, ואחרי
`fields.rebuild` הערך שנפתר הוא `15/09/2026` — התאריך האמיתי של יום המדידה,
בתמונת הפורמט שהתבקשה. בקרה באותה ריצה: מתג לוח השנה העברי נשאר נבלע,
ו-`resolvedText` שלו ריק.

## מה שכן נכתב נכון — ציטוטים

זו הקבוצה הראשונה מאז הסימניות שעברה גם את שכבת ה-docx בלי הסתייגות, ולכן
היא רשומה כאן במפורש: לא כל מה שהמנוע כותב שבור.

**שש השורות נמדדו מחדש על 2.15.0-next.15 (15.9.2026) ועומדות במלואן.** מה
שנוסף בייצוא של היום, ואינו פוגם: לצד `customXml/item1.xml` נכתב גם
`customXml/superdoc-generated-apparatus.xml`.

- המקורות יושבים ב-`customXml/item1.xml` כ-`<b:Sources>` בסכימת OOXML,
  עם `itemProps1.xml` שמצהיר על ה-`schemaRef`, רלציה מ-`document.xml.rels`
  ו-`Override` ב-`[Content_Types].xml`. זה בדיוק המקום של „נהל מקורות”
  ב-Word.
- `<w:fldSimple w:instr="CITATION src-…">` וה-`<b:Tag>` שלצידו **זהים**.
  כלומר Word יפתור את הציטוט — ההפך מ-`REF SDXREF` ומ-`TA` בלי `\l`.
- העברית עוברת שלמה בכל השדות: `שו״ת הרמב״ם`, `בן מימון`, `תתקצ״ה`,
  `מוסד הרב קוק`, `ירושלים`.
- הביבליוגרפיה נבנית **מלאה כבר ביצירה**, ו-`rebuild` באמת אוסף מקור
  שנוסף אחריה (`sourceCount` 2 → 3) וגם עריכה של מקור קיים.
- `citations.insert` על `sourceId` שאינו קיים מוחזר `TARGET_NOT_FOUND`,
  ו-`bibliography.rebuild` על מזהה של פסקה רגילה גם הוא. הכתובות מאומתות
  ואינן נבלעות.
- `bibliography.remove` מפיל את הבלוק כולו ואינו משאיר פסקה — ההפך מתוכן
  העניינים.

## מה שכן נכתב נכון — כיתובים

הקבוצה השנייה (אחרי הציטוטים) שעברה את שכבת ה-docx בלי הסתייגות על קוד
השדה. **נמדדה מחדש על 2.15.0-next.15 (15.9.2026): כל הסעיפים עומדים, בלי
רגרסיה.**

- הפסקה היא `<w:pStyle w:val="Caption"/>` — סגנון הכיתוב האמיתי של Word —
  ובתוכה `<w:fldSimple w:instr="SEQ איור \* ARABIC"><w:r><w:t>1</w:t></w:r></w:fldSimple>`
  עם התוצאה ה-cached לצידה, בדיוק הצורה ש-Word עצמו כותב.
- **התווית העברית נכנסת אל תוך קוד השדה כמות שהיא.** `SEQ איור`,
  `SEQ טבלה` — לא `Figure`, לא רשימה סגורה, ולא תרגום. `label` הוא
  `string` חופשי, וכל מחרוזת מתקבלת.
- **המספור אמיתי ולפי סדר המסמך.** כיתוב שני באותה תווית מקבל 2, כיתוב
  שנוסף לפניו דוחף אותו ל-3, והסרה מורידה את השאר — גם ב-`list` וגם בערך
  שבתוך `fldSimple`. כל תווית מנהלת רצף משלה.
- **גרשיים ולוכסן מוברחים כהלכה.** `א"ב` → `SEQ "א\"ב" \* ARABIC`,
  ו-`איור \* MERGEFORMAT` → `SEQ "איור \\* MERGEFORMAT"` — כלומר אי אפשר
  להזריק מתג דרך התווית. ההפך מ-`TA`.
- **ההסרה נקייה.** הפסקה כולה יורדת מ-`blocks.list`, ואין שיירים. הסרה
  חוזרת על אותה כתובת, כתובת של פסקה רגילה, ומזהה מומצא — כולם
  `TARGET_NOT_FOUND`.
- **הכתובות ייחודיות.** שני כיתובים זהים תו-בתו מקבלים שני `nodeId`
  שונים — ההפך מתוכן העניינים, שבו ה-hash נגזר מה-`instruction`.

## מה שכן נכתב נכון — הערות שוליים והערות סיום

הקבוצה השלישית (אחרי הציטוטים והכיתובים) שעברה את שכבת ה-docx בלי
הסתייגות על מה שנכתב לקובץ. **נמדדה מחדש על 2.15.0-next.15 (15.9.2026):
עומדת, בלי רגרסיה** (שורת „ירידת שורה בתוכן” לא נמדדה שוב).

- `document.xml` מקבל
  `<w:r><w:rPr><w:rStyle w:val="FootnoteReference"/></w:rPr><w:footnoteReference w:id="1"/></w:r>`,
  ו-`footnotes.xml` נבנה שלם: `separator` ו-`continuationSeparator`
  במקומם, וכל הערה `<w:footnote w:id="…"><w:p><w:pPr><w:pStyle w:val="FootnoteText"/>`.
  הערות סיום מקבלות את אותו טיפול ב-`endnotes.xml` עם `EndnoteReference`
  ו-`EndnoteText`. זו הצורה ש-Word עצמו כותב.
- **`footnotes.update` מחליף ואינו מוסיף** — ההפך מ-`captions.update`.
  שלושה צעדים רצופים על אותה הערה החזירו `'ראשונה'` → `'שנייה'` →
  `'שלישית'`, ולא שרשור. `content: ''` **מוחק** את התוכן. כלומר עריכה כאן
  היא קריאה אחת, **בלי** `remove`+`insert` — הדרך שהכיתובים נאלצו ללכת בה
  מפני ש-`captions.update` מוסיף — ולכן גם בלי בחירת עוגן ובלי רשת שחזור.
- **העברית עוברת שלמה**, כולל ניקוד וגרשיים:
  `רַשִׁ״י בְּרֵאשִׁית א׳ א׳, ועיין ב"שו״ת הרמב״ם" סי׳ ק״י` חזר תו-בתו
  מ-`get` ונכתב תו-בתו ל-`<w:t>`.
- **הקלט של `insert` מאומת ואינו נבלע:** `content: 5` נזרק („requires a
  content string”), ו-`type: 'zigzag'` נזרק אף הוא. ירידת שורה בתוכן
  **כן** נכתבת גולמית לתוך `<w:t>`, כמו בכיתובים.
- **`remove` מוריד את ההערה** מ-`footnotes.xml` ומ-`list`, והסרה חוזרת על
  אותה כתובת מוחזרת `TARGET_NOT_FOUND`. מה שהוא כן משאיר בגוף המסמך הוא
  ריצה ריקה בסגנון `FootnoteReference` בלי `<w:footnoteReference>` בתוכה
  — שארית בלתי נראית שאין דרך ציבורית לנקות, ואינה פוגמת בתקינות הקובץ.

## מלכודות מבניות

**הפרק נמדד מחדש על superdoc 2.15.0-next.15 (15.9.2026).** אחת מן המלכודות
**נפלה** (היעד המכווץ של `citations.insert`, למטה); כל השאר משחזרות, ושלוש
שורות לא נמדדו בסבב הזה ומסומנות בגופן: מרוץ ההקלדה שאחרי `doc.insert`,
חלון ה-TOCTOU של ההערות עצמו, ו-`footnotes.insert` בלי `at`.

- **כתובות אינן בהכרח ייחודיות.** שתי טבלאות תוכן עניינים עם אותו
  `instruction` מקבלות את **אותו** `nodeId`, גם ב-`toc.list` וגם
  ב-`blocks.list` — ה-hash נגזר מה-`instruction`. `toc.remove` על הפריט
  השני מוחק את הראשון. במפתח ובטבלת מקורות אין כפילות כזאת.
- **תוכן עניינים אינו בלוק אחד.** הראשון `tableOfContents` והשאר פסקאות
  בסגנון `TOC1…TOC9`; `remove` מוחק את הראשון בלבד ומשאיר את השאר על
  המסך עם `success: true`. במפתח ובטבלת מקורות ההסרה נקייה.
- **אין מיון ואין מספרי עמודים במפתח.** הערכים מופיעים בסדר הסימון, בלי
  כותרות אותיות, למרות `\h "A"`. Word ממיין וממספר בפתיחה.
- **אין דרך למצוא ביבליוגרפיה דרך `blocks`.** ל-`citations.bibliography`
  אין `list`, ו-`blocks.list` מציג את הבלוק כ-`nodeType: 'paragraph'` רגילה.
  הדרך היחידה היא `fields.list`, שמחזיר `fieldType: 'BIBLIOGRAPHY'` ואת
  `address.blockId` — וכתובת שנבנתה ממנו מניעה `get`/`rebuild`/`remove`.
  זה גם מה שמאפשר לעבוד על ביבליוגרפיה שנוצרה ב-Word.
- **`citations.insert` אינו דורש עוד יעד מכווץ — השתנה.** נמדד על
  2.15.0-next.15 (15.9.2026): טווח שאינו מכווץ (0..3) **מתקבל**, הציטוט
  נכנס בתחילת הטווח, והטקסט שבטווח אינו מוחלף; הסירוב שהיה כאן
  (`INVALID_TARGET`, „requires a collapsed text target”) אינו קיים עוד.
  הכיווץ שאנחנו עושים לפני הקריאה — אל **סוף** הבחירה — נשאר הנכון: הוא
  קובע את מקום הציטוט במקום לקבל את מה שהמנוע יבחר בשקט. סמן **בתוך** שדה
  קיים לא נמדד מחדש, ועד שיימדד ההנחה היא שהוא עדיין
  `CAPABILITY_UNAVAILABLE` („text-range-in-field”).
- **`citations.sources.update` הוא `Partial` אמיתי.** נמדד בשני הכיוונים:
  patch **בלי** `year` השאיר את `תש״ף` שבמסמך כמו שהיה, ו-patch עם
  `year: ''` מחק אותו. כלומר השמטה משמרת ומחרוזת ריקה מוחקת — וטופס עריכה
  שמשמיט שדה שהמשתמש רוקן מייצר „הצלחה מדומה”: `{ok:true}` בלי הודעה,
  והערך חוזר ברענון הבא. מי ששולח patch חייב להחליט לכל שדה מה משמעות
  הריקון אצלו.
- **תצוגת הציטוט אינה מתרעננת אחרי עריכת המקור.** כותרת שהשתנתה מתעדכנת
  בביבליוגרפיה אחרי `rebuild`, אבל הטקסט שבתוך שדה ה-`CITATION` נשאר
  הישן עד `citations.update` על אותו ציטוט. Word מחשב מחדש בפתיחה.
- **מחבר בלי `last` מפיל את המנוע** ב-`TypeError` גולמי ולא בקבלה:
  „Cannot read properties of undefined (reading 'trim')”.
- **אין API להזזת הסמן בין stories.** `doc.selection` הוא קריאה בלבד, ולכן
  אי אפשר להעביר את הסמן אל גוף הכותרת העליונה או אל הכותרת התחתונה.
- **`doc.insert` אינו מזיז את הסמן אל סוף מה שהוכנס**, ואין פעולה שתזיז אותו
  (אותה קריאה-בלבד שלמעלה: יש `selection.current` ו-`selection.delete`, ואין
  `selection.set`). נמדד ב-`scripts/qa/acronym-completion-qa.mjs`: קבלת השלמה
  ב-Tab על „חז"ל ” כותבת „חכמינו זכרונם לברכה”, והתו הבא שהוקלד נחת **לפני**
  הטקסט שנכתב — „חז"ל א חכמינו זכרונם לברכה”. נוגע לכל השלמה שמכניסה טקסט
  ליד הסמן (engine/book-completion-overlay.ts), ולא לראשי-התיבות בלבד.
  **לא נמדד מחדש על 2.15.0-next.15:** השער עובר 9/9, אך הוא בודק רק שההשלמה
  נכתבת ואינו מקליד תו מיד אחרי `Tab` — כלומר אינו מכסה את המרוץ עצמו, ושחזור
  שלו דורש תזמון הקשות אמיתי. גם אילו היה נסגר, אין במה לתקן: `selection.set`
  עדיין אינו קיים (השורה שמעל, שכן נמדדה — `doc.selection` הוא `current` בלבד).
- **`selection.current` אינו מדווח מקטע** — אבל המיפוי כן ניתן להרכבה, וזה
  מה שנעשה. `SectionDomain.range` מחזיר `startParagraphIndex`/
  `endParagraphIndex` באינדקסים של הגוף, ו-`blocks.list()` מחזיר את הגוף
  לפי הסדר; שילוב השניים הופך `blockId` של הבחירה למקטע שלה
  (`sectionsForSelection` ב-engine/page-setup.ts). נמדד ב-
  `scripts/qa/section-scope-mapping-probe.mjs` על מסמך של שש פסקאות ושלושה
  מקטעים: המיפוי מדויק — **חוץ מטווח המקטע האחרון, שמדווח חסר**:
  `[0..1]`, `[2..3]` ואז `[4..4]` במקום `[4..5]`. כלומר הפסקה האחרונה של
  המסמך אינה בטווח של אף מקטע, וכל קורא של `range` חייב למתוח את המקטע
  האחרון עד סוף הגוף בעצמו.
  **נמדד מחדש על 2.15.0-next.15 (15.9.2026), בשני מסמכים:** שש פסקאות
  ושלושה מקטעים נותנות שוב `[0..1]`, `[2..3]`, `[4..4]`; ואחת-עשרה פסקאות
  ושלושה מקטעים נותנות `[0..5]`, `[6..8]`, `[9..9]` — הפסקה האחרונה (10)
  שוב מחוץ לכל טווח. המיפוי עצמו של סמן→מקטע מדויק בשלושת המקטעים, ו-
  `blocks.list({nodeIds})` מחזיר את האורדינל בגוף ואת גודל הגוף — כלומר
  המתיחה עד סוף הגוף, שהיא מה שנעשה כאן, נשענת על מה שנמדד ועובד. הערה
  לגשש: בחלון ברירת המחדל המקטע השלישי מתחיל בעמוד חדש מתחת לקצה החלון,
  ולכן המדידה דורשת חלון גבוה — כפי שהשער עושה.
- **אין דרך ליצור מקטע סביב הבחירה — שלושה מסלולים, שלושתם סגורים.** זה מה
  ש-Word עושה ב„עמודות ← החל על: הטקסט הנבחר”: שני מעברי מקטע **רציפים**
  סביב הבחירה, ו-`w:cols` על מה שביניהם. נמדד על 2.14.0-next.5, **ונמדד
  מחדש על 2.15.0-next.15 (15.9.2026): שלושת המסלולים מחזירים בדיוק אותן
  תשובות, ו-`sections.list` נשאר על מקטע אחד**:

  | מסלול | תשובת המנוע | הגשש |
  | --- | --- | --- |
  | `create.sectionBreak({at:{kind:'before'\|'after', target}})` | `INVALID_TARGET` — „create.sectionBreak currently supports body documentEnd targets on v2” | `section-break-scope-probe.mjs` |
  | פקודת ProseMirror | `editor.commands` **ריק לגמרי** (0 מפתחות), ואין צומת/מאפיין מקטע בסכימה | `section-command-surface-probe.mjs` |
  | `doc.insert` עם צומת `sectPr` או `sectionBreak` בפרגמנט | `CAPABILITY_UNSUPPORTED` — „Fragment block kind \"sectPr\" is not supported for rich inbound application” | `section-fragment-probe.mjs` |

  ‏`representation: 'attachToPreviousParagraph'` אינו עוקף: הוא נדחה באותה
  הודעה, כלומר הבדיקה על ה-`at` קודמת לו. שני הצמתים **כן** רשומים ב-
  `SD_CONTENT_NODE_KINDS` ויש להם טיפוסים מלאים (`SDSectPr`, `SDSectionBreak`)
  — הקטלוג מונה אותם, והיישום דוחה אותם.

  דווח למעלה: [#4009](https://github.com/superdoc/docx-editor/issues/4009).
  **אינו ניתן ל-PR**, ולא מפני שחסר לנו זמן: `createDocumentApi` מקבל את
  ה-adapters מבחוץ, והמוטציה עצמה יושבת בחבילת המנוע הקניינית — בעץ הפתוח
  יש רק את החוזה. לכן ה-issue שואל מי אחראי על ה-adapter של v2 ומתי, ולא
  מציע טלאי. הטיעון שם נבנה **מהקוד הפתוח בלבד** (חוזה, קטלוג הצמתים, ובדיקת
  הפריסה `section-breaks-columns.test.ts` שכבר מפרסת מעבר רציף עם שינוי
  מספר טורים באמצע הגוף) — ראו את מגבלת הרישיון על פרסום מדידות מנוע.

  נסרקו כל 65 האישיוז הפתוחים לפני הפתיחה: שבעה נוגעים לטורים או למקטעים,
  וכולם על **ציור** של מקטע קיים (‏#3990, #3966, #3967, #3964, #3960, #3946,
  #3996). אף אחד מהם אינו על **יצירת** גבול מקטע, כלומר אין אישיו שתיקונו
  היה פותר את זה.

  #### מה נבנה כאן ביום ש-#4009 ייסגר

  זה אינו „נשפר קצת” אלא **שכבה שלמה שחסומה היום**, ולכן היא נכתבת כאן ולא
  נזכרת מחדש מאפס. שלושה דברים, לפי סדר הערך:

  1. **„החל על: הטקסט הנבחר” אמיתי.** `sectionsForSelection`
     (engine/page-setup.ts) כבר יודע לקחת בחירה ולהחזיר את המקטעים שלה; מה
     שיתווסף לפניו הוא צעד **יצירה**: `create.sectionBreak({at:{kind:'before',
     target:<פסקה ראשונה בבחירה>}, breakType:'continuous'})` ועוד אחד `after`
     על האחרונה, שניהם ב-`representation:'attachToPreviousParagraph'` כדי
     שלא תיוולד פסקה נושאת ריקה שהמשתמש לא ביקש. משם המסלול הקיים ממשיך בלי
     שינוי — הבחירה נופלת במקטע החדש, והכתיבה מצטמצמת אליו מאליה. כלומר
     התיקון יושב בנקודה אחת, וכל תשעת פקדי המקטע יורשים אותו יחד, בדיוק כפי
     שהם יורשים היום את הצמצום.
  2. **`WHOLE_DOCUMENT_NOTE` יורדת.** ההודעה קיימת **רק** בגלל הפער הזה:
     היא מסבירה למשתמש למה סימן פסקה וכל המסמך השתנה. ברגע שהבחירה ניתנת
     לתחימה אין מה להסביר, וההודעה הופכת לרעש. השלב `single` ב-
     `scripts/qa/section-scope-qa.mjs` הוא שמודד אותה היום, והוא זה שיתהפך
     לשער ההתנהגות החדשה — אותו מסמך, אותה בחירה, ובדיקה ש-`[1,2,1]` נכתב
     במסמך שהיה בעל מקטע יחיד.
  3. **גלריית „מעברים” של Word.** `setBreakType` עובד וכותב `<w:type>`
     קנוני, ולא נשלח רק מפני שבמסמך בעל מקטע יחיד אין לו מה לעשות (ראו
     „`setBreakType` — עובד, ולא נשלח” למעלה). ברגע שאפשר **ליצור** מקטע
     באמצע המסמך, „מעברים ← עמוד / עמודה / מקטע רציף / עמוד זוגי / אי-זוגי”
     הופך לפקד שלם — וזו הקבוצה הראשונה בלשונית „פריסה” של Word שחסרה אצלנו
     כולה, ולא פקד בודד.

  **מה שלא ישתנה:** מסמך שכבר יש בו מקטעים ימשיך לעבוד כמו היום — הצמצום שם
  אינו תלוי ב-#4009 ואינו ממתין לו. זו הסיבה שהוא נבנה עכשיו ולא נדחה:
  **כל** מסמך Word עם מעברי מקטע כבר מקבל את ההתנהגות הנכונה, והפער נשאר
  פתוח רק במסמך בעל מקטע יחיד.

  **מה שנעשה בעקבות זה:** במסמך שכבר יש בו מקטעים הפעולה מצטמצמת לאלה
  שהבחירה נוגעת בהם (זה כל מסמך Word עם מעברי מקטע, ושם ההתנהגות הקודמת —
  כתיבה לכולם — הייתה באג); במסמך בעל מקטע יחיד אין מה לתחום, הפעולה חלה על
  הכול **ואומרת את זה** בפס המצב כשהמשתמש סימן טקסט. אין קוד שמנחש: היעדר
  `blocks.list`, מזהה שאינו בגוף (פסקה בתוך תא — `list-resolver-qa.mjs`)
  או בחירה ריקה מחזירים את כל המקטעים, כמו קודם.
- **כתובת מקטע היא `section-<index>`, ו-`refStability` שלה `'ephemeral'` —
  ובכל זאת היא יציבה.** `create.sectionBreak` תומך ב-v2 **רק** ב-
  `documentEnd` (`at: {kind:'before', …}` מוחזר `INVALID_TARGET`:
  „supports body documentEnd targets on v2”), ולכן מקטע חדש מקבל תמיד את
  האינדקס הבא ואינו מזיז את הקיימים. נמדד: תצלום של הכתובות שרד מוטציה על
  מקטע אחר וגם הוספת מקטע — `snap[1]` המשיך להיפתר לאותו מקטע עם אותם
  ערכים. כלומר החלון שהיה הופך את הלוגו של „החל על כל המקטעים” ל-TOCTOU
  סגור מצד המנוע, ולא מצידנו.
- **`sections.list` הוא `DiscoveryOutput` עם `limit` של 250, ו-`applyToSections`
  קורא עמוד אחד — פער **קיים**, לא פער של גל 10.** `list()` מחזיר
  `page: { limit: 250, offset: 0, returned: N }` (נמדד), `SectionsListQuery`
  **כן** חושף `offset` (`{limit:1,offset:1}` החזיר את המקטע השני עם `total`
  מלא), ו-`limit: -3` **זורק** ואינו מוחזר ריק כמו ב-`footnotes.list`.
  `applyToSections` שב-`page-setup.ts` קורא `list()` בלי ארגומנטים, **אינו
  משווה `items.length` ל-`total`, ואינו מדפדף** — כלומר במסמך של 251 מקטעים
  ומעלה הפעולה חלה על 250 בלבד והמשתמש מקבל „בוצע”.

  **הסיווג חשוב:** זו התנהגות של `applyToSections` מגל 1, והיא חלה באותה
  מידה על ארבעת הפקדים שקדמו — שוליים, כיוון, גודל נייר ועמודות. חמשת
  הפקדים של גל 10 יורשים אותה ואינם מקורה. **הפער לא נסגר כאן בכוונה**,
  והוכרע לגל נפרד: התיקון משנה את התנהגותו של המודול כולו, ודורש הכרעה
  שאינה טכנית — מה מדווחים כשמקטע נכשל באמצע דפדוף, אחרי שעמודים שלמים
  כבר שונו ואין `rollback`.
- **פסקת כיתוב נכתבת בלי `<w:bidi/>`.** `captions.insert` כותב
  `<w:pPr><w:pStyle w:val="Caption"/></w:pPr>` ותו לא, בעוד שהפסקה הרגילה
  שלצידה במסמך העברי כן נושאת אותו — כלומר הכיתוב ייפתח ב-Word משמאל
  לימין. `paragraphs.setDirection({direction:'rtl'})` מתקן: נמדד שהוא
  מוסיף `<w:bidi/>` לאותה `pPr` ומשאיר את סגנון ה-`Caption` על מקומו
  (`alignmentPolicy: 'matchDirection'` מוסיף גם `<w:jc w:val="right"/>`,
  ולכן אינו נשלח). על פסקה שכבר ימין-לשמאל התשובה היא `NO_OP`.
- **`paragraphs.setDirection` אינו בקטלוג היכולות.**
  `capabilities.get().operations['paragraphs.setDirection']` הוא
  `undefined`, אף שהפעולה עצמה עובדת וכותבת `<w:bidi/>` (נמדד). כלומר
  הקטלוג אינו רשימה מלאה של הפעולות הקיימות, וקוד שנועל פקד על „הפעולה
  בקטלוג” היה מנטרל כאן פעולה תקינה. הבדיקה היחידה שאפשר לסמוך עליה היא
  `typeof doc.paragraphs?.setDirection === 'function'`.
- **„טבלת איורים” אפשרית, אבל ריקה.** `create.tableOfContents` מקבל
  `instruction` גולמי, ו-`TOC \c "איור" \h \z` נכתב קנונית לתוך ה-sdt של
  תוכן העניינים; `toc.list` מחזיר אותו עם
  `preserved.seqFieldIdentifier: 'איור'`. אבל המנוע **אינו** אוסף את
  הכיתובים אליה: `entryCount` נשאר 0 גם אחרי `toc.update`. Word ימלא
  אותה בפתיחה, בדיוק כמו את המפתח. (`toc.configure` אינו יכול להגדיר את
  `\c` — הוא ב-`TocPreservedSwitches`, שמוגדר „round-tripped but not
  configurable”.)
- **כיתוב שמתווסף כשהסמן בתוך תא של טבלה נכתב לתוך התא, ולא טופל.**
  `insertCaption` מעגן תמיד את הבלוק שהסמן דיווח עליו, בלי לבדוק אם הוא
  יושב בתוך תא: לחיצה בתוך תא מדווחת את פסקת התא עצמה ולא את הלוח שמכיל
  אותה (נמדד), ולכן הכיתוב נצמד לאותה פסקה ונכתב בתוך התא, לא ליד הלוח.
  `blocks.list` שטוח — אין בו דרך זולה לעלות מפסקת תא אל הטבלה שמסביבה —
  ולכן אי אפשר לתקן כאן באותה שיטה שתיקנה את העוגן של `captions.update`
  ושל `captions.insert` למעלה. נמצא תוך כדי אותה עבודה, ונשאר פתוח.
- **`displayNumber` של הערה אינו המספר ש-Word יציג.** הוא זהה ל-`noteId`,
  כלומר לסדר ה**יצירה**: הערה שנוספה בהמשך המסמך וקיבלה 1 נשארת 1, והערה
  שנוספה אחריה במקום מוקדם יותר בטקסט מקבלת 2 — בעוד ש-Word ממספר לפי סדר
  ההופעה (נמדד ב-docx: הרפרנסים יושבים בגוף לפי המיקום). בנוסף, `remove`
  **אינו** ממספר מחדש את השאר: הסרה של 2 השאירה 1 ו-3, גם ב-`list`.
  `footnotes.list` גם מחזיר את הפריטים בסדר היצירה ולא בסדר המסמך, ואין
  בכל ה-API דרך לדעת היכן הערה יושבת. כל ממשק שמציג את המספר הזה חייב
  לומר שהוא סדר היצירה.
- **אימות לפני מוטציה על הערה הוא חלון פתוח.** הכתובת אינה מבדילה בין
  הערת שוליים להערת סיום, ולכן כל עריכה והסרה מאמתות ב-`get` — אבל ה-`get`
  חוצה גבול macrotask, וכל הוספה שנקלטת בזמן הזה משנה את מה שהכתובת
  נפתרת אליו. ההגנה היחידה היא נעילה בצד הממשק בזמן שהפעולה באוויר; ראו
  „כתובת אחת לשתי הערות שונות” למעלה. (**המרוץ עצמו לא הורץ מחדש בסבב
  2.15.0-next.15** — הוא דורש לחיצה אמיתית ברצועה בתוך חלון ה-`get`;
  העמימות שמתחתיו, שתי הערות על כתובת אחת, כן נמדדה מחדש ועומדת.)
- **`footnotes.insert` בלי `at` דורש בחירה חיה** (**לא נמדד מחדש על
  2.15.0-next.15** — התנאי הוא של headless, והגשש של הסבב הזה רץ עם תצוגה
  חיה). ב-headless (שם `activeEditor.view` הוא `null`) הוא מוחזר
  `PRECONDITION_FAILED / live-selection-unavailable`. זה גם מה שקובע
  שהוספת הערה שייכת לכפתור ברצועה ולא לדיאלוג: מרגע שדיאלוג נפתח, הסמן
  אינו בעורך.
- **`blocks.list` מחזיר מעטפה אחרת מכל שאר ה-discovery**: `blocks` ולא
  `items`. קוד שקורא `items` מקבל `undefined` על מסמך שיש בו פסקה.
- **`doc.insert` פשוט מוסיף לפסקה האחרונה** ואינו יוצר פסקה חדשה, ולכן
  אי אפשר לזרוע שתי פסקאות בשתי קריאות.
- **`activeEditor.view` הוא `null` ב-headless** — אי אפשר למדוד שם שום דבר
  שדורש מיקוד בעורך.

## פעולות שהמנוע מסמן כלא-זמינות

`create.image`, `images.delete`, `images.replaceSource`, `hyperlinks.patch`
— `OPERATION_UNAVAILABLE`. אין לבנות עליהן פקד פעיל.

**וגם כאן הקטלוג אינו רשימה נכונה — נמדד על 2.15.0-next.15 (15.9.2026),
ואותה תוצאה על 2.14.0-next.5:** ‏`create.image` עם PNG כ-`data:` **מצליחה
בפועל** — היא כותבת `word/media/image1.png` ו-`w:drawing` אחד, ו-
`images.list` מחזיר את התמונה — בעוד הקטלוג ממשיך לדווח עליה
`available: false`. זו המלכודת של `paragraphs.setDirection` מהכיוון ההפוך:
שם פעולה שעובדת חסרה מהקטלוג, כאן פעולה שעובדת רשומה בו כלא-זמינה. המסקנה
אחת לשני הכיוונים — **הקבלה של הקריאה עצמה היא העדות היחידה**, לא הקטלוג.
שלוש הפעולות האחרות לא נמדדו מחדש.

## `format.paragraph.*` — גל 11, מה שנמדד לפני המימוש

Chrome headless על ה-dist הארוז; כל סבב מלווה בפירוק ה-zip של
`export.toDocx`. ההנמקות ב-engine/paragraph-format.ts, וזה הפער:

- **היחידות הן twips גולמיים.** `setIndentation({left:720})` כתב
  `<w:ind w:left="720"/>`, `setSpacing({before:240})` כתב `w:before="240"` —
  אחד לאחר, בלי המרה. **שונה** מ-`sections.*`, שם ה-API מקבל אינצ'ים וכותב
  `Math.round(v*1440)`. מי שמעביר ערכי UI ישירות כותב שוליים במידות מטר.
- **כל קריאה מחליפה את האלמנט כולו.** `setIndentation({left:-500})` אחרי
  `setIndentation({left,right,firstLine})` השאיר `<w:ind w:left="-500"/>`
  בלבד. אותו דין ל-`w:spacing`. אין patch; כל ממשק חייב לשלוח מצב מלא.
- **מה שהמנוע מאמת וזורק** (`INVALID_INPUT`, ולא קבלה): ערך שאינו מספר
  שלם (`hanging:0.5` → „must be a non-negative integer”), שלילי בריווח
  (`before:-240`), וערכי enum (`lineRule:'zigzag'`, alignment `'zigzag'`
  בטאב). הזריקות מחייבות catch אצל כל קורא.
- **מה שעובר בשקט ונאסר אצלנו:**
  - `setIndentation({left:-500})` → `success:true` ו-`w:left="-500"`.
    חוקי ב-OOXML (`ST_SignedTwipsMeasure`) אך לא מוצע ב-Word — ולא אצלנו.
  - `setTabStop({position:-100})` → `success:true`. `w:pos` שלילי אינו
    חוקי ב-ECMA-376; השער יושב במודול שלנו (מיקום חייב להיות שלם > 0).
- **NO_OP:** קריאה חוזרת עם ערכים זהים מחזירה
  `success:false / code:'NO_OP'` — „produced no changes”. זו הצלחה
  מבחינת המשתמש, כמו בכל המרחבים האחרים.
- **טאבים הם רשימה:** `setTabStop` **מוסיף** ואינו נוגע באחרות (שתי קריאות
  השאירו `<w:tab w:val="center" w:pos="1440" w:leader="dot"/>` ו-
  `<w:tab w:val="right" w:pos="2880"/>` יחד); `clearTabStop({position})`
  מוריד יעד יחיד; `clearAllTabStops` מוריד את `<w:tabs>` כולו.
- **קריאת המצב בנקודות.** `doc.get()` מחזיר SDM/1 שבו ה-indentation,
  ה-spacing וה-tabs הם ב**נקודות** (והטאבים נושאים `kind:'set'|'clear'`) —
  פי 20 מה-API של הכתיבה. הקורא (`readParagraphFormat`) הוא המקום היחיד
  שמכיר את שתי המערכות.
- **keep options:** `keepNext`/`keepLines` נכתבים `<w:keepNext/>`,
  `<w:keepLines/>`; `widowControl:false` נכתב `<w:widowControl w:val="0"/>`.
  סדר הילדים ב-`pPr` יוצא מהמנוע קנוני ועובר round-trip.

## `format.apply` ומשפחת `format.<inlineKey>` — גל 12, מה שנמדד

Chrome headless על ה-dist הארוז; פירוק zip לכל סבב. ההנמקות ב-
engine/font-advanced.ts:

- **היחידות — ה-API בנקודות:** `letterSpacing: 2` → `w:spacing="40"` (×20,
  twips); `position: 3` → `w:position="6"`, `kerning: 12` → `w:kern="24"`,
  `fontSizeCs: 12.5` → `szCs="25"` (×2, חצאי-נקודות — **חצאי נקודות
  מקובלות**, כמו fontSize); `charScale` אחוזים כמות-שהוא.
- **letterSpacing ו-position חתומים:** שלילי עובר (`-20` → `-400`) וזה
  חוקי — „מכווץ"/„מונמך". לעומתם `charScale` ו-`kerning` דורשים שער:
  - `charScale: 9999` → `success:true` ו-`w:w="9999"` — Word תחום
    1..600 אחוז.
  - `kerning: -5` → `success:true` ו-`w:kern="-10"` — ST_HpsMeasure
    אינו חתום.
- **הליבה העברית נכתבת קנונית:** `rtl/cs/bCs/iCs` → `<w:rtl/>` וכו';
  `fontSizeCs` → `szCs`; `lang {bidi:'he-IL'}` → `<w:lang w:bidi=...>`;
  `rFonts {cs:'David'}` → `<w:rFonts w:cs="David"/>`.
- **פער במנוע — `bold` על עברית כותב `w:b` בלבד, בלי `w:bCs`.** Word מציג
  הדגשה של טקסט מורכב מ-`bCs`; ריצה עברית עם `b` לבד אינה תוצג מודגשת.
  פקד ה-bold (דרך הפקודה) וגם `format.bold` מתנהגים כך. עקיפה אצלנו:
  הדיאלוג מציע „מודגש (מורכב)" דרך `bCs`. הפער עצמו מדווח כאן.
- **אין קריאת מצב לאפקטים — ולכן הפקד תלת-מצבי.** אף אחד משלושת המשטחים
  שהממשק קורא מהם אינו מדווח את הערך של `dstrike`/`outline`/`shadow`/
  `emboss`/`imprint`/`vanish` ושל משפחת ה-CS על הבחירה: `ui.commands` אינו
  מכיל פקודות כאלה, `selection.current().activeMarks` מחזיר שמות marks בלבד,
  ו-`query.match()` מדווח bold/italic/underline/strike, צבע, הדגשה, גופן
  וגודל — ותו לא (אותו רישום בדיוק כמו ב-`vertAlign`; ראו engine/vert-align.ts).
  לכן כפתור דו-מצבי ב„גופן מתקדם” היה **משקר** על טקסט שכבר נושא את האפקט,
  ו„כבוי” שלו היה מסיר צל/מסגרת/חריטה מכל מה שסומן באישור אחד. הפקד
  (ui/panels/common/TriToggle.vue) מחזיק לכן מצב שלישי — „ללא שינוי” — שאינו
  נשלח כלל. מעקב אחר קריאת המצב:
  <https://github.com/superdoc/docx-editor/issues/3982>; ברגע שהיא תהיה
  זמינה, הכפתור יוכל להיפתח על המצב האמיתי ולהיות דו-מצבי כמו B/I/U ברצועה.
  **נמדד מחדש על 2.15.0-next.15 (15.9.2026) — פתוח.** רשימת הפעולות זהה
  לזו של 2.14.0-next.5: `format.apply` הוא עדיין היחיד לעיצוב ריצה ואין לו
  `format.get`; `SelectionInfo` נושא `activeMarks` בלבד; ו-`query.match`
  עדיין מדווח מודגש/נטוי/קו תחתון/קו חוצה, צבע, הדגשה, גופן וגודל — בלי
  `bCs`/`iCs`/`szCs`/`rFonts.cs`/`kern` ובלי האפקטים. באותה ריצה נמדד גם
  שהכתיבה **מטליאה**: `setFlowOptions` עם `pageBreakBefore` ואחריו
  `contextualSpacing` השאיר את שניהם ב-`pPr` — אבל `doc.get()` מחזיר לאותה
  פסקה `{"bidi":true}` ותו לא, כלומר אין מה לקרוא. ה-TriToggle נשאר תלת-מצבי.
- **NO_OP מופיע רק ב-`format.apply`:** ה-alias הבודדים החזירו
  `success:true` גם על חזרה זהה; `apply` מחזיר NO_OP ("produced no
  change") כשה-patch לא משנה דבר.
- **`format.<key>` דורש SelectionTarget** ואינו מקבל TextTarget
  („target must be a SelectionTarget object") — וב-headless `view` הוא
  null ואין בחירה חיה, כלומר היעד מגיע תמיד מהממשק (vert-align.ts).
- **`vanish:false` כותב `w:vanish w:val="0"`** — הסרה מפורשת בדיוק כמו
  Word, ולא הסרת האלמנט; round-trip תקין.
- **`rStyle` לא נשלח** — נוגע בסגנונות תו; גל 13 (סגנונות) הוא בעליו,
  ושני מסלולים לאותה כתיבה הם באג. גם `webHidden` דילג: אין לו משמעות
  ממשק מחוץ לתשתית ההסתרה של Word.

## `styles.*` — גל 13, מה שנמדד

- **`format.paragraph.setStyle/setStyleRef/clearStyle` אינן נתמכות
  בדפדפן** למרות ש-`capabilities.get()` מדווח עליהן `available:true`:
  הריצה מחזירה `CAPABILITY_UNAVAILABLE` ("not a supported v2 browser
  Document API operation"). **הסתירה החדה ביותר במאגר** של הכלל „available
  אינו הוכחה". הפקודה `linked-style` נשארת המסלול היחיד להחלת סגנון על
  תוכן — אין להוסיף מסלול Document API מקביל.
- **`resetDirectFormatting` כן עובד** (success), וכבר מיוצג בפקודה
  „נקה עיצוב".
- **docDefaults (ערוץ run) ביחידות גולמיות:** `patch {fontSize: 14}` →
  `<w:sz w:val="14"/>` = **חצאי-נקודות**, ולא נקודות כמו `format.fontSize`
  (שם 24 → sz 48). `fontFamily` record `{ascii,hAnsi,cs}` נכתב ישירות
  ל-`w:rFonts`. ההמרה pt→×2 יושבת ב-engine/doc-style-defaults.ts.
- **`dryRun` הוא קריאת המצב היחידה:** אין `styles.get`;
  `apply(...,{dryRun:true})` מחזיר `before/after` בלי לשנות. במסמך הריק
  `before.fontSize = 24` (=12pt).
- **חזרה זהה אינה NO_OP אלא `success:true, changed:false`** — שונה מכל
  מרחב אחר; מבחינת המשתמש זו הצלחה.
- **`getCatalog({view:'quickGallery'})` עובד:** 7 פריטים במסמך ריק,
  `sourceStatus.styles:'present'`, שמות כפי שהם במסמך (`heading 1`
  באות קטנה). הגלריה כבר צורכת אותו דרך `ui.styles` מגל קודם — לא
  שוכפלו כאן שני מסלולי קריאה. **שבעה הם גם מה שחסר:** `Heading2`…`Heading9`
  אינם ביניהם, והתוצאה הייתה שאי אפשר לבנות תוכן עניינים ביותר מרמה אחת.
  ראו „תוכן עניינים — שני פערים שהתגלו מדיווח משתמש”.

### אין פעולה שמגדירה סגנון בשם — ולכן ירדו „חידוש”/„קושיא”/„תירוץ”

בקטלוג הפעולות (2.8.0, **ונבדק מחדש על 2.15.0-next.15 ב-15.9.2026: 429
פעולות בקטלוג החי, ומתוכן `styles.*` הן עדיין `apply`, `getCatalog` ושלוש
ה-`paragraph.*` בלבד**) אין דבר שיוצר סגנון פסקה חדש:

- `styles.apply` מקבל `target: { scope: 'docDefaults' }` **בלבד** — הוא משנה
  את ברירת המחדל של המסמך כולו, לא מגדיר סגנון.
- `styles.paragraph.setStyle` מחיל סגנון **קיים** לפי `styleId`, או אחד
  מארבעה תפקידים סמנטיים (`defaultParagraph`, `heading`, `title`,
  `subtitle`). אין בהם „חידוש”.
- מרחב `styles.*` כולו הוא `apply`, `getCatalog` ושלושת ה-`paragraph.*`.

`linked-style` עם מזהה שאינו קיים במסמך פשוט נכשל, ומיפוי „קושיא” אל סגנון
בנוי כמו Heading 2 היה כפתור שעושה משהו אחר ממה שכתוב עליו. הוספת הסגנונות
לקטלוג ה-docx ב-XML ידני נשללה (§12).

לשונית „אוצריא” נשאה בגלל זה שלושה כפתורים מנוטרלים, עם הסבר בטולטיפ. הם
**ירדו** מהלשונית — ולא בגלל הניטרול: הפקד עצמו אינו נצרך, ולא נתבקש.

הבקשה למנוע **נשארת פתוחה כמות שהיא** ואין לגעת בה:
https://github.com/superdoc/docx-editor/issues/3975. ירידת הכפתורים אינה
סיבה למשוך אותה — פעולה שמגדירה סגנון בשם נצרכת גם לדברים אחרים, ו„עיצוב
מילה ראשונה” (engine/shulchan/first-word.ts) הוא הראשון שבהם: בהיעדרה הוא
עושה עיצוב ישיר במקום סגנון תו.

## `lists.*` — גל 14א, התשובה לשאלת המספור העברי

- **`setLevelNumberStyle` עם `'hebrew1'` עובד.** `numberStyle` הוא
  **string חופשי** בחוזה (לא union) — ההפך מ-`sections.setPageNumbering`.
  נמדד: `<w:numFmt w:val="hebrew1"/>` נכתב ל-numbering.xml. מספור
  א׳ ב׳ ג׳ אפשרי, ומיושם בתפריט „רשימה" ב„בית". השער שלנו: רשימת
  numFmt תקניים של ECMA-376; מחוץ לה — נדחה.
- **`restartAt({startAt})`** עובד; **`continuePrevious`** מחזיר קבלת
  כשל `INVALID_CONTEXT / NO_PREVIOUS_LIST` כשאין קודם (לא זורק);
  **`canContinuePrevious` בוליאני = TOCTOU** — לא נשלח לפני פעולה,
  הקבלה עצמה מדווחת.
- **`convertToText({includeMarker:true})`** מעתיק את סמן הרשימה
  ('a. ') לתוך הטקסט והפריט הופך לפסקה — בלתי-הפיך למעשה; הפקד דורש
  אישור דו-לחיצה.
- **כתובת פריט:** `{kind:'block', nodeType:'listItem', nodeId}`; פסקה
  רגילה מקבלת `target.nodeType must be 'listItem'`. היעד נפתר אצלנו
  מהבחירה + `blocks.list`.
- **`lists.create mode:'fromParagraphs'`** מקבל BlockAddress בודד או
  BlockRange `{from,to}` — **לא מערך**.

## גלים 17–25 — ממצאי מדידה והכרעות

נמדדו בסבב אחד על ה-dist הארוז (Chrome headless, CDP):

### גל 17 — תמונות: דחייה מנומקת
`images.list` עובד (ריק), אך `create.image` לא-זמין ו-HTML `<img data:>`
נדחה `INVALID_PAYLOAD` — אין דרך לקבל תמונה במסמך, ולכן אין מה לבדוק
ואין מה לבנות. כפי שהמדריך קבע: „הגל הוא תיעוד ולא קוד".

**הנימוק התיישן (נמדד 15.9.2026):** `create.image` עם `data:` **כן** מכניסה
תמונה למסמך, אף שהקטלוג מדווח אחרת. מה שנשאר נכון הוא ש-HTML עם `<img>`
נדחה, ושפקד תמונות לא נבנה בגל ההוא. אם ייבנה אי פעם, הוא נשען על הקבלה של
הקריאה ולא על הקטלוג.

### גל 18 — metadata: חוסך בין החוזה לתיאור
`metadata.*` בחוזה הוא anchored metadata (JSON מוסתר ב-SDT + Custom XML
Storage Part על טווח טקסט) — ולא „מאפייני מסמך" (כותרת/מחבר). נמדד
עובד: attach/list עם payload עברי (`customXml/item1.xml`). אין API
שכותב docProps ב-2.8.0, ולכן לא נבנה UI „מאפיינים" על מצג שווא.
התשתית בעלת ערך עתידי (למשל קישור מקור→ציטוט).

### גל 19 — הגנת מסמך: מיושם
מסלול הביטול נמדד לפני ההפעלה ועובד ללא סיסמה: set(readOnly) →
enforced:true; capabilities.get אחריה: **4 פעולות נפלו ל-false**
(התוסף עצמו מוגבל!); clear → enforced:false. מיושם כמתג עם אישור
דו-לחיצה ב„סקירה" (engine/protection.ts).

### גל 20 — diff: דחייה עד לפתרון ה-host
`diff.capture` עובד (`sd-diff-snapshot/v2`, fingerprint sha256).
`diff.compare` דורש **מסמך שני** — כלומר `fs.pickUserFile` של אוצריא;
בלעדיו אין מסלול משתמש. `diff.apply` הרסני ולא נבדק בנפרד.

### גל 21 — תגובות: דחייה על חוסם זהות
`comments.create` דורש מחבר; לאוצריא אין מודל זהות (`app.getInfo`
אינו מחזיר משתמש) — §13.1 דורש הגדרת משתמש מקומית, שעדיין אינה
קיימת. הפקד „תגובה חדשה" נשאר מנוטרל עם הסבר. `trackChanges.decide`
מתנגש בשש הפקודות הקיימות ולא נשלח; `history.undo/redo` מתנגשות
בפקודות undo/redo ולא נשלחות.

### גל 22 — hyperlinks: מיושם (ראו למעלה)
wrap/remove דורשים TextAddress; wrap דורש מפרט `{link:{destination}}`;
patch לא זמין → עריכה = remove+wrap.

### גל 23 — blocks/create: רובו דחוי — אין פקד Word
`create.paragraph({at:{target,placement:'after'}})` עובד.
`blocks.delete` על פסקה עם `w:bidi` זרק
`paragraph-tracked-wrapper-unsupported` — האי-עקביות שהמדריך ציין
**אושרה**. כל הפעולות הרסניות ואין להן מקבילה ברצועת Word — דילוג
לפי השאלה שהמדריך מציב.

### גל 24 — plan.execute: חוסם חתימת input ל-bookmarks.insert
`plan.execute` רץ אך נכשל בשלב הראשון: "Cannot use 'in' operator to
search for 'story' in undefined" — bookmarks.insert דרך plan דורש
input שונה מה-direct call. נדרש מיפוי מלא של חתימות ה-plan לכל
operationId לפני שאפשר לשלוח מאקרו. mutations.preview/apply לא
נבדקו עד אז.

### גל 25 — contentControls: דילוג לפי ההמלצה
`d.contentControls` ו-`d.customXml` קיימים. 54 פעולות לקהל שאינו
קהל התוסף — ההשקעה גרועה, כפי שהמדריך קובע.

## הסרגל — ארבעה ממצאים, ומה נגזר מהם

נמדד ב-Chrome headless על ה-`dist` הארוז, עם קליק אמיתי בתוך הפסקה
(`Input.dispatchMouseEvent`) והקלדה אמיתית (`Input.insertText`), ואחר כך
מדידה של המלבנים שהמנוע צייר בפועל.

**נמדדו מחדש על superdoc 2.15.0-next.15 (15.9.2026) — ארבעתם פתוחים, ללא
שינוי.** ‏`w:ind`: בפסקה עברית עם שוליים של 96px, `left: 1440` מזיז את קצה
ההתחלה ל-192px ו-`right` מכניס את קצה הסוף — לוגי ונכון; `firstLine: 1440`
מציב את תחילת השורה הראשונה ב-0px, כלומר החוצה; ו-`left: 1440` עם
`hanging: 720` נותן 240px לשורה הראשונה מול 192px לשאר — עמוק יותר, כלומר
הפוך. ערך שלילי ב-`firstLine` ושדה `start` נדחים בוולידציה של המנוע.
`doc.get()`: אחרי `setTabStop` ו-`setKeepOptions` — שתיהן `success`,
ו**הייצוא אכן נושא** `keepNext`, `keepLines` ו-`w:tabs` — התכונות שחוזרות
הן `indent` ו-`bidi` בלבד; גם פסקה שהגיעה מ-XML עם שלושתם חוזרת בלי
ה-`tabs` וה-keep. כלומר הפער הוא **בקריאה בלבד**, והכתיבה שלמה.

**ומה שנוסף למדידה הזאת, כדי שלא ינוסה שוב:** תצלום ה-`pageMetrics` נושא
היום לכל עמוד גם `viewport` עם `xPx`/`yPx`/`widthPx`/`heightPx`. **הגודל**
אמין ועוקב אחרי הזום (397 / 794 / 1191px ב-50% / 100% / 150%), אבל
`xPx`/`yPx` נשארים 0 בעוד העמוד ב-DOM זז (494 / 296 / 97px באותם זומים) —
כלומר **המיקום עדיין אינו מושג משם**, והקריאה של `data-page-index` נשארת
נחוצה. ‏`ui.viewport` עצמו עדיין חושף `getRect`/`getHost`/`entityAt`/
`contextAt`/`observe`/`scrollIntoView` בלבד, ו-`getRect` פותר יעדי טקסט
וישויות — לא עמוד. (‏`npm run check:ruler` אינו נוגע בכל אלה: הוא מודד חיתוך
CSS של תוויות הסרגל על עמוד סינתטי ואינו טוען את המנוע כלל.)

### `w:ind` — `left`/`right` ממופים לוגית, `firstLine`/`hanging` לא

בפסקה עברית (`bidi: true`), שוליים של 2.54 ס"מ:

    setIndentation({left: 1440})   → הקצה הימני של הטקסט מ-96px ל-192px  ✔ צד ההתחלה
    setIndentation({right: 1440})  → הקצה השמאלי נכנס פנימה               ✔ צד הסוף
    setIndentation({firstLine: 1440}) → הקצה הימני של השורה הראשונה מ-96px
                                        ל-**0** — כלומר החוצה, אל תוך השוליים
    setIndentation({left:1440, hanging:720}) → השורה הראשונה **עמוק יותר**
                                        מהשאר (240px מול 192px)

כלומר `left`/`right` מתנהגים כמו `w:start`/`w:end` של OOXML — הצד הלוגי —
ואילו שני האחרים מצוירים בסמנטיקה פיזית, כלומר הפוכה. „כניסת שורה ראשונה”
של Word בפסקה עברית אינה ניתנת להשגה: `firstLine` מצייר החוצה, ולערך שלילי
המנוע עונה „must be a non-negative integer”.

**מה נגזר:** הסרגל מציג שני סמני כניסה — התחלה וסוף — ואינו מציג את סמן
השורה הראשונה ואת הסמן התלוי. סמן שגורר ערך שמצויר הפוך גרוע מסמן שאינו
קיים. ראו engine/page-ruler.ts.

### `doc.get()` אינו מחזיר את עצירות הטאב

`format.paragraph.setTabStop({position:2880, alignment:'right', leader:'dot'})`
מחזיר `success: true`, אבל תכונות הפסקה שחוזרות מ-`doc.get()` הן
`{ indent: {...}, spacing: {...}, bidi: true }` — בלי `tabs`, וגם בלי
`keepWithNext`/`keepLines`/`widowControl` אחרי `setKeepOptions` מוצלח.

**מה נגזר:** אין לסרגל דרך לצייר את העצירות הקיימות, ולכן אין בו עצירות
טאב בכלל. סרגל שמראה רק את מה שנוסף בו עצמו, ומעלים את מה שהגיע מקובץ
Word, מטעה יותר משהוא עוזר. אותו ממצא הוא גם הסיבה ש-`readParagraphFormat`
מחזיר `tabs: []` ו-`keepNext: false` על מסמך שיש בו את שניהם.

### מודל הפסקה: `paragraphIds.paraId` ו-`indent`

הצומת שחוזר מ-`doc.get()` הוא

    { kind: 'paragraph', paragraphIds: { paraId: '41964671' },
      paragraph: { inlines: [...], props: { indent: {...}, bidi: true } } }

— **בלי `id`**, ועם `indent` ולא `indentation`. הקוד שחיפש `node.id` ואת
`props.indentation` החזיר אפסים על כל מסמך, ודיאלוג „פסקה” שנפתח עליהם
ואושר מחק כניסות שהגיעו מ-Word (`setIndentation` מחליף את `<w:ind>` כולו).
תוקן בגל הזה; `paraId` הוא בדיוק ה-`blockId` שהבחירה מחזירה.

### מלבן העמוד אינו ניתן לחישוב מבחוץ

זום מיושם ב-`width: 100/zoom%` + `transform: scale(zoom)` עם
`transform-origin: top left`, ולכן העמוד ממורכז בתוך **תיבת הפריסה של
ה-wrapper** ולא בתוך מיכל הגלילה. „רוחב עמוד כפול זום, ממורכז במיכל” נמדד
כשגוי בכל זום שאינו 100% (ב-50% העמוד נמצא ב-‎-625px, והנוסחה נותנת 176px).
אין API ציבורי שמחזיר את המלבן.

**מה נגזר:** הסרגל מודד את המלבן דרך `ui.viewport.getHost()` ותכונת
`data-page-index` שהמנוע מסמן בה עמוד. זו חריגה מתועדת מגבול ה-DOM, והיא
נשמרת צרה בשני שערים: tests/unit/engine-boundaries.test.ts מוודא שרק
engine/page-ruler.ts נוגע בעיגון ושהוא קורא בלבד, ו-
tests/contract/engine-page-hooks.test.ts מוודא שהעיגון עדיין קיים באריזה.

## הסרגל האנכי — שני ממצאים נוספים

נמדד באותה שיטה: ה-`dist` הארוז ב-Chrome, קליק והקלדה אמיתיים, ואז סריקת
ערכים דרך `sections.setPageMargins` ומדידת מה שצויר.

### שולי הטקסט מורמים בשקט כשיש כותרת עליונה

A4, כותרת עליונה **ריקה**, `headerDistance` של חצי אינץ':

| `top` שנשלח | ראש הטקסט שצויר |
|---|---|
| 0.75" | 72px |
| 0.7"  | 67.2px |
| 0.6"  | 66.4px |
| 0.5"  | 66.4px |
| 0.25" | 66.4px |
| 0"    | 66.4px |

כלומר מתחת ל-66.4px הטקסט **אינו זז**, וכל גרירה נוספת אינה עושה דבר.
66.4 = 48 (מרחק הכותרת) + 18.4 (גובה שורה ריקה אחת). עם `headerDistance`
של אינץ' הרצפה עוברת ל-114.4px — כלומר הנוסחה היא
`max(topMargin, headerDistance + גובה הכותרת)`, וזו גם הנוסחה שמופיעה
במקור של מנוע הפריסה (`layout-engine/src/section-breaks.d.ts`, ובאריזה
`X=(io,ia)=>O>0?Math.max(ia,io+O):ia`). ההתנהגות עצמה תואמת ל-Word.

**נמדד מחדש על 2.15.0-next.15 (15.9.2026) — פתוח, והנוסחה זהה. ומה
שהתחדד: ‏66.4 אינו מספר של המנוע אלא של הפיקסטורה.** באותה סריקה בדיוק, עם
כותרת ריקה ומרחק כותרת של חצי אינץ' אבל בגופן ברירת מחדל אחר, ראש הטקסט
יצא 96 / 72 / 67.2px ל-1 / 0.75 / 0.7 אינץ', ומ-0.6 ומטה הוא נעצר על
**64.3px** — כלומר 48 (מרחק הכותרת) + 16.3 (גובה השורה הריקה בגופן שנפתר
שם), בעוד שבמדידה הראשונה אותו איבר שני היה 18.4. הרצפה היא תוצאה של
הנוסחה, ולא קבוע שאפשר לקודד. ‏`pageMetrics` ממשיך לדווח בדיוק את הערך
שצויר (64.25), ו-`sections.list` ממשיך לדווח את מה שנכתב (0.6 … 0).

**מה שאינו תואם ל-Word** הוא שקריאת המסמך אינה מגלה את זה: `sections.list()`
מחזיר את `w:top` כפי שנכתב, ולכן פקד שנשען עליו מצייר שוליים שאין מתחתיהם
טקסט. מה שכן מגלה: **`activeEditor.pageMetrics.getSnapshot()`**, שמחזיר
ב-`pages[0].base.marginTopPx` את הערך **אחרי** ההרמה (66.4 גם כשבמסמך כתוב 0).

**מה נגזר:** הסרגל האנכי מצייר ומגביל לפי `pageMetrics` ולא לפי המסמך, והידית
נעצרת על הרצפה עם הסבר ב-`title`. המימוש ב-`readEffectiveMargins`
(engine/page-setup.ts), והשער באריזה ב-tests/contract/engine-page-hooks.test.ts —
`pageMetrics` אינו מופיע ב-`.d.ts` של `superdoc`, בדיוק כמו `data-page-index`.

### שוליים בלי מקום לטקסט מקפיאים את המסמך — **בלי דרך חזרה**

A4 (גובה 1122.53px), שוליים תחתונים של אינץ'. כך זה נמדד בפעם הראשונה:

| `top` | גובה הטקסט שנשאר | מה שצויר |
|---|---|---|
| 10.29" | 38.7px | 8 עמודים |
| 10.60" | 8.9px  | 4 עמודים |
| 10.68" | 1.3px  | 4 עמודים |
| **10.70"** | **‎-0.7px** | **אפס עמודים** |

ומרגע הקריסה זה נשאר כך: `setPageMargins({top: 1})` מחזיר `success: true`,
`setPageMargins({bottom: 0.9})` אחריו גם — והמסך נשאר ריק, `pageMetrics` קפוא
על התצלום האחרון, ו-`[data-page-index]` אינו קיים. כלומר המסמך נעלם ואינו חוזר
עד טעינה מחדש. אין קבלת כשל, אין אזהרה, ואין הודעה בקונסולה.

**ההתנהגות השתנתה ב-2.15.0-next.15, והפער לא נסגר — נמדד 15.9.2026, שתי
ריצות זהות** (הקריאה נשלחה ישירות למנוע, כלומר בלי החסם שלנו במסלול).
המסך כבר אינו מתרוקן: ב-10.70" הקבלה עדיין `success: true`, אבל המנוע זועק
עכשיו פעמיים דרך `onError` — „layoutDocument: non-positive content area”
ואחריה „canonical render failed 2 consecutive times” — ו**הפריסה האחרונה
שהצליחה נשארת על המסך** (שמונה עמודים, ראש טקסט ב-1025px) במקום אפס
עמודים. משם ואילך שום דבר אינו מצויר מחדש: `top: 1` ואחריו `bottom: 0.9`
מחזירים הצלחה ו-`sections.list` מראה 1 / 0.9, `doc.insert` מחזיר הצלחה —
והטקסט שהוכנס אינו מופיע, גם אחרי חמש שניות, ו-`pageMetrics` קפוא. בקרה
באותה ריצה: 10.29" נותן חמישה עמודים, וחזרה ל-1" מחזירה עמוד אחד עם טקסט
ב-96px — כלומר הקריסה שייכת בדיוק לאזור תוכן שאינו חיובי.

כלומר מה שהמשתמש רואה היום הוא **מסמך שנראה תקין ומפסיק לשקף עריכות**, עד
טעינה מחדש. זה קשה **יותר** לזיהוי ממסך ריק.

**מה נגזר:** `applyPageMargins` דוחה שוליים שאינם משאירים חצי אינץ' לטקסט,
לפני שהוא נוגע במנוע — בשכבת המנוע ולא בפקד, כדי שכל כותב עתידי יעבור דרך
החסם. הסרגל מגביל את הגרירה לאותו ערך. חצי אינץ' ולא ס"מ: ס"מ (37.8px) עבר
במדידה, אבל מרווח של פחות מפיקסל מקריסה בלתי הפיכה אינו חסם אלא צירוף מקרים.
מסמך שכבר מגיע חנוק אינו ננעל — שינוי שמשפר את המצב מותר, אחרת החסם היה מונע
דווקא את התיקון.

**ומה שנוסף עכשיו כאפשרות, ולא נבנה:** ההתנהגות החדשה **מדידה** — „canonical
render failed” מגיעה ל-`onError` שלנו, שהיום רק כותב לקונסולה. אם אי פעם
נרצה לומר למשתמש „המסמך קפא, יש לטעון מחדש”, זו הנקודה שבה יש סימן אמיתי
לתלות אותה עליו.

## בחירה בעכבר — מה שנמדד

שלוש התנהגויות של עכבר נמדדו מול המנוע ב-Chrome אמיתי, גם על המנוע לבדו
וגם על התוסף הארוז מ-`file://` עם DOCX עברי מנוקד. שתיים מהן תקינות, ואחת
שבורה בדיוק במקום שכואב לאוצריא.

### לחיצה כפולה — הניקוד והטעמים נסגרו, הגרש והגרשיים לא

אותו משפט, בכמה צורות, בלחיצה כפולה על אותה מילה — **והטבלה מעודכנת
למדידה של 15.9.2026 על המנוע לבדו** (2.15.0-next.15, ואותה תוצאה בדיוק על
2.14.0-next.5; השכבה שלנו נוטרלה בזמן ריצה: מאזין חלון מחזיר לאירוע את
הקואורדינטות המקוריות ומסמן אותו בדגל המסונתז, כך ששכבת המילה מדלגת עליו):

| הטקסט | המנוע ב-2.12.0 | המנוע ב-2.14.0-next.5 וב-2.15.0-next.15 |
|---|---|---|
| `שלום עולם גדול` | `עולם` — תקין | תקין |
| `שלום, עולם! (גדול)` | `עולם` — תקין | תקין |
| `בראשית ברא אלהים` **מנוקד** | `ר` — אות בודדת | **המילה כולה** — 6 מתוך 6 יחידות קוד |
| אותו טקסט **מנוקד ומוטעם** | `ר` — אות בודדת | **המילה כולה** — 7 מתוך 7 |
| `רמב״ם` | `רמב` — נשבר לפני הגרשיים | `רמב` — **עדיין נשבר**, 4 מתוך 6 |

כלומר סימני הניקוד והטעמים נספרו אצל המנוע כמפרידי מילה, וזה **נסגר**
במנוע לפני 2.15. מה שנשאר הוא הגרש והגרשיים — כלומר בדיוק ראשי התיבות
שמסמכי אוצריא עשויים מהם. עם השכבה שלנו כל חמשת המקרים מדויקים, והיא
no-op במקומות שבהם המנוע כבר צודק.

**נעקף בשכבה מבחוץ,** `src/engine/word-selection.ts`, בלי לגעת במנוע: הזרע
הוא מה שהמנוע בחר (`doc.selection.current`), החלון סביבו נקרא ב-
`doc.ranges.resolve`, גבול המילה מחושב אצלנו, והבחירה נקבעת ב-
`ui.selection.apply` — שלוש פעולות ציבוריות. נמדד ב-8ms על פסקה בת 3,899
תווים.

שני גבולות של `ranges.resolve` שנמדדו ומכתיבים את המימוש:
`preview.text` נחתך ב-200 תווים ומסמן `truncated`, ולכן החלון הוא ±90 תווים
ולא הפסקה כולה; והיסט שחורג מאורך הבלוק **נחתך** לאורכו, וזה מה שמעיד
שהחלון הגיע לסוף הבלוק.

`query.match` עם regex בתוך הבלוק היה מסלול חלופי שעובד (מחזיר את המילים
עם הטווחים), אך הוא דורש `within` עם `nodeType` — `{kind:'block', nodeId}`
לבדו נדחה ב-`within-node-type-unsupported` — כלומר קריאה נוספת ל-
`getNodeById`, ו-150ms על אותה פסקה. לא נבחר.

### שלוש לחיצות — תקין במנוע, ובכל זאת נשלח גם מאיתנו

במנוע לבדו שלוש לחיצות בוחרות את הפסקה כולה (נמדד: 0..519 על פסקה בת 519
תווים), גם עם שכבת המילה מותקנת וגם בלעדיה. **נמדד מחדש על 2.15.0-next.15
(15.9.2026): תקין כשהיה** — 0..1340 על פסקה בת 1,340 תווים, בבלוק אחד,
ובדיוק אותו דבר דרך התוסף. אף על פי כן `installWordSelection`
סופר לחיצות בעצמו ושולח בחירת פסקה בלחיצה השלישית — ראו הסעיף הבא.

### ספירת הלחיצות אינה בידי הדף, והיא שונה בין הסביבות

זה הפער היחיד כאן שאינו של מנוע ה-DOCX אלא של המארח, והוא מסביר למה בדיקה
ב-Chrome אינה עדות למה שקורה באוצריא. **ומאותה סיבה הוא גם לא נמדד בסבב
2.15.0-next.15, ולא ייתכן שיימדד כאן:** גרסת המנוע אינה נוגעת בו כלל —
הוא נקבע בהעברת אירועי העכבר של אוצריא, ומדידה שלו היא באוצריא עצמה.

אוצריא ב-Windows מריצה את התוסף ב-WebView2 במצב **visual hosting**, ושם כל
אירוע עכבר מועבר ידנית מ-Flutter דרך `SendMouseInput`. מה שנשלח בפועל
(`flutter_inappwebview_windows`, הפורק של אוצריא, `resolved-ref c0c43c42d`):

    MOVE · LEFT/RIGHT/MIDDLE_BUTTON_DOWN · ..._UP · LEAVE · WHEEL

`COREWEBVIEW2_MOUSE_EVENT_KIND_LEFT_BUTTON_DOUBLE_CLICK` **אינו קיים בחבילה
כלל** — לא ב-C++ (`windows/in_app_webview/in_app_webview.cpp`,
`setPointerButtonState`) ולא ב-Dart (`InAppWebViewPointerEventKind` מכיל
`activate, down, enter, leave, up, update, cancel`). ערכי ה-enum של WebView2
הם הודעות Win32, ו-`DOUBLE_CLICK` הוא `WM_LBUTTONDBLCLK`; מה שמציל את המצב
הוא ש-Chromium מחשב `clickCount` בעצמו מזמן וממרחק, ולכן לחיצה כפולה כן
מגיעה לדף. אבל זה חישוב של המארח, לא חוזה של הדף.

**מה נגזר:** השכבה אינה מאזינה ל-`dblclick` אלא סופרת `mousedown`/`click`
בעצמה, באותם ספים (500ms — ברירת המחדל של Windows — ו-5px). כשהמנוע צודק
התוצאה זהה ולא נשלחת בחירה שנייה; כשהוא לא, התוצאה עדיין נכונה.

### גרירה שיורדת מתחת לשורה האחרונה מאפסת את הבחירה — **נסגר במנוע, וההצמדה נשארת**

**נסגר — נמדד על 2.15.0-next.15 (15.9.2026), שתי ריצות, ואותה תוצאה על
2.14.0-next.5.** במנוע לבדו הגרירה כבר אינה קורסת: הראש גדל עם הגרירה,
וברגע שהסמן נמצא מעל `DIV.superdoc-page` מתחת לשורה האחרונה הוא **נהדק
לסוף הפסקה** (1340 מתוך 1340) ונשאר שם בכל צעד, עד 226px מתחת לשורה
האחרונה — בלי שום התכווצות. גם הגרירה במסמך של גשש ההצמדה לא חזרה לתחילת
הבלוק. המדידה שלמטה — הקריסה ל-1 ול-0 — היא של 2.12.0 ומטה, והיא נשארת
כאן מפני שהיא מסבירה את הרצועה המתה ואת הסעיף הבא, שעדיין פתוח.

**ובכל זאת ההצמדה נשארת, משתי סיבות שנמדדו:** היא משותפת למסלול הלחיצה
שבסעיף הבא, שלא נסגר; והיא נותנת את התוצאה של Word ולא את זו של המנוע —
עם ההצמדה הראש עוקב אחרי ה-x של הסמן על השורה האחרונה (1300), בעוד המנוע
לבדו קופץ לסוף הפסקה.

גרירה כלפי מטה, בצעדים של 30px, על פסקה אחת ארוכה (4,199 תווים, מילים
ייחודיות) בחלון 1400x1200. בכל צעד נדגמו ארבעה דברים באותה נקודה: מה
`document.elementFromPoint` מחזיר, לאן `document.caretRangeFromPoint` ממפה,
מה `getSelection()` מחזיק, ומה `doc.selection.current()` מדווח.

    y=284..1004  → 0 → 3,960 תווים, גדל ב-180 לכל 30px   תחת הסמן: SPAN.superdoc-text-run
    y=1034       → 4,139                                  תחת הסמן: DIV.superdoc-line
    y=1064       → 1        ← קריסה                       תחת הסמן: DIV.superdoc-page
    y=1094..1154 → 0–1                                    תחת הסמן: DIV.superdoc-page
    y=1184       → 1                                      תחת הסמן: FOOTER (מחוץ לעמוד)

**הגורם:** הקריסה מתרחשת בדיוק בצעד שבו הסמן חדל להיות מעל תיבת שורה. עמוד
הוא מלבן A4 בגודל קבוע, והטקסט כמעט אף פעם אינו ממלא אותו — לכן בין השורה
האחרונה לתחתית העמוד יש רצועה מתה שהיא `DIV.superdoc-page` ותו לא. מיפוי
הקואורדינטה למיקום במסמך של המנוע אינו מהדק (clamp) את הנקודה לשורה הקרובה
ברצועה הזאת, ולכן הגרירה מקבלת מיקום ליד תחילת הבלוק והבחירה מתאפסת.

זו אינה תקלה של הדפדפן: `document.caretRangeFromPoint` באותן קואורדינטות
בדיוק המשיך להחזיר את המיקום הנכון והמהודק — צומת הטקסט האחרון, היסט 59 —
בכל אחד מהצעדים שאחרי הקריסה. המנוע גם אינו מניח את הבחירה על ה-DOM כלל:
`getSelection()` היה ריק לאורך כל הגרירה, כלומר הוא מצייר שכבת בחירה משלו
ומחזיק את המיקום בעצמו, ולכן אינו יורש את ההידוק של הדפדפן בחינם.

**בקרה שמאשרת:** אותה מדידה בדיוק, עם פסקה קצרה שנוספה אחרי הארוכה. הקריסה
זזה יחד עם הטקסט — ב-y=1064 וב-y=1094 הבחירה החזיקה (4,175 תווים) מפני
שהסמן היה מעל שורות הפסקה השנייה, והקריסה הופיעה רק ב-y=1124, מתחת לשורה
האחרונה שלה. זה גם מסביר למה במסמך של 31 פסקאות קצרות הגרירה הגיעה עד סוף
המסמך: הטקסט מילא את העמוד, ולא נותרה רצועה מתה.

**ו-„91" מהמדידה הראשונה אינו קבוע קסם:** הבחירה גדלה ב-180 תווים לכל 30px
בגובה שורה של 17px, כלומר שורה בקובץ הזה היא כ-90 תווים. מה שנשאר אחרי
הקריסה הוא שווה-ערך לשורה אחת.

**נקודה שכדאי לדעת:** ברגע שהסמן יוצא מהעמוד לגמרי (y≥1184, מעל שורת המצב)
המנוע מפסיק לעדכן והבחירה נשארת בערך האחרון התקין. כלומר גרירה *רחוקה*
מטה בטוחה; מה שהורס היא דווקא הרצועה המתה שבתוך העמוד.

**וגורם שני, במארח, שאינו זהה לזה:** `custom_platform_view.dart` שולח
`LEAVE` מתוך `MouseRegion.onExit` **בלי לבדוק אם כפתור לחוץ**, ועם
`virtualKeys = NONE`. כלומר ברגע שהסמן יוצא מגבולות ה-WebView באמצע גרירה,
Chromium מקבל „העכבר עזב, בלי כפתורים" ומסיים את בחירת הגרירה. זה מסלול נפרד
מהקריסה שנמדדה למעלה. תוקן ב-Otzaria/flutter_inappwebview#11.

**נעקף ב-`engine/pointer-snap.ts`, ולא בחסימה אלא בהצמדה.** התנאי המדויק —
„הסמן בתוך העמוד ולא מעל `.superdoc-line`" — הוא אותו תנאי בדיוק של הסעיף
הבא (לחיצה מחוץ לגליפים), והעקיפה משותפת: בזמן שהכפתור לחוץ, כל אירוע תנועה
ושחרור מקבל את הקואורדינטות של הנקודה הקרובה על שורת הגוף הקרובה, ולכן
הגרירה ברצועה המתה **ממשיכה עד סוף הטקסט** כמו ב-Word, במקום להיעצר על
הערך האחרון. מה שנמדד לפני כן ונדרש כאן: המנוע מאזין ל-`pointerdown`
ו-`mousedown` על ה-host שלו, ול-`pointermove`/`pointerup` על `window` ב-capture
שהוא רושם ברגע הלחיצה; הוא קורא `clientX`/`clientY` מהאירוע, ולכן מאזין
שנרשם על `window` **לפני בניית המופע** ומחליף אותם על האירוע עצמו קודם לו.

### לחיצה מחוץ לגליפים — הסמן קופץ לתחילת הטקסט — **נעקף**

נמדד ב-`scripts/qa/click-snap-qa.mjs` (superdoc 2.11.0, docx-engine 0.10.0):
מסמך של שלוש פסקאות — עברית ארוכה, ריקה, עברית קצרה — ולחיצה (press, 40ms,
release) בנקודות שאינן על גליף:

| הלחיצה | המנוע ב-2.11.0 | המנוע ב-2.15.0-next.15 | Word |
|---|---|---|---|
| משמאל לסוף השורה העברית, על השורה | היסט 0 — **תחילת** השורה | היסט 0 — **ללא שינוי** | סוף השורה |
| בשוליים הימניים, ליד השורה | היסט 0 | היסט 0 — נכון במקרה הזה | תחילת השורה |
| מתחת לשורה האחרונה, בכל x | **הפסקה הראשונה**, היסט 0 | **תלוי מרחק** — ראו למטה | סוף השורה האחרונה |
| בשוליים העליונים | הפסקה הראשונה, היסט 0 | **השורה הראשונה באותו x — תקין** | השורה הראשונה, באותו x |
| רחוק מהגליף, על שורה ריקה | — | **אותה שורה — תקין** | אותה שורה |

**„תלוי מרחק”, במדידה של 15.9.2026 (שתי ריצות, שתי השהיות קריאה):** עד כ-150
פיקסלים מתחת לשורה האחרונה ומעל הגליפים שלה הסמן נוחת על השורה האחרונה
באותו x; מ-200 פיקסלים ומטה הוא **אינו זז כלל**; ומשמאל לגליפים של השורה
האחרונה הוא נוחת על השורה שמעליה (היסט 1287 במקום 1340 שהוא הסוף) — בכל
מרחק. במסמך הקצר של שלוש הפסקאות, 200 פיקסלים מתחת לטקסט גם הם לא הזיזו
את הסמן. כלומר „הפסקה הראשונה, היסט 0” נסגר, אבל התוצאה עדיין אינה של
Word — ארבעה מתוך שמונה מצבי הגשש שבורים על המנוע לבדו.

**מה נשאר לגורם:** חלק מהנפילה-לאחור תוקן במנוע (השוליים העליונים והשורה
הריקה נוחתים היום נכון), ומה שלא תוקן הוא ההמרה של x שמחוץ לגליפים בשורה
עברית, והרצועה המתה שמתחת לשורה האחרונה. השער `click-snap-qa.mjs` נשאר ירוק
מפני שהוא מודד את העקיפה; כדי שהטבלה הזאת תימדד בשער ולא בגשש חד-פעמי, הוא
צריך מצב „מנוע לבדו”.

**הגורם**, בקוד המנוע (`clickToPositionDom`): המיפוי עובר ב-`elementsFromPoint`.
כשנמצאה שורה (`.superdoc-line` — והיא רחבה כרוחב הטור, לא כרוחב הטקסט),
ה-x מומר להיסט; מחוץ לגליפים ההמרה מחזירה 0 במקום להדק לקצה. כשלא נמצא
פרגמנט בכלל (הרצועה המתה, השוליים), הנפילה-לאחור היא **הפרגמנט הראשון של
העמוד** — לא הקרוב. אותו מסלול בדיוק הוא שמאפס את הגרירה שבסעיף הקודם.

**העקיפה** — `engine/pointer-snap.ts`, והגיאומטריה ב-`measurePageGlyphs` של
`engine/page-ruler.ts`: מאזין על `window` ב-capture, שנרשם לפני בניית המופע,
מחליף על האירוע עצמו (`Object.defineProperty` על `clientX`/`clientY`, שמאפיל
על ה-getter של `MouseEvent.prototype`) את הנקודה בנקודה הקרובה על השורה
הקרובה — לפי מלבני הגליפים של צומתי הטקסט, בהיקף שנגזר ממטרת האירוע (השורה,
התא, או עמוד שלם ברצועה המתה, ואז שורות הגוף בלבד). המנוע, וגם
`word-selection.ts` ו-`format-painter.ts` שקוראים אחריו, רואים את אותה נקודה;
אין אירוע מסונתז ואין `stopPropagation`. תמונות וידיות אינן מוצמדות — לחיצה
עליהן היא בחירת אובייקט.

**מה שלא נמדד:** לחיצה כפולה באזור כותרת עליונה שפתוחה לעריכה. ההצמדה אינה
נוגעת ב-`dblclick`, והמנוע נכנס לכותרת מהאירוע הזה בקואורדינטות המקוריות.

### הלחצן הימני — המנוע מתעלם ממנו

נמדד ב-`scripts/context-menu-probe.mjs`, Chrome אמיתי על ה-dist הארוז, מסמך של
24 פסקאות.

| מה נבדק | התוצאה |
|---|---|
| `contextmenu` נורה בדף | כן — גם ב-capture וגם ב-bubble, ואינו מגיע מבוטל |
| לחיצה ימנית **בתוך** בחירה קיימת | הבחירה נשמרת בשלמותה |
| לחיצה ימנית **מחוץ** לבחירה | **הסמן אינו זז** |
| בקרה: לחיצה שמאלית באותה נקודה | הסמן זז (בלוק אחר, סמן מכווץ) |
| `preventDefault` על `pointerdown` ימני | `contextmenu` עדיין נורה |

**נמדד מחדש על 2.15.0-next.15 (15.9.2026), `npm run check:menu` — פתוח, כל
שורות הטבלה משחזרות.**

הבקרה היא מה שהופך את זה לממצא. במדידה הראשונה גם הלחיצה השמאלית לא הזיזה —
מפני שהנקודה שנבחרה כלל לא הייתה על טקסט. בלי בקרה הייתה נרשמת כאן מסקנה על
המנוע שהיא בעצם באג במדידה.

**מה שמאפשר לעקוף מבחוץ**, ושניהם נמדדו באותה ריצה:

- `ui.selection` חושף `getRects()` ו-`getAnchorRect()`, **ורק השני שימושי
  כאן.** נמדד, וגם נמדד מחדש על 2.15.0-next.15 (15.9.2026): על בחירה חיה בת
  61 תווים ש-`doc.selection.current()` מדווח אותה במלואה, `getRects()` מחזיר
  **אפס מלבנים** ו-`getSnapshot().empty` הוא `true`; `getAnchorRect()` כן
  מחזיר נקודה. לכן „האם הנקודה שנלחצה בתוך הבחירה” אינו נענה מהגיאומטריה
  הזאת, ו-`use-context-menu.ts` אינו נשען עליה — אבל ל-`Shift+F10` יש עוגן
  אמיתי, וזה מה שמאפשר לתפריט להיפתח במקום הנכון.
- לחיצה שמאלית **מסונתזת** (`pointerdown`/`mousedown`/`pointerup`/`mouseup`/
  `click` על `elementFromPoint`) כן מזיזה את הסמן.

`composables/use-context-menu.ts` מרכיב את השניים להתנהגות של Word: לחיצה ימנית
בתוך הבחירה משאירה אותה, ומחוצה לה מזיזה את הסמן. הלחיצה המסונתזת מסומנת
ב-`markSyntheticPointer` כדי שמונה הלחיצות של `word-selection.ts` לא יספור
אותה — אחרת שתי לחיצות ימניות באותה נקודה היו נחשבות ללחיצה כפולה.

**שני דברים נוספים שנמדדו שם, ואינם על הלחצן הימני:**

- **הבחירה שורדת מעבר מיקוד** לפקד של הממשק. זה מה שמתיר לתפריט ההקשר לקבל
  מיקוד — כלומר ניווט מקלדת אמיתי — במקום להעתיק את `@pointerdown.prevent`
  של הרצועה, שהיה הופך אותו לתפריט שאי אפשר לנווט בו.
- **`story` הוא אובייקט קריא**: `{"kind":"story","storyType":"body"}`.
- **`hyperlinks.list` מחזיר מיקום** — `paraId`, `hyperlinkNodeId`, `address`,
  `text`, `externalTarget`, `targetKind`. העטיפה ב-`hyperlinks-manage.ts`
  משטחת את זה ל-`{id,href,anchor}`; זיהוי „קישור תחת הסמן” אפשרי, והוא דורש
  להרחיב את העטיפה ולא לעקוף אותה.

**מה שאינו נמדד כאן, ובמכוון:** האם `preventDefault` מבטל את תפריט ברירת
המחדל של WebView2, והאם `contextmenu` מסונתז שם בכלל. השער רץ ב-Chrome
headless, שאין בו תפריט נייטיב להדגים עליו; אוצריא מעבירה כל אירוע עכבר ידנית
דרך `SendMouseInput`, ולכן זה נבדק באוצריא עצמה עם DevTools.

## שני טורים — סדר הטורים נסגר ב-2.12.0, ובחירת המקלדת נסגרה אחריו

**הפער נסגר במנוע.** ECMA-376 §17.6.1 קובע ש-`w:bidi` ב-`sectPr` הוא שמכריע
את הכרום של המקטע — מספרי עמודים, שוליים פנימיים, ו**סדר הטורים** — כלומר
שבמקטע עברי הטור הראשון שייך לצד **ימין**. עד superdoc 2.11.0 המנוע מילא
שמאל→ימין בכל מקרה. התיקון נכנס כ-`SD-4764` ונכלל ב-**superdoc 2.12.0**
(‏`@superdoc/docx-engine` 0.11.0).

נמדד מחדש ב-Chrome אמיתי מול ה-`dist` הארוז, על 2.12.0 —
`scripts/qa/column-selection-probe.mjs`. הגשש מודד **את שני הכיוונים** על אותו
מסמך, וזו השורה שמכריעה מה שייך ל-`w:bidi` ומה לטורים בכלל. **הטבלה הורצה
שוב על 2.15.0-next.15 (15.9.2026, שתי ריצות זהות): שלוש השורות הראשונות
חוזרות במספרים זהים, והרביעית — הקשת המקלדת — השתנתה, ולכן היא מופיעה כאן
בשתי גרסאות:**

| מה נמדד | מקטע עברי (`w:bidi`) | בקרה: אותו מסמך, מקטע לועזי |
|---|---|---|
| הייצוא — `sectPr` אחרי „עמודות ← שתיים” | **נכון**: `w:bidi` ולצדו `<w:cols w:space="720" w:num="2" w:equalWidth="1"/>` | — |
| שורה 01 של המסמך | בטור ה**ימני** (x=862, אמצע הדף 700) — כמו בוורד | בטור ה**שמאלי** (x=538) |
| גרירה מהטור הראשון אל השני | **מדויקת**: 9..37, רצף אחד, בדיוק הטווח שנגרר | **מדויקת**: אותו 9..37 |
| `Shift+חץ מטה` בגבול שבין הטורים (2.12.0) | **גולש לתחילת המסמך**: עוגן שאומת בשורה 23, שש הקשות מטה → 4..23 | **זהה**: 4..23 |
| אותה הקשה, **2.15.0-next.15** | **חוצה את הגבול**: שש הקשות → 24..29 | **זהה**: 24..29 |

שתי מסקנות, ושתיהן נמדדו ולא הונחו:

1. **סדר הציור והבחירה תקין עכשיו בשני הכיוונים.** הגרירה בוחרת בדיוק את
   הטווח שנגרר, ובעברית מחוות הקריאה (ימין ואז שמאל) ומחוות סדר-המסמך
   התלכדו — עד 2.11.0 הן היו הפוכות זו לזו, וזה מה שהפך את הבחירה סביב העוגן.
2. **מה שהיה שבור במקלדת נסגר מאז — ומעולם לא היה פער של RTL.** ב-2.12.0
   ‏`Shift+חץ מטה` בגבול שבין הטורים נתן **בדיוק את אותה תוצאה** במקטע לועזי
   ובמקטע עברי — הראש עלה 19 שורות אחורה במקום להגיע לראש הטור השני — ולכן
   לא היה ניתן לתלות אותו ב-`w:bidi`. **נמדד מחדש על 2.15.0-next.15
   (15.9.2026), שתי ריצות זהות: נסגר.** העוגן אומת בהקשה אחת, ושש הקשות מטה
   בוחרות 24..29 — כלומר הבחירה חוצה את גבול הטורים — **בשני הכיוונים**.
   אין לזה אישיו נפרד: גוף #3990 (שנפתח על 2.13.0) כבר אומר שהחצייה עובדת
   אחרי ‏#3952. הגשש נשאר כשער רגרסיה.

**מה הוסר אצלנו, ולמה.** ‏`rtlColumnNote` ב-`src/engine/page-setup.ts` הודיעה
למשתמש „העמודה הראשונה מצוירת בצד שמאל, וגם הסימון עובר שמאל→ימין”, והכרטיס
„ספר קודש — שני טורים” נשא „הטורים מצוירים הפוך בעורך; הקובץ נשמר נכון”.
שתיהן ירדו עם המעבר ל-2.12.0: הודעה שמתארת התנהגות שכבר אינה קיימת גרועה
מהיעדרה — היא מרתיעה מתבנית תקינה, ומלמדת את המשתמש לא להאמין לפס המצב.
מה שהחליף אותן הוא **שער**: הגשש דורש עכשיו שהטור הראשון ינחת בימין ושפס
המצב יישאר שקט, כך שרגרסיה בציור לא תגיע למשתמש בשקט (ה-docx המיוצא תקין
בשני המקרים, ולכן שום בדיקת XML לא הייתה תופסת אותה).

**מה שלא נמדד בסבב הזה:** „סמן ברוחב הטור הראשון מתחת לשורה האחרונה שבו”.
בפריסה הנוכחית שני הטורים מסתיימים קרוב מדי זה לזה, ואין רצועה שבה אפשר
להציב עכבר מתחת לאחד ומעל השני. הגשש מדווח את זה כ„לא נבדק” ולא כ„עובד” —
וכך גם בסבב של 15.9.2026, מאותה סיבה בדיוק.

**עבודה פתוחה במעלה הזרם.** ‏PR ‏#3953 (סדר הטורים) נסגר לטובת הפורט של הצוות
עצמו, ‏`SD-4764`, שהוא מה שנכלל ב-2.12.0. עדיין פתוחים
[#3962](https://github.com/superdoc/docx-editor/pull/3962) (מרזבים נמדדים,
וסדר עמוד מאוזן לפי טור) ו-[#3961](https://github.com/superdoc/docx-editor/pull/3961)
(רצועת הערות השוליים כטור משלה). הצוות מסר שהוא עובד על התנהגות הבחירה
והגרירה סביב הטורים, ו-`Shift+חץ מטה` אכן נסגר אצלם — כפי שנמדד למעלה. מה
שנשאר פתוח שם הוא **ציור** הבחירה, ולא המודל שלה: ראו הסעיף הבא.

## שני טורים — פס הבחירה מפגר אחרי הבחירה, ומכסה את המרזב

הפער הזה אינו במודל הבחירה אלא ב**ציור** שלה, ולכן הוא שרד את סגירת סדר
הטורים ואת סגירת `Shift+חץ מטה` שלמעלה. נמדד ב-Chrome אמיתי על ה-`dist`
הארוז, 2.15.0-next.15 (15.9.2026, שתי ריצות), ולשם השוואה גם על
2.14.0-next.5: מקטע עברי, שני טורים דרך התפריט האמיתי („פריסה ← עמודות ←
שתיים”), ופסקה אחת של כ-2,000 תווים שזורמת על שני הטורים. הגשש אינו במאגר —
הוא נכתב לסבב הזה, ודוגם בכל פריים את מספר המלבנים ששכבת הבחירה של המנוע
מציירת, לפי צד העמוד.

מה שנמדד:

* **המלבן המצויר חוצה את המרזב.** אחרי בחירה בשלוש לחיצות המודל תקין —
  הפסקה כולה, 1,979 תווים — אבל מתוך 34 המלבנים שצוירו, 21 הם מלבן אחד
  שמשתרע על **שני הטורים ועל הרווח שביניהם**, כלומר על שטח שאין בו טקסט
  נבחר בכלל.
* **הצבע הישן נשאר על המסך אחרי שהבחירה כבר השתנתה.** אחרי לחיצה חדשה בתוך
  הטור הראשון, הפריים האחרון שעדיין נושא את הצבע הדו-טורי הישן נמדד ב-130ms
  ובריצה השנייה ב-212ms (על 2.14.0-next.5: 106ms); רק אחריו הצבע מצטמצם
  ל-5 מלבנים בטור אחד. המצב הסופי תקין — 149 תווים, 5 מלבנים.
* **רגיש לעומס.** באחת הריצות נמדד פער של 127ms בין פריימים בדיוק בזמן
  שהצבע הישן היה על המסך, כלומר חוט ראשי עסוק מאריך את החלון. ה-issue מתאר
  על מסמך אמיתי גדול גם מצב ביניים **ריק** ומשך של כשנייה; בפיקסטורה הקטנה
  הזאת לא נדגם פריים בלי שום הדגשה, ולכן זה נרשם כאן כלא-משוחזר ולא כמופרך.

**אין מה לעקוף אצלנו, ואי אפשר גם לשמור על זה בשער קיים.** שכבת הבחירה
מצוירת בתוך המנוע ואין API שמבקש ציור מחדש; `ui.selection.getRects()` מדווח
את מלבני ה**מודל** (ובמדידת תפריט ההקשר הוא חוזר ריק לגמרי), ולכן גשש שנשען
עליו לא היה רואה את הפיגור הזה כלל. הדרך היחידה שנמצאה היא דגימת פריימים של
שכבת הציור עצמה.

דווח למעלה: [#3990](https://github.com/superdoc/docx-editor/issues/3990) —
פתוח, ונמדד פתוח על 2.15.0-next.15.

## תוכן עניינים — אין עצירת טאב, ולכן אין נקודות מוביל

### `toc.insert` אינו כותב `w:tabs`, ואף סגנון `TOC*` אינו מוגדר

שורת תוכן עניינים עברית צריכה להיקרא „כותרת......5”: הכותרת בימין, נקודות
מוביל, ומספר העמוד נדחף לשולי השורה. נמדד על superdoc 2.11.0 / docx-engine
0.10.0 — `scripts/qa/toc-direction-probe.mjs`:

- **הכיוון תקין, ומקור אחר ממה ששוער:** אין `<w:bidi/>` ב-`pPr` של פסקאות
  ה-TOC, אבל `sectPr` הוא `bidi` והכיוון נורש ממנו. הכותרת יושבת בימין
  (x=535) ומספר העמוד בשמאלה (x=497). `setDirection` על פסקת TOC כן כותב
  `<w:bidi/>`, ואינו משנה את הפריסה — כלומר הסדר שהיה נכון נשמר.
- **מה שחסר הוא עצירת הטאב:** `TOC1: tabs=[אין]`, הסגנונות `TOC1`/`TOC2`
  **אינם מוגדרים ב-`styles.xml`** כלל, ובגוף הריצות יש `<w:tab/>` בלי
  `w:leader` ובלי `w:pos`. שלוש השכבות שיכולות לשאת עצירה עם
  `w:leader="dot"` — `pPr`, סגנון, `w:defaultTabStop` — וכולן ריקות ממנה.
- **מה שרואים על המסך:** מספר העמוד יושב 38px מתחילת הכותרת ולא בשולי השורה
  ההפוכים, ובלי נקודות בדרך. תו טאב בלי עצירה נופל על `w:defaultTabStop` —
  קפיצה אחת, ובלי מוביל.

`toc.tabLeader` ו-`toc.rightAlignPageNumbers` מחזירים `success: true` ואינם
עושים דבר (מתועד ב-`src/engine/toc.ts`), ולכן הדרך היחידה היא לכתוב `w:tabs`
בעצמנו — או בסגנון `TOC*` שאנחנו מגדירים, או על הפסקאות.

**נעשה מאז:** `layOutTocRows` כותבת את העצירה לכל שורה, באותו `w:pos` בכל
הרמות כדי שה-DOCX יישאר תקין גם ב-Word. הפער אושרר מחדש על 2.13.0 **ושוב על
2.15.0-next.15 (15.9.2026)** ודווח
כ-[#3988](https://github.com/superdoc/docx-editor/issues/3988). הפירוט המלא
בסעיף „תוכן עניינים — שני פערים שהתגלו מדיווח משתמש” בראש הקובץ.

## מיקום הגלילה — snapshot יחיד לכל המנועים, נסגר ב-2.13.0

### שורש הגלילה נזכר במקום אחד, ושני מסמכים חיים דורסים זה את זה

**נסגר — נמדד על superdoc 2.15.0-next.15 (15.9.2026); כבר ב-2.13.0.** אותה
מדידה בדיוק: שני טאבים עם מנוע חי בכל אחד, שבע גלגלות אמיתיות (840px), מעבר
לטאב השני וחזרה, ואז גלגלת אחת. בשתי הריצות הדפדפן משחזר 840 בעצמו, הגלגלת
נוחתת על 940, ו**אין אף כתיבה ל-`scrollTop`** — לא של המנוע ולא שלנו (הלוגר
יושב על ה-setter ועל `scrollTo`/`scroll`, ואומת בכתיבת בקרה שהוא אכן רואה
כתיבות). אותה תוצאה על 2.13.0 ועל 2.14.0-next.5.

**והבקרה היא מה שהופך את זה לסגירה ולא להיעדר תצפית:** אותו גשש על 2.12.0
**כן** משחזר את הפער — אחרי החזרה לטאב המנוע מבקש 0 בזמן שהמיכל על 940,
השומר הישן מחזיר 840 מיד אחריו, והתוצאה היא ש**צעד הגלגלת של המשתמש נבלע**.

**מה נעשה אצלנו:** `guardPaneScroll` הוסר — מ-`src/sessions/pane-scroll.ts`
ומ-`src/App.vue`, יחד עם בדיקות היחידה שלו (קומיט `dc7b620` בענף הזה). הוא
הגן מפני כתיבה שאינה קורית עוד, והמחיר שלו — גלילה ראשונה אמיתית כלפי ראש
המסמך נמשכת חזרה פעם אחת — כבר לא קנה דבר. `applyPaneScroll`,
`repairPaneScroll` ומסלול החזרה מהרקע **נשארים**: הם מסלולים אחרים (מעבר
טאב, והצגה מחדש של תוסף שהוסתר) ולא נמדדו בסבב הזה.
**ומה שמכסה את זה היום:** `scripts/qa/scroll-switch-qa.mjs` (קומיט `119ceeb`),
רץ ב-`npm run check:scroll` וארוג ל-`verify:boundary` לצד `multi-doc-qa.mjs`.
הוא פותח שני מסמכים, גולל כל אחד, מחליף ביניהם הלוך ושוב, ומוודא שכל חזרה
נוחתת בדיוק על ההיסט הנזכר וששלב גלגלת אחרי החזרה זז בדיוק כמה שנשלח. ירוק
על המנוע הנוכחי, ואדום על 2.12.0, שם האיפוס נראה כבקשה לגלול לאפס בזמן
שהמיכל יושב במקום אחר.

**המדידה המקורית, שבגללה נכתב השומר:**

נמדד ב-superdoc 2.11.0 (docx-engine 0.10.0), Chrome אמיתי על ה-dist הארוז, שני
טאבים פתוחים עם מנוע חי בכל אחד. הדיווח שהוביל לכאן: „הוא זוכר איפה אני, וברגע
שאני מתחיל לגלול הוא חוזר לראש”.

התופעה **אינה** אובדן של `scrollTop`. הוא משוחזר נכון בכל נקודות הזמן שנמדדו
אחרי מעבר טאב, ואז גלגלת אחת מאפסת אותו:

| מתי נמדד `scrollTop` אחרי חזרה לטאב | הערך |
|---|---|
| מיד, באותה משימה | 720 |
| מיקרו-משימה | 720 |
| `requestAnimationFrame` | 720 |
| rAF שני | 720 |
| 150ms | 720 |
| **אחרי גלגלת אחת** | **0** |

הכתיבה היא **JS של המנוע** ולא של הדפדפן: `dist/assets/app.js:2046`, שמקורו
ב-`@superdoc/docx-engine`. הוא מחזיק snapshot **יחיד** של שורש הגלילה, ומזרים
אליו בגלילה הראשונה את הערך שנשמר בו. נמדד ישירות על ה-setter:
`{"ev":"set","asked":0,"was":720,"got":0}` — כלומר „בקשו 0, היה 720, יצא 0”.

**האפס אינו שרירותי:** הוא נקרא בזמן שהפאנל של הטאב האחר היה מוסתר. עם טאב
אחד אין איפוס בכלל, ולכן החתימה של הפער היא בדיוק „שני מנועים חיים חולקים
snapshot אחד”.

**ומה שהופרך באותה מדידה:** ההנחה שהייתה כתובה אצלנו — ש-`display: none` הורס
את קופסת הפריסה ולכן הדפדפן שוכח את הגלילה — אינה נכונה. Chrome **משחזר
בעצמו** אחרי מחזור `display:none`: מיכל שנגלל ל-840 קרא 840 בחזרה, באותה משימה
סינכרונית (840→0→840).

**לא היה ניתן לעקוף מהמנוע, וגם לא בהשמה.** האיפוס קרה מתוך הגלגלת של
המשתמש, כלומר אחרי כל השמה שאנחנו יכולים לעשות בזמן מעבר הטאב. לכן העקיפה
הייתה `guardPaneScroll`: מאזין חד-פעמי לאירוע הגלילה הראשון שאחרי הפעלת
טאב, שזיהה את החתימה („המיכל על ראש המסמך ואנחנו זוכרים אחרת”) והחזיר את
המיקום, והתפרק אחרי שרץ פעם אחת או ברגע שהמשתמש גלל בעצמו — שומר שנשאר דרוך
היה הופך תיקון חד-פעמי לנעילה. **הוא הוסר, כאמור למעלה**; התיאור נשאר כאן
מפני שהוא מה שצריך לבנות מחדש אם הפער יחזור.

**מה שנשלל במפורש כפתרון:** להחליף את `display: none` של טאב לא-פעיל
ב-`visibility: hidden`. נכונות ההדפסה נשענת בדיוק על כך שה-`display: none`
ה-inline שורד — `src/styles/print.css` אינו מצהיר `display` ל-`.document-pane`
— ולכן החלפה כזאת הייתה מדפיסה את **שני** הטאבים.

## תצוגה חיה של גופן — שלושה מסלולים, ואף אחד מהם אינו נאמן במסמך

נמדד ב-superdoc 2.11.0 (docx-engine 0.10.0), Chrome 152.0.7977.75, על ה-dist
הארוז. השאלה: המשתמש מרחף על שם גופן ברשימה, ואמור לראות מיד איך הטקסט הנבחר
ייראה. המימוש הראשון עשה זאת בכתיבה למסמך, וזה נשלל במדידה; שני המסלולים
האחרים אינם נוגעים במסמך כלל, ובכל זאת נשללו — מסיבה אחרת לגמרי.

**נמדד מחדש על superdoc 2.15.0-next.15 (15.9.2026) — שלושת הפערים פתוחים.**
ההרחבה הופעלה גם על מסמך האתחול וגם על מסמך שנפתח, עם
`{canRender: true, canUseShortcuts: false, canMutate: false}` ו-
`{highlight, decorate, inlineBox}` — כמקודם. ‏`scope: 'text'` עדיין אינו
מדויק לתו: דקורציה על יעד „two thre” צבעה את כל הריצה בת 23 התווים, ועל
`BBBB` נצבע כל העלה. אין זרימה מחדש: החלפה ל-Courier New הרחיבה את השורה
הרחבה מ-599 ל-883px — 282px מעבר לטור — ו-`scrollWidth` של העמוד עלה מ-794
ל-979 מול `clientWidth` 794, כלומר נחתך, בעוד מספר השורות והעמודים לא זז;
אחרי `dispose` הכול חוזר ל-599 / 794. וההיסטוריה נשארת נקייה: undo ו-redo
0 לאורך כל הצביעה ופירוקה, ו-`dispose` מותיר אפס class ואפס attribute.

**ושיטת המדידה נרשמת כאן, כדי שלא תיבנה מחדש:** ההרחבות נרשמות **בבנאי
בלבד** ואין API לרישום מאוחר, ולכן המדידה נעשתה בלי בנייה מחדש — סקריפט
שמוזרק לפני הבאנדל מוסיף getter לא-מנוי ל-`Object.prototype` שעונה **רק**
לאובייקט התצורה של `create-editor.ts` (מזוהה לפי המפתחות שלו) ומחזיר הרחבת
גשש אחת. אין בזה עריכת קוד מקור ואין דגל.

### נקודת הפתיחה: הכתיבה למסמך, ומה היא עולה

`doc.format.apply({ target, inline: { fontFamily } })` מדווח `success: true`,
ובאותה מדידה:

| | לפני | אחרי |
|---|---|---|
| `undoDepth` | 7 | **8** |
| `markDirty` | 39 | **40** |
| `word/document.xml` | — | **שונה** |
| `w:rFonts` בריצה | `[]` | `<w:rFonts w:ascii="Courier New" w:hAnsi="Courier New"/>` |

כלומר ריחוף אחד — בלי לחיצה — מסמן את המסמך כנערך, מתזמן שמירה, וקונה דרגת
undo. `Ctrl+Z` הראשון של המשתמש מבטל „גופן שהוא לא בחר”, ולא את מה שהקליד.
זה מה שהוביל ל-`FONT_PREVIEW_ENABLED = false` ב-`src/engine/font-preview.ts`.

#### ומה שנמדד מחדש: `history.undo` **אינו** מנקה אחריו, והוא הורג את ה-redo

התיקון המתבקש הבא הוא „לצבוע, ואז לבטל בעצמנו” — `format.apply` בריחוף,
`doc.history.undo()` ביציאה. הביטול עצמו אכן נקי להפליא, ונמדד:
`document.xml` חוזר **זהה בייט-בבייט**, `undoDepth` חוזר למקום, הבחירה נשמרת
(`selectionTarget` זהה), והגופן חוזר בפריים אחד. ובכל זאת המסלול נשלל, משתי
מדידות שאין להן עקיפה ב-API הציבורי:

**א. הריחוף מלכלך פעמיים, לא פעם.** `markDirty` נמדד גם על הצביעה וגם על
הביטול — שניהם סינכרוניים למוטציה (‎−5ms ו-‎−6ms מחותמת המוטציה). כלומר גם
„צבע ובטל”, שהוא נטו אפס במסמך, מותיר את המסמך מסומן „לא נשמר” **ומתזמן
autosave שכותב לקובץ של המשתמש** גופן שהוא לא בחר.

**ב. ה-redo של המשתמש נהרס — גם כשהוא ביטל את התצוגה.** זו המדידה שמכריעה,
והיא מקצה לקצה: המשתמש הקליד `Q`, ביטל, ויש לו `redoDepth: 1` אמיתי שממתין.

| הצעד | `undoDepth` | `redoDepth` | מה שם |
|---|---|---|---|
| לפני התצוגה | 13 | **1** | ה-`Q` שהמשתמש ביטל |
| הריחוף צובע | 14 | **0** | ה-redo של המשתמש **נמחק** |
| היציאה מבטלת | 13 | **1** | ה-redo הוא עכשיו **הגופן שלנו** |

ואז `Ctrl+Y` מחזיר `Courier New` במקום את ה-`Q`. כלומר ריחוף שהמשתמש **ביטל**
מחק לו עבודה שהמתינה ל-redo, והחליף אותה בגופן שאיש לא בחר. אין לזה גידור:
מחיקת מחסנית ה-redo היא ההתנהגות הנכונה של כל עורך על מוטציה חדשה, ואין
ב-superdoc 2 שום דרך ציבורית למוטציה שאינה נוגעת בהיסטוריה (ראו הסעיף הבא).

זהו הפער שסוגר את המסלול לגמרי, והוא **חמור** מן החיתוך שלמטה: חיתוך הוא
כיעור, וזה אובדן עבודה.

### מה שהופרך: אין ProseMirror, ולכן אין `addToHistory: false`

התיקון המתבקש לכתיבה שמלכלכת היסטוריה הוא טרנזקציה מסומנת
`tr.setMeta('addToHistory', false)`. הוא אינו קיים כאן:

| מה נבדק | מופעים |
|---|---|
| `prosemirror` ב-`node_modules/@superdoc/docx-engine/dist/docx-engine.es.js` (11MB) | **0** |
| `setMeta` באותו באנדל | **0** |
| `addToHistory` בכל עץ המקור `C:/tmp/superdoc-src` | **0** |
| `prosemirror` ב-`THIRD_PARTY_NOTICES` של המנוע | **0** |

וברמת המופע החי: `activeEditor.view`, `.state` ו-`.commands` הם `null`;
`.chain`, `.extensionManager`, `.schema`, `.plugins` ו-`.registerPlugin` הם
`undefined`. כל כיוון שמניח ProseMirror מתחת ל-superdoc 2 מבוזבז מראש.

### מה שכן קיים, ועובד: ספק דקורציות ציבורי, render-only

החוזה קיים ומתועד ב-2.11.0 המותקן:

- `node_modules/superdoc/dist/superdoc/src/public/index.d.ts:180` — `defineSuperDocExtension`
- `node_modules/superdoc/dist/superdoc/src/public/index.d.ts:198` — `SuperDocDecorationProvider`
- `node_modules/superdoc/dist/superdoc/src/core/extensions/types.d.ts:172`, `:344`
- `node_modules/superdoc/dist/superdoc/src/core/types/index.d.ts:4099` — `Config.extensions?: SuperDocExtension[]`
- בעץ המקור: `packages/superdoc/src/core/extensions/types.ts:161`, `:357`, `:480`, `:521`, ודוגמה ב-`define.ts:37`

הרישום **חייב להיות בבנאי** — אין רישום מאוחר. `activate` נקרא פעם אחת לכל
מסמך, לפני `onReady`. `ctx.capabilities` שנמדד:
`{ canRender: true, canUseShortcuts: false, canMutate: false }`, ו-`ctx.visuals`
הוא `{ highlight, decorate, inlineBox }`. ידית `highlight` מדביקה class על
הטקסט, ואת הגופן מחליפים במשתנה CSS.

**השער שהרג את המסלול הראשון עובר כאן במלואו:**

```
הקלדה     → undoDepth 1
צביעה     → undoDepth 1     ← לא זז
Ctrl+Z אחד → ביטל את ההקלדה
```

בכל הגששים: `undoDepth` 0→0, `onEditorUpdate` 0→0, `onTransaction` 0→0,
`diagnostics.getSnapshot()` = `[]`, ו-**כל 12 החלקים ב-docx זהים בייט-בבייט**
לפני, בזמן ואחרי. אין `w:rFonts` כלל. ההחזרה מדויקת לפיקסל (רוחב 192 → 169 →
192). והביצועים אינם השיקול: הצביעה הראשונה 17–35ms, וריחוף לגופן הבא הוא
כתיבת משתנה CSS אחת — 200 החלפות ב-0ms, בלי קריאה למנוע. (המשמעות הנגזרת:
השהיית ה-300ms ב-`src/composables/font-preview.ts` מיותרת לכל צביעה שאינה
הראשונה בסבב.)

**עמידות שנמדדה:** ה-class שורד עימוד מחדש בזום ושורד הקלדה בפסקה אחרת בזמן
שהצביעה חיה, ו-`handle.dispose()` מותיר אפס class ואפס attribute. זה **מפל**,
לא כתיבה לצומת — צמתים שנולדים מעימוד מחדש מקבלים את הכלל מעצמם. לשם השוואה,
`Range.surroundContents` כן עובד (רוחב 473.83 → 494.75) אך **נמחק בהקלדה
הראשונה באותה פסקה** (`wrappersLeft` 1 → 0).

### ובכל זאת נשלל: המנוע אינו זורם מחדש, והתצוגה משקרת

זה הפער האמיתי, והוא **משותף לכל מסלול חזותי** — הוא נובע מ-`position:
absolute` ברוחב מחושב מראש, לא מהטכניקה שמחליפה את הגופן. החלפת גופן משנה רוחב
תווים; המנוע אינו מודד מחדש; הריצה גדלה במקום.

| מה נמדד | Arial (מקור) | בזמן הצביעה |
|---|---|---|
| רוחב הריצה | 389px | **518px** |
| חריגה מעמודת הטקסט | 0 | **122px** |
| `scrollWidth` של העמוד (מול `clientWidth: 794`) | 794 | **824** |
| מספר שורות / עמודים | 3 / 1 | 3 / 1 — **ללא שינוי** |

ומכיוון שהעמוד הוא `overflow: hidden`, 30 הפיקסלים האלה **נחתכים בפועל**. מה
שהמשתמש רואה, מילה במילה מן הצילום:

```
לפני:      The quick brown fox jumps over the lazy dog while the BOLDRU…
בריחוף:      e quick brown fox jumps over the lazy dog while the BOLDRU…
```

תחילת המשפט נעלמה מעבר לקצה.

**המכניקה, ולא מה שנראה במבט ראשון:** העלים כן נמצאים בזרימת inline רגילה,
ולכן ריצה שהתרחבה **דוחפת** את שכנתה ואינה נוחתת עליה — נמדד: השכנה זזה 1px עם
Courier ו-‎29px עם Impact, וחפיפה אנכית **אפס בכל המקרים** (גובה השורה מקובע).
מה שאין הוא **שבירת שורות מחדש**: השורה כולה מתארכת מעבר לטור, ומה שלא נכנס
נחתך בקצה. על שורה שמילאה את הטור נמדד גרוע פי שניים מהטבלה למעלה — ריצה
517 → **759px**, גלישה **237px** מתיבת הטקסט, **141px** מחוץ לעמוד ו-
`scrollWidth` 794 → **939**, כלומר 145px גלילה אופקית שלא הייתה. במסלול החוק
הגלובלי נמדד אותו דבר: ריצה 601 → 797px, **195px** מעבר לשוליים ו-**99px מעבר
לקצה הנייר** — הטקסט על האפור שמסביב לדף.

גופן **צר** נקי לגמרי (Impact: אפס גלישה, `scrollWidth` ללא שינוי). גופן רחב על
שורה מלאה — מכוער, ובלי גידור אפשרי: איננו יודעים מראש אם הטור יספיק.

**ופגם שני, שאין לו תיקון בצד שלנו:** פס הבחירה נשאר במקומו בזמן שהטקסט זז
מתחתיו. נמדד בזרימה האמיתית (בוחרים, ואז מרחפים) — הפס ב-`462..993`, הטקסט עבר
ל-`302..993`; פער של 160px ו-198px. הסימון הכחול והאותיות נפרדים על המסך.

זרימה מחדש הייתה פותרת את שניהם, והיא מחייבת מדידה מחדש של הטקסט במנוע —
כלומר בדיוק את מה ש-`canMutate: false` שולל, ואת מה שהצביעה החזותית נמנעת ממנו
בכוונה. **התצוגה במסמך אינה יכולה להיות נאמנה, ותצוגה שמראה משהו אחר ממה
שיקרה בפועל גרועה מאין תצוגה.**

### פער שלישי: `scope: 'text'` אינו מדויק לתו, בניגוד לחוזה שלו

`SuperDocVisualOptions.scope` מתועד כ„`'text'` paints exact visible text
ranges”. נמדד אחרת: דקורציה על `BBBB` בפסקה `AAAA |BBBB| CCCC` צבעה את **כל
עלה `BBBB`** — לא את הבלוק, אך גם לא את התווים המדויקים. האנקור עצמו כן פותר
נכון (`segments()` = `[{ blockId, range: { start: 5, end: 9 } }]`), ולכן חוסר
הדיוק הוא בצייר.

בבאנדל הסגור (`docx-engine.es.js`, פונקציות `Wxn`/`Z2o`/`eBo`/`tBo`) יש שני
מסלולים — מוכוון-טווח כשה-host מספק callback לצביעת טווח, ונפילה לכל הבלוק
בלעדיו. שרץ הראשון, ולכן התוצאה היא עלה ולא בלוק.

**וזה מה שמכריע את הפיצ'ר, יותר מן החיתוך.** נמדד מקצה לקצה, ריצה אחת:

| | הטקסט |
|---|---|
| הריצה במסמך | `one two three four five` (23 תווים) |
| מה שהמשתמש סימן | `‎ two thre` (9 תווים) |
| מה שהריחוף צבע | `one two three four five` — **כל 23** |
| מה שהלחיצה שינתה בפועל | `‎ two thre` בלבד — מאושר ב-OOXML |

כלומר התצוגה מראה שינוי רחב פי שניים וחצי ממה שיקרה. הכתובת מדויקת והצביעה גסה,
וזו הטעיה ולא אי-דיוק.

הסתירה לחוזה מנוסחת בעץ הפתוח עצמו —
`packages/superdoc/src/core/extensions/types.ts:497`: „paints exact visible text
ranges … never silently paints the whole block”. מכיוון שהצייר סגור, מה שפתוח
כאן הוא **דיווח באג**, לא PR.

הצייר של `highlight` (התכונות `data-superdoc-extension-decoration` ו-`-classes`)
נמצא **רק** בבאנדל הסגור — אפס מופעים בכל עץ המקור הפתוח, ולכן אין כאן PR.

### שלוש חסימות נוספות שנמדדו, לחוסך למי שינסה שוב

**`::highlight()` בכרום 152 אינו נושא `font-family`.** עם
`background: yellow; color: red; font-family: "Courier New"; font-weight: 900`
הצבעים כן נצבעו, אבל `Range.getBoundingClientRect().width` נשאר **46.31 →
46.31**. גם עם `font-family` ו-`font-size: 32px` בלבד — רוחב 46.31, גובה 17.
ה-Custom Highlight API הוא קבוצת תכונות צביעה, ואינו יכול להחליף גופן.

**`window.getSelection()` אינו רואה את הבחירה.** עם בחירה חיה ומאומתת
(`{ status: 'ready', empty: false }`, פס כחול על המסך): `rangeCount: 1`,
`collapsed: true`, `textLen: 0`, `clientRects: 0`. ה-`activeElement` הוא
`<textarea data-v2-ime-host>` בגודל 1px ובאטימות 0. המנוע אינו מניח את הבחירה
ב-DOM, ולכן כל מסלול שנשען על `Range` מן הדפדפן חסום מהיסוד. גם למפות
מודל→DOM בעצמנו אינו פתוח: ה-API נותן `{ blockId, offset }` וה-DOM נושא
`data-pm-start`/`data-pm-end` במספור אחר (גלובלי מול מקומי, סטייה של אחד).

**`!important` הוא חובה, לא סגנון.** המנוע כותב `font-family` **inline** על כל
`span.superdoc-text-run`, ולכן כלל CSS על class — כולל class שידית דקורציה
מדביקה — לא יתפוס בלעדיו. נמדד: כלל ירושה על המיכל נכשל לחלוטין
(`computedFF` נשאר `Arial, __superdoc_core_symbols__, sans-serif`), ואותו כלל
עם `!important` הצליח. מימוש בלי `!important` נכשל **בשקט**.

### `paintInlineBoxes` מדלג על RTL לחלוטין — וזה פתוח ל-PR

`C:/tmp/superdoc-src/packages/layout-engine/painters/dom/src/runs/inline-box.ts`
פותח את `paintInlineBoxes` ב-`if (isRtl) return;`. החבילה
(`@superdoc/painter-dom`) היא **קוד פתוח**, בשונה מצייר ה-`highlight`. אינו
נדרש לתצוגת הגופן, ונרשם כאן מפני שהוא הפתח היחיד שנמצא במסלול הזה לתיקון
במעלה הזרם.

**מה שהשתנה עד 2.15.0-next.15 הוא הדיווח ולא ההתנהגות (נמדד 15.9.2026):**
תיבת inline על פסקה לטינית **כן** משתתפת בפריסה — השורה גדלה מ-139.6
ל-167.6px — ועל פסקה עברית השורה אינה זזה כלל (118.1px). אבל הדילוג כבר
אינו שקט: ההרחבה מקבלת אזהרה בדיאגנוסטיקה שהיעד הוא פסקה RTL שאינה נתמכת,
וה-`.d.ts` הציבורי מצהיר במפורש שתיבות inline נתמכות ב-LTR בלבד ונכשלות
סגור. כלומר זה **חוזה מתועד** היום, ולא הפתעה — וההחזרה המוקדמת עדיין שם
(שורה 162; הקובץ שונה לאחרונה ב-25.8.2026), כלומר הפתח ל-PR פתוח כשהיה.

### מה שנשלח במקומו

תצוגה שאינה במסמך: **פס דגימה בתחתית רשימת הגופנים**, שמציג את הטקסט הנבחר של
המשתמש בגופן שמרחפים עליו. הוא נאמן — אין בו עימוד לשקר עליו — ואינו נוגע
במסמך, בהיסטוריה או בשמירה. הטקסט מגיע מ-`doc.selection.current({ includeText:
true })`, שנמדדה כקריאה טהורה: 1ms, ו-`history.get()` זהה לפניה ואחריה.

**ובגודל שבמסמך, ולא בגודל של הרשימה.** הפס צייר בתחילה ב-14px קבועים, וזה
הפער שהשאיר אותו „הנה האותיות” במקום „הנה איך הטקסט שלי ייראה”: גופן נראה אחרת
לגמרי ב-11pt וב-20pt, ומי שבוחר כתב לספר בוחר את הצירוף. הגודל מגיע עכשיו
מוכן מ-`use-font-controls` (`sampleSize`) — גודל הבחירה, מומר `pt × 4/3`
ל-CSS px, וחסום ל-`[12,24]px` (`SAMPLE_SIZE_MIN_PX`/`MAX_PX`) מפני שהפס יושב
ברשימה בגובה קבוע: 72pt הם 96px והיו שוברים את הרשימה, ו-6pt קטנים מכדי
להראות צורה. בטווח הנפוץ (12–18pt) הגודל יוצא מדויק, ומעליו הצורה נשמרת
והמידה מוקטנת. הפקד רק מצייר — ההמרה והחסימה יושבות אצל הקורא, שהוא היחיד
שיודע מה גודל הבחירה.

**מלכודת מדידה שיש לרשום:** `doc.history.get()` מחזיר **Promise**. בלי `await`
הוא נראה כאובייקט ריק `{}`, וכל שער undo שנשען עליו „עובר” בלי למדוד דבר. שתי
מדידות ראשונות בסבב הזה נפלו בדיוק כך.

**ומה שנשלל במפורש כפתרון:** להשתמש ב-`.superdoc-text-run` כסלקטור. היא מחלקה
פנימית של המנוע, מאותה קטגוריה ש-`tests/unit/engine-boundaries.test.ts` אוסר
ושנדחתה כבר ב-`src/engine/line-number-layer.ts`. נמדד ש-`.editor-stack__host *`
— מחלקה שלנו (`src/sessions/editor-swap.ts`) — נותנת תוצאה זהה בדיוק, ולכן גם
אילו המסלול החזותי היה נשלח, לא היה בו צורך בשם פנימי.

## תצוגה מקדימה של ריווח בתוך הדף — הכיוון האנכי, ולמה גם הוא אינו נאמן

הרשומה שמעליה („תצוגה חיה של גופן”) מדדה את הכיוון ה**אופקי**: החלפת גופן משנה
רוחב תווים, המנוע אינו שובר שורות מחדש, והטקסט נחתך. מרווח שורות וריווח פסקה
הם הכיוון ה**אנכי**, וזה אינו נגזר מן הראשון: רוחב השורה אינו משתנה כלל, ולכן
שבירת השורות — מה שהרג את הראשון — אינה נדרשת כאן בכלל.

נמדד ב-superdoc 2.14.0-next.5, Chrome, על ה-dist הארוז, מסמך של שלושה עמודים
ו-33 פסקאות. הגשש: `scripts/spacing-preview-probe.mjs`. **נמדד מחדש על
2.15.0-next.15 (15.9.2026): כל שורות הטבלה למטה משחזרות לפיקסל, וכך גם
הפיצוי בקסקייד ושני הפערים שהרגו את המסלול** — העמוד אינו מתעמד מחדש
(17/16 פסקאות ו-`layout-epoch` שאינו זז, מול 16/16 ו-epoch 2 בשינוי האמיתי),
ומלבני הבחירה זהים לפני ואחרי ההזזה החזותית.

### מה שהמנוע מצייר, ומה שאי אפשר להזיז בו

תיבת הפסקה היא `position: absolute` עם `height` קשיח, ו**כל שורה בתוכה נושאת
`line-height` ו-`height` בסגנון inline**. שני אלה יחד סוגרים את רוב המסלולים:

| מה שהוחל על הפסקה | פסיעה בין שורות | דיו הפסקה | השכנה זזה | חפיפה |
|---|---|---|---|---|
| `line-height: 32px` | 18.4 → **18.4** | ללא שינוי | לא | — |
| `line-height` + `height: auto` | 18.4 → 37 | +55.8px | לא | **55.8px** |
| `line-height` + `height: 32px` | 18.4 → 32 | +40.8px | לא | **40.8px** |
| `margin-top: 40px` | ללא שינוי | +40px | לא | **40px** |
| `padding-top: 40px` | ללא שינוי | +40px | לא | **40px** |
| `transform: translateY(40px)` | ללא שינוי | +40px | לא | **40px** |
| `margin-bottom: 40px` | ללא שינוי | ללא שינוי | לא | — |

`line-height` לבדו הוא **אפס אפקט**: ה-`height` ה-inline של השורה גובר עליו.
ומה שגדל בתוך התיבה אינו מגדיל אותה (h נשאר 55.2 בכל השורות של הטבלה) — ולכן
הטקסט נוחת על הפסקה הבאה.

### הפיצוי בקסקייד כן עובד — בתוך העמוד

`line-height` + `height` על היעד, ועליהם `translateY(Δ)` על **כל** האחיות
שאחריו (`~ .superdoc-fragment`):

```
פסיעה בין שורות ביעד: 32 / 32px     (בסיס 18.4 / 18.4)
הפסקה הבאה זזה ב-40.8px
חפיפה: אין
```

הקבוע הוא `שורות × Δ` ולא `(שורות−1) × Δ`: תיבת הפסקה גדלה בגובה של **כל**
שורה ולא במרווחים שביניהן (נמדד: h 55.2 → 96, כלומר 40.8 = 3 × 13.6).

### ובכל זאת נשלל — שני פערים שאין להם גידור

**1. העמוד אינו מתעמד מחדש, והשינוי האמיתי כן.** אותו ריווח בדיוק דרך
`setSpacing({line: 480, lineRule: 'exact'})`:

| | התצוגה המדומה | האמת (API) |
|---|---|---|
| פסיעה בין שורות | 32px | 32px |
| הפסקה הבאה זזה | 40.8px | 40.8px |
| הרכב העמודים | 17 / 16 — **ללא שינוי** | **16 / 16** |
| `layout-epoch` | 1 (לא זז) | 2 |

כלומר בפסקה עצמה התצוגה מדויקת לפיקסל, אבל **פסקה שנדחקת לעמוד הבא אינה
נדחקת בה**. במקומה הטקסט גולש מתחת לשולי העמוד: 40.8px על שורה של 32px,
ו-232.8px מתיבת הטקסט ו-107.9px **מעבר לקצה הנייר** על 96px. והצירוף `~`
אינו חוצה עמודים, ולכן העמודים שאחריו אינם זזים כלל.

**2. פס הבחירה נשאר במקומו בזמן שהטקסט זז מתחתיו.** נמדד: ראשי השורות עברו
מ-553.2 / 571.6 / 590 ל-553.2 / 585.2 / 617.2, ומלבני הבחירה **זהים לפני
ואחרי**. אותו פגם בדיוק שנמדד בתצוגת הגופן.

שניהם נובעים מאותו דבר: פריסה שחושבה מראש, ותצוגה חזותית שאינה מבקשת אותה
מחדש. עימוד מחדש הוא `canMutate`, וזה בדיוק מה שמסלול הדקורציות שולל בכוונה.

### מה נשלח במקום

פס תצוגה **בתוך התפריט** — `src/ui/ribbon/common/SpacingPreview.vue` — על אותו
חישוב שהדיאלוג מצייר לפיו (`engine/paragraph-preview.ts`). אותה הכרעה כמו בפס
הדגימה של בורר הגופן: התצוגה יוצאת מהמסמך אל הפקד, ושם היא נאמנה למה שתעשה.

## גל 23 — אזכור „@” וקישורי עומק

### `hyperlinks.wrap` נכשל על טווח שנכתב זה עתה

`wrap` מחזירה `INVALID_TARGET` (`"hyperlinks.wrap could not resolve the target
range."`) על טווח שנוצר בקריאת `doc.insert` שקדמה לה — גם כאשר
`doc.ranges.resolve` על **אותו טווח בדיוק** מחזירה את הטקסט הנכון ואת אותם
היסטים. **נמדד בגשש חד-פעמי שלא נכנס למאגר** — הוא נכתב לסבב המדידה ההוא
ולא הוגש, ולכן אין קובץ כזה בעץ (ההפניה שהופיעה כאן קודם ל-
`scripts/qa/wrap-after-insert-probe.mjs` הצביעה על שם שמעולם לא היה במאגר).
מה ששרד הוא הפלט:

```
resolved: { text: "פסחים דף לד", start: 4, end: 15 }
wrap:     { success: false, code: "INVALID_TARGET" }
```

**שלוש הסתעפויות שנשללו במדידה:**

- **תזמון** — ניסיון שני אחרי `requestAnimationFrame` + 60ms נכשל זהה.
- **`blockId` שהתחלף בהחלפה** — ה-`blockId` נקרא מהבחירה שאחרי ההכנסה והוא
  זהה לזה שלפניה (`41964671`).
- **סינון הסכימה** — `wrap` עם `href` של `https://example.com/x` על אותו טווח
  נכשל באותה הודעה. כלומר ההודעה על „הטווח” אינה מכסה על דחיית `otzaria:`.

`wrap` נמדדה עובדת רק על הטווח שהמשתמש סימן בעצמו — כך היא נקראת
ב-`src/engine/hyperlinks-manage.ts`, וזה המסלול שממשיך לעבוד.

### מה שנשלח במקומו

מחיקת הטווח ואז הוספה: `doc.insert({ value: '' })` על האזכור, ואז
`doc.hyperlinks.insert({ target: נקודה, text, link })`. שתי הפעולות נמדדו
מצליחות ברצף, והתוצר נבדק ב-OOXML ולא בקבלה — `<w:hyperlink r:id>` בתוך
`document.xml` יחד עם ה-`Relationship` התואם ב-`document.xml.rels`.

`hyperlinks.insert` לבדה אינה מספיקה: נמדד שהיא **מוסיפה ואינה מחליפה**, וללא
המחיקה שלפניה נשאר „@פסחים לד” לצד הקישור שנוצר.

### `otzaria://` נחסם לציור, ולכן הקישור אינו לחיץ בתוך העורך

ה-DomPainter מריץ כל `href` דרך סניטציה שמשווה את הסכימה מול רשימה קבועה —
`http`, `https`, `mailto`, `tel`, `sms` (ו-`ftp`/`sftp`/`irc` כאופציונליות) —
וחוסם את השאר עם `[DomPainter] Blocked potentially unsafe URL`. הקריאה
לסניטציה נעשית **בלי להעביר אופציות**, כך ש-`allowedProtocols` הקיים בחתימה
אינו נגיש מנקודת השימוש, ואין קונפיגורציה בבנאי.

**מה זה כן שובר ומה לא:** הקישור נכתב ל-DOCX תקין ועובד בכל תוכנה שפותחת את
הקובץ. מה שאינו עובד הוא לחיצה עליו **בתוך העורך שלנו**:
`hyperlinks.onActivate` (`src/engine/otzaria-link-activation.ts`) אינו נקרא, כי
המנוע חוסם את ה-href לפניו. ה-handler נשאר מחווט — הוא נכון, זול, ויתחיל
לעבוד ברגע שהסכימה תעבור.

השער `scripts/qa/at-mention-qa.mjs` מודד את הפער הזה בצעד נפרד ומדווח אותו
כ„חלקי”, כך שסגירתו במעלה הזרם תיראה מיד.

### `hyperlink-nested-unsupported`

הוספת קישור כשהסמן כבר בתוך קישור קיים נכשלת בקוד הזה, עם הודעה באנגלית מן
המנוע. `at-mention-overlay.ts` מתרגם אותו להודעה בעברית ואינו מנסה לעקוף.

## הקלדה — `:has()` בגיליון המארח מחשב סגנון לכל המסמך בכל תו

נמדד ב-9.2026 על ה-`dist` הארוז (Chrome, trace של ה-renderer עם
`invalidationTracking`): כלל CSS **שלנו** עם `:has()` שעוגנו ישב בתוך `.superdoc`
גרם לחישוב סגנון מחדש של כל עץ המסמך על כל הקשה. הסיפור, המספרים והתיקון —
בראש README („איטיות בהקלדה”) ובהערת הכלל ב-src/styles/engine-chrome.css.

מה שנוגע למנוע, ולכן נרשם כאן: גיליון המנוע עצמו משתמש ב-`:has()`, ולכן קבוצת
האינבלידציה של `:has` בדף אינה שלנו לבד. בלי `:has()` שלנו בתוך `.superdoc`
התופעה אינה מופיעה (נמדד: מחיקת כללי המנוע לבדם ומחיקת הכלל שלנו לבדו נתנו
שתיהן 0), ולכן ההגנה היא בצד המארח: אין `:has()` שעוגנו יכול להיות אב של
צומת במסמך. tests/unit/css-hygiene.test.ts אוכף זאת סטטית, ו-
`npm run check:typing-recalc` מודד זאת בדפדפן.

**נמדד מחדש על 2.15.0-next.15 (15.9.2026) — התיקון מחזיק.** השער ירוק על
מסמך של ארבעה עמודים: 126 חישובי סגנון ב-40 הקשות, חציון של 16 אלמנטים,
שיא 863, ואף לא חישוב אחד שנוגע ב-1000 אלמנטים ומעלה.

**ומה שלא הוכרע — [#3984](https://github.com/superdoc/docx-editor/issues/3984)
(פתוח, „status: in-progress”).** הטענה היא שהקלדה בפסקה שיש בה `w:br` או
`w:tab` inline עולה פי שניים להקשה. **לא שוחזר כאן, ואינו נסגר.** שני זוגות
מדידה של השהיית הקשה→ציור, 40 פסקאות עבריות מול אותו מסמך עם `br` ו-`tab`
בכל פסקה: חציונים 70/74/62 מול 78/65/75ms בזוג הראשון, ו-77/64/75 מול
85/74/92ms בשני; ועל 2.14.0-next.5 באותם מסמכים 91/74/72 מול 87/80/74 —
כלומר אותה תמונה בשני המנועים, הפרש של עשרות אחוזים לכל היותר ולא פי שניים.
**אבל המדד אינו יכול להכריע:** הוא מקוונטט לפריים (‏`MutationObserver` ועוד
`rAF` על מרווח הקשות של 80ms), והמכונה הייתה ב-56%–100% מעבד לאורך המדידה
(שערי Chrome של סוכנים מקבילים). מה שיכריע הוא השוואת פרופיל או trace על
מכונה פנויה — לא מדידת השהיית ציור. עד אז: **לא נסגר, לא אושר**.

לא נשלח למעלה: הגיליון הזה הוא של `@superdoc/docx-engine` הקנייני, והרישיון שלו
אוסר לפרסם מדידות של המנוע. אם יידווח — כתיאור איכותי בלבד, בלי מספרים.

## מקלדת — `End` בשורה עברית הוא `Home`

נמדד ב-9.2026 על ה-`dist` הארוז (superdoc 2.12.0, Chrome אמיתי, מסמך שנפתח
דרך הבורר, סמן שהונח בלחיצה אמיתית): בפסקה שיש בה `<w:bidi/>` מקש `End` מעביר
את הסמן ל**תחילת** השורה המצוירת, כלומר בדיוק לאן ש-`Home` מעביר.

| הפסקה | הסמן באמצע | `Home` | `End` | מה שנכון |
|---|---|---|---|---|
| עברית, שורה גולשת 1 | 48 | 0 | **0** | 98 |
| עברית, שורה גולשת 2 | 144 | 98 | **98** | 191 |
| אנגלית, שורה 1 | 45 | 0 | 92 | 92 |
| אנגלית, שורה 2 | 141 | 92 | 157 | 157 |

`Shift+End` נגרר אחריו ובוחר אחורה (0..48 במקום 48..98). `Home`, `Ctrl+Home`
ו-`Ctrl+End` נמדדו תקינים.

**ומה שנכתב כאן קודם על החצים — שהם תקינים בעברית — אינו נכון, ונמדד
כשגוי.** הוא נכון לפסקה עברית רגילה בלבד: ברשימה ממוספרת או עם תבליט החצים
האופקיים לוגיים ולא חזותיים, ובגבול שבין פסקה מוזחת לרשימה הסמן נתקע. ראו
„חצים אופקיים בעברית” בסוף הקובץ. כלומר תנועת הסמן אינה מודעת-כיוון בשני
מקומות — `End`, והחצים לפי סוג הפסקה.

**מה זה עשה למשתמש:** זה הדיווח „בקובץ הוורד שהמסמך נשמר הנקודה עוברת לתחילת
המסמך”. מי שלוחץ `End` כדי לחזור לסוף המשפט ומקליד נקודה מקבל אותה בהיסט 0:
`<w:p><w:pPr><w:bidi/></w:pPr><w:r><w:t>.</w:t></w:r><w:r><w:t>ברוך הבא
לעולם</w:t></w:r></w:p>`. בפסקה הראשונה זו תחילת המסמך.

**נעקף בשכבה מבחוץ,** `src/engine/rtl-line-end.ts`: מאזין `keydown` על ה-div
שלנו (`paintedHost`), שקורא את הסמן ב-`activeEditor.host
.readLiveSelectionSyncSnapshot()` (סינכרוני — ולכן אפשר להחליט בתוך המאזין אם
לבטל את האירוע), גוזר את סוף השורה המצוירת מ-`data-pm-end` שלה פחות
`data-pm-start` של הפסקה, ומזיז את הסמן ב-`authoring.setSelectionTarget`.
ההמרה אינה ניחוש: באנגלית, שבה `End` של המנוע תקין, היא נותנת בדיוק את
המספרים שהוא נותן. שער: `npm run check:line-end`.

היסט שיושב בתפר בין שתי שורות גולשות נקרא כסופה של הקודמת, כדי ש-`End` חוזר
לא יטייל שורה-שורה. החריג הוא `Home` ואז `End`: המאזין זוכר את ה-Home עד
להקשה הבאה ומייחס את התפר לתחילת השורה השנייה; כל מקש אחר או לחיצת עכבר
מאפסים את הרמז. הקלדה בתפר, לעומת זאת, אכן מעבירה את הסמן לשורה הבאה
(נמדד: נקודה שהוקלדה בהיסט 98 נכנסה לתיבת השורה השנייה), ולכן `End` שאחריה
מגיע לסוף השורה השנייה.

**למה לא דרך API של פריסה:** `host.readMountedLayoutData()` אכן מתאר את
השורות, אבל ב-`fromRun`/`fromChar`/`toRun`/`toChar` ובלי אורכי הריצות — כלומר
אין דרך להמיר אותם להיסט בלי לנחש. זו הסיבה שהחריגה מגבול ה-DOM
(tests/unit/engine-boundaries.test.ts, „טווח ה-pm של השורה המצוירת”) נבחרה
עליה.

אין PR: הקוד שמזיז את הסמן אינו בחלקים הפתוחים של SuperDoc — בעץ המקור יש רק
הצהרת טיפוסים. **דווח למעלה כ-issue:**
<https://github.com/superdoc/docx-editor/issues/3986> — כתיאור התנהגותי
(מה שהמקש עושה, מה ש-Word עושה, וה-OOXML שיוצא), בלי טבלת המדידות שכאן, שהיא
מידע על המנוע הקנייני. **נמדד מחדש על superdoc 2.13.0 (מנוע 0.12.0), על עץ
נקי:** אותה התנהגות בדיוק — `End` בשורה עברית מחזיר 0 במקום 98, ובפסקה קצרה
הקלדת נקודה אחריו נותנת `<w:r><w:t>.</w:t></w:r>` לפני הטקסט. בכל שדרוג מנוע
לבדוק אם ה-issue נסגר, ואז למחוק את היירוט ואת השער במקום להשאיר עקיפה
מיותרת.

**ונמדד שוב על superdoc 2.15.0-next.15 (15.9.2026), על המנוע לבדו — פתוח.**
הפעם בלי לגעת בקוד: אחרי שהמסמך נפתח הוסר בזמן ריצה המאזין היחיד שלנו
ב-capture על ה-`div` שלנו (זוהה לפי המקור שלו, ואומת שהוסר אחד ושלא נשאר
אף אחד, ושמאזיני המנוע עצמם לא נגעו). התוצאה, שתי ריצות זהות: בשורה עברית
גולשת `End` מחזיר 0 במקום 98, בשורה השנייה 98 במקום 146, ו-`Shift+End`
מ-48 בוחר 0..48 — כלומר אחורה; בשורה אנגלית `End` תקין. הטבלה שלמעלה
עומדת מילה במילה. השער עם העקיפה עובר 8/8 באותו יום.

## מקלדת — חצים אופקיים בעברית: חזותיים בפסקה, לוגיים ברשימה, ומלכודת בגבול

נמדד ב-Chrome אמיתי על ה-`dist` הארוז (2.15.0-next.15, 15.9.2026), בגשש
הסקר `scripts/qa/rtl-caret-probe.mjs`, שמניח סמן בלחיצה אמיתית ומקיש חצים
אמיתיים: 24 שורות — 8 תקינות, 5 חלקיות, 11 שבורות. הטבלה המלאה יושבת בכותרת
הגשש; מה שמכריע יושב בטבלה הזאת:

| ההקשר | `ArrowRight` בפסקה עברית | מה שנכון |
|---|---|---|
| פסקה עברית רגילה | 2 → 1 → 0 — **חזותי**, הסמן נע ימינה על המסך | חזותי |
| רשימה ממוספרת (בכל סיומת: טאב, רווח, או כלום) | 0 → 1 → 2, ומאמצע הטקסט 6 → 7 → 8 — **לוגי**, הסמן נע שמאלה | חזותי |
| רשימת תבליטים, וטאב בתוך פסקה עברית | לוגי, כמו הרשימה הממוספרת | חזותי |
| בקרות: הזחה בלי מספור, ורשימה ממוספרת **לועזית** | חזותי בשתיהן | חזותי |

כלומר המודל אינו „לוגי” ואינו „חזותי” אלא **מתחלף לפי סוג הפסקה, באותו
מסמך**. זה מה שהופך את זה לפער ולא להעדפה: אותו מקש עושה שני דברים שונים
בשתי פסקאות סמוכות, והמשתמש אינו יכול לבנות הרגל.

שתי נגזרות שנמדדו באותה ריצה:

* **מלכודת בגבול.** בפסקה מוזחת שאינה ממוספרת, מהיסט 0, `ArrowRight` מקפיץ
  את הסמן הלוך ושוב בין תחילת הפסקה ובין היסט 15 של פריט הרשימה שמעליה —
  שש פעמים ברציפות. אין יציאה מהגבול הזה בחץ אחד.
* **`Shift+חץ ימני` יורש את הסתירה.** בפסקה הבחירה מתרחבת אחורה
  (‏[8,8] → [5,8]) — נכון; ברשימה היא מתרחבת קדימה (‏[6,6] → [6,9]) — הפוך
  ממה שהמשתמש רואה.

**אין עקיפה אצלנו, ובכוונה.** תנועת סמן אופקית אינה נגזרת משום קלט
שה-Document API חושף — אותה מסקנה בדיוק כמו ב-`End` — והיירוט של
`rtl-line-end.ts` הוא התבנית היחידה שהייתה מתאימה. אבל שם יש חישוב
חד-משמעי מתוך ה-DOM (‏`data-pm-start`/`data-pm-end` של השורה המצוירת), וכאן
היעד הנכון תלוי במודל הדו-כיווני שהמנוע מחזיק ואיננו רואים. לכן כאן הגשש
**מודד ומדווח בלבד**, ואינו מתקן.

דווח למעלה: [#3996](https://github.com/superdoc/docx-editor/issues/3996) —
פתוח, ונמדד פתוח על 2.15.0-next.15.
